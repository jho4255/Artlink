/**
 * 작가 한 명이 등록할 수 있는 작품 사진 수 (2026-09-22: 30 → **150**, 사용자 결정 — 공격적 마케팅을 위해 한도를 넓힘).
 *
 * ⚠️ 0(무제한)으로 두지 말 것 — 봇·실수로 수천 장이 들어와도 막을 게 없다. 넉넉한 상한을 두는 것과 없는 것은 다르다.
 * ⚠️ 프론트 `lib/artwork.ts` 의 `PORTFOLIO_IMAGE_MAX` 와 **같은 값**이어야 한다 — 화면이 먼저 막고 서버가 최종 판정한다.
 *    어긋나면 화면은 받는데 서버가 400 을 내거나(서버가 작을 때), 서버는 받는데 화면이 먼저 막는다(화면이 작을 때).
 *    `portfolio-artwork.test.ts` 가 프론트 소스를 읽어 대조한다.
 */
export const PORTFOLIO_IMAGE_MAX = 150;
