-- 작가 자료제출 마감일
--
-- 신규 공모 등록에서는 필수지만 컬럼은 **nullable** 이다.
-- 이미 실서버에 올라가 있는 공모 12건에는 이 값이 없고, 임의로 채워 넣으면
-- 갤러리가 작가에게 안내한 적도 없는 기한이 생겨 버린다.
-- 대신 갤러리가 공모 상세에서 직접 한 번 입력하게 하고(그 뒤로는 Admin 만 수정),
-- 값이 없는 동안에는 화면에서 관련 안내를 아예 띄우지 않는다.

ALTER TABLE "Exhibition" ADD COLUMN "submissionDeadline" TIMESTAMP(3);
