# ArtLink 로컬 데모 가이드 (팀 시연용)

배포본(`deploy/render` = `9ed4713`) **이후 추가된 기능**을 로컬에서 채워진 데이터로 시연하기 위한 문서입니다.
현재 새 기능은 전부 아직 **미배포(working tree)** 상태입니다.

---

## 0. 준비 — 실행 + 데모 데이터 채우기

> DB 는 **데모 DB `artlink`** 를 씁니다(실사용자 클론 `artlink_prod` 아님).

```bash
# 1) DB 시작(WSL) + 앱 실행
sudo service postgresql start
bash run_web.sh          # 마이그레이션 + 기본 시드 + 백엔드(:4000)+프론트(:5173)

# 2) 데모 데이터 채우기 (backend/ 에서, 세 개 모두 멱등 — 여러 번 돌려도 안 불어남)
cd backend
# ⚠️ backend/.env 의 **로컬** DATABASE_URL 을 그대로 넣으세요(DB 이름이 artlink 인 것).
#    실서버 복제본(artlink_prod)을 가리키면 데모 시드가 실제 가입자 데이터를 건드립니다.
#    레포에 접속 문자열을 그대로 적지 않습니다 — scripts/predeploy-check.sh 가 막습니다.
export DATABASE_URL='<backend/.env 의 로컬 DATABASE_URL>'
npx tsx scripts/seed-community-samples.ts   # 커뮤니티 샘플 글 5
npx tsx scripts/seed-sample-show.ts         # 진행중 전시 2
npx tsx scripts/seed-demo.ts                # 이웃·스토리·방명록·광고 + 데모 로그인 비번
# (선택) 작가 전시 8단계: npx tsx scripts/seed-artist-stages.ts
```

**정리(데모 후):** 각 스크립트에 `--clean` 을 붙여 데모분만 지웁니다.
`[샘플]`/보이지 않는 표식으로 골라 지우므로 실데이터는 건드리지 않습니다.

### 로그인 계정 (비밀번호 `demo1234`, 일반 로그인 폼)

| 역할 | 이메일 |
|------|--------|
| 작가 | `artist1@artlink.com` |
| 작가 | `artist2@artlink.com` |
| 갤러리 | `gallery@artlink.com` |
| Admin | `admin@artlink.com` |

> `seed-demo.ts` 가 이 4계정에 `demo1234` 를 심습니다. dev-login 없이 로그인됩니다.

---

## 1. 홈 화면 개편  *(로그인 불필요)*

- **최상단이 작품**(ArtWorks) — 새로고침마다 랜덤 노출.
- 그 아래 **배너(슬라이더)** — 10초 자동, 좌우 화살표는 호버 때만.
- 그 아래 **2×2 그리드(1:1)**: `[인기글 | 진행중인 전시]` · `[마감임박 공모 | 주목할 갤러리]`.

**시연:** `/` 접속 → 작품 새로고침 → 배너 → 아래 네 블록이 열 맞춰 정렬된 것 확인(진행중 전시·마감임박 공모에 샘플이 채워짐).

## 2. 커뮤니티 게시판 (블라인드식)  *(읽기 공개, 쓰기 로그인)*

- 글마다 **실명/익명** 선택 · 좋아요 · 댓글 · **조회수**.
- 목록 우측 **[내 글] · [내 댓글]** 필터, **[글쓰기]**(전용 페이지).
- 홈 좌측 **인기글 위젯**의 출처.

**시연:** `artist1` 로그인 → `/community` → [글쓰기](사진 첨부 가능, 익명 토글) → 목록/상세에서 좋아요·댓글 → [내 글]/[내 댓글] 필터 → 홈 인기글에 뜨는지 확인.

## 3. ArtStory (소식 · 스토리 피드)  *(로그인)*

- 마이페이지 **우측 사이드바 [ArtStory · 소식 공유]**(카메라 아이콘) → `/feed`.
- 작업 사진+짧은 글 올리기, **공개범위 글마다**(전체공개/이웃공개).
- 좋아요 · 댓글 · **[좋아요 누른 사람]** 명단.
- 피드 = **내가 이웃한 사람 + 나** 의 스토리.

**시연:** `artist1` → [소식] → 이미 artist2·gallery 스토리가 보임(이웃이라) → 좋아요/댓글 → [좋아요 누른 사람] → 직접 스토리 올리기.

## 4. 이웃 (단방향 팔로우)  *(로그인)*

- **작가 홈페이지**(`/portfolio/:id`)·**갤러리 상세**의 **[이웃 추가]**. 역할 무관(작가↔갤러리).
- 추가하면 상대에게 **알림**. 이웃의 스토리가 내 [소식]에 뜬다.

**시연:** `artist2` 로그인 → 다른 작가 홈페이지 [이웃 추가] → 그 작가로 로그인해 알림(벨) 확인 → [소식]에 그 사람 글이 뜨는지.

## 5. 방명록  *(작가 홈페이지 하단)*

- 공개 글 + **비밀글**(본문만 가림, 신원은 보임) + **답글은 방 주인만**.
- 홈페이지 **헤더 [방명록]** 버튼으로 바로 이동, 제목 옆 글 수.

**시연:** `artist1` 홈페이지(`/portfolio/1`) → 헤더 [방명록] → 이미 남겨진 글/비밀글/답글 확인 → 다른 계정으로 글 남기기(비밀글 체크) → 방 주인(artist1)으로 답글.

## 6. 광고 배너 (Admin)  *(Admin)*

- 마이페이지 **[광고 관리]** 탭에서 이미지·문구·링크·활성·순서 등록.
- 활성 배너가 **우측 사이드바 로그아웃 아래**에 'AD' 라벨과 함께 노출.

**시연:** `admin` 로그인 → [광고 관리] → 이미 등록된 데모 광고 확인/수정 → 사이드바 하단에 뜨는지, 클릭 시 링크 이동.

---

## 7. 이전 세션 추가분 (같은 미배포 묶음, 데이터는 흐름으로 생성)

- **ArtTalk 대화 첨부** — 갠톡/단톡에서 사진·동영상·파일 전송(용량 상한). 작품 모달/작가 홈페이지 [메시지]로 방 열기.
- **갤러리 스카우팅·초대** — 갤러리가 하트로 작품 모으고(관심 작품 탭) 작가에게 **전시 초대**. 작가는 [내 전시]의 '초대받은 전시'에서 수락 → 바로 참가.
- **공모 운영 인라인** — 갤러리 [내 공모]의 [상세 운영]이 카드 안 아코디언. 작가 [내 전시]에서 공지·자료제출·정산을 카드 안에서.
- **포트폴리오/홈페이지 편집** — 좌우 2단(입력/실시간 미리보기), PDF 포맷 4종, ArtLook(액자 걸기).

> 이 흐름들은 `seed-artist-stages.ts`(작가 전시 8단계)로 상태별 데이터를 채우면 시연이 쉽습니다.

---

## 참고

- 새 백엔드 라우트: `community` · `follow` · `stories` · `guestbook` · `ads` (+ `chat` 첨부).
- 새 마이그레이션: `add_chat_attachment` · `add_community` · `post_images_views` · `add_follow_story` · `add_guestbook` · `story_social_and_ads`.
- 상세 규칙은 `CLAUDE.md` 의 「커뮤니티 · 소셜」·「홈 레이아웃」 절 참고.
