# ArtLook 화질 골든 회귀 (visual regression)

액자 렌더의 화질을 **눈이 아니라 수치로** 지키는 장치. "비슷해 보인다"는 판정 근거가
될 수 없어서 만들었다(2026-08-30).

## 왜 있나

ArtLook 결과물이 경쟁사(FrameIt)에 비해 "잘라 붙인 티가 난다"는 지적이 반복됐다.
원인을 추측으로 짚었다가 두 번 틀렸다(→ `artlook-scene-engine` 메모). 그래서 **레퍼런스를
고정해 두고 같은 코드로 재는** 방식으로 바꿨다.

## 쓰는 법

```bash
cd scratchpad/vt
node render.mjs                 # 스위트 10케이스 렌더 + 정확한 기하 기록
python run.py                   # 지표 + 임계 판정
python run.py --crops           # 100% 코너/변 크롭까지 (증거)
python run.py --save NAME       # 이번 결과를 기록으로 저장
python run.py --vs NAME         # 저장된 기록과 비교 (회귀 감시)
node sweep.mjs frames           # 액자 18종 × 매트 2단계 전수
node sweep.mjs scenes           # 장면 15개 전수
python golden.py                # FrameIt 레퍼런스를 같은 코드로 재측정
python mat.py                   # 매트·틈 지표 (2차 라운드)
python presence.py              # 존재감 지표 — 벽 소란도·주인공 크기·색 정합 (3차)
python artpreserve.py           # 작품 보존 — 자름/늘림/재채색/디테일 변형 감시 (3차)
python sweepcheck.py frames|scenes   # 전수 스윕을 모든 지표로 판정
node masks.mjs t01              # 디버그 마스크·그림자 레이어 + BEFORE/AFTER 추출
python masksheet.py t01         # 그 레이어로 시트 만들기 (상호배타·유출 검사 포함)
python audit4.py                # 그림자 감쇠 곡선·경계 날카로움을 골든과 대조
python wallcalm.py --write      # 장면별 벽 진정 세기 초기값(원본 결 기준)
python calmtune.py --write      # ★ 렌더를 보고 되먹임으로 최종 조정 (sweep scenes 먼저)
python lightdir.py --write      # 장면별 광원 방향을 **측정해서** scenes.json 에
python lightref.py              # FrameIt 조명 샘플(~/ArtLink/light/) 역분석 — 설계 근거
node light.mjs strengths        # 조명 강도 0/25/50/75/100 × 장면 4개
python lightcheck.py            # ★ 조명 판정 (작품 보존 · 방향성 · 마스크 테)
node light.mjs frames           # 조명 켠 채 액자 18종 × 매트 전수
SYN=0 SYNDIR=0 node render.mjs renders_off   # 합성 음영을 끈 기준 렌더
python syncheck.py              # ★ 7차 — 합성이 **지배적인가** (원본 대비 기여도)
python physics.py               # ★ 6차 — 그림자 방향 · 얇은 선 · 틈 바닥 (골든 대조)
node framelight.mjs             # ★ 6차 — 액자 18종이 **광원을 따르는가** (판만, 장면 없이)
node minsize.mjs                # 실치수 모드에서 작은 작품이 얼마나 작아지는가
```

로컬 dev 서버(`localhost:5173`)가 떠 있어야 한다. Python 은 numpy/pillow/scipy 필요.

## 구성

| 파일 | 하는 일 |
|---|---|
| `suite.json` | 10 케이스 정의(세로/가로/정사각 · 밝은/어두운 · 액자 5종+절차적+캔버스랩 · 매트 3단계 · 평벽/실내) |
| `render.mjs` | 각 케이스를 렌더하고 **페이지의 계측 훅**(`window.__artlook`)에서 조각·작품 사각형을 받아 적는다 |
| `metrics.py` | 단면 프로파일 기반 지표. 레퍼런스와 우리 렌더에 **같은 코드**를 쓴다 |
| `run.py` | 임계 판정 · 반복 간 비교 · 크롭 생성 |
| `golden.py` | FrameIt 레퍼런스 5장의 손확정 기하 + 같은 코드 측정 |
| `cases/` | FrameIt 레퍼런스 (저작권상 **커밋하지 않는다**) |
| `lightref.py` | FrameIt 조명 샘플 16장 역분석. 강도 4단계에서 '무엇이 변하는가'를 뽑는다 |
| `light.mjs` / `lightcheck.py` | 조명 슬라이더 회귀. 작품 dE·채도비·대비비·그늘구석·방향성·경계 spike |
| `reports/` | 반복별 기록(JSON). `--vs` 로 회귀를 잡는다 |

## 지표와 임계값 (FrameIt 실측에서 유도 — 통과시키려고 느슨하게 고치지 말 것)

| 지표 | 뜻 | 골든 실측 | 임계 |
|---|---|---|---|
| `keyline_spike` | 벽↔액자 경계가 양옆보다 얼마나 더 어두운가 = 오려 붙인 티 | 0.0~8.2 | ≤12 |
| `rebate_ratio` | 작품이 액자 **뒤로** 들어간 깊이(상대 대비) | 0.26~0.80 | ≥0.24 |
| `rail_span` | 살 단면 밝기 낙차 = 몰딩인가 인쇄한 띠인가 | 7~54 | ≥20 |
| `dir_tb` | 위살↔아래살 밝기차 = 방향광을 받는가 | 30~90 | ≥15 |
| `contact_drop` | 접지 그림자 세기(**절대** 밝기 낙차) | 22~79 | 20~85 |
| `recover_pct` | 그림자가 사라지는 거리(조각 폭 %) | 1~24 | 5~30 |
| `aspect_err` | 작품 비율 보존 % (CLAUDE.md 18) | 0 | ≤0.5 |

### 3차 라운드 — 존재감(`presence.py`)

"고급 제품 사진인가, 벽에 합성한 AI 이미지인가"를 잰다. 앞의 두 라운드가 **조각 안쪽**만
쟀다면 이건 **조각과 배경의 관계**다 — 같은 액자라도 벽이 시끄러우면 싸구려 목업이 된다.

| 지표 | 뜻 | 골든 실측 | 임계 |
|---|---|---|---|
| `piece_pct` | 조각이 화면에서 차지하는 **면적** % | 4.5~44.1 (Pro 44.1) | 26~58 |
| `wall_hf` | 벽의 고주파 결(1200px 정규화 std) | 2.9~12.6 (기준벽 2.9) | ≤4.5 |
| `hf_ratio` | 조각 디테일 ÷ 벽 디테일 | 1.0~5.3 | ≥1.3 |
| `con_ratio` | 조각 국부대비 ÷ 벽 국부대비 | 1.1~5.3 | ≥1.05 |
| `sat_rail` | 살의 채도 폭 — 크면 '플라스틱 나무' | 6~36 | ≤40 |
| `temp_dl` | 살 ↔ 벽 색상(ab) 거리 = 색온도 정합 | 6~35 | ≤40 |
| `rail_cv` | 네 살 폭의 변동계수 = 기하 왜곡 | 0~0.08 | ≤0.06 |

⚠️ **스위트는 작품 치수를 주지 않는다**(`selectedWork = null`). 2026-08-31 부터 실제
동작은 **실제 크기 단일 모드**라, 치수가 있는 작품은 벽 대비 실제 비율로 걸려 `piece_pct`
가 훨씬 낮아진다(흰 벽돌 기준 4호 5.7% · 30호 43.6%). 여기 지표는 **합성 품질**을 재는
것이지 실사용 크기를 재는 게 아니다 — 둘을 혼동하지 말 것.

⚠️ `wall_hf` 는 **밴드**다(2.5~4.5). 상한만 두면 벽을 뭉갤수록 점수가 좋아진다 —
그건 "액자만 선명하고 배경만 부드러운" 또 다른 합성 티다(골든 최저 2.86).

### 5차 라운드 — 살 길이방향 변화(`rail_pct`)

`audit5.py` 가 다섯 축을 골든과 대조한다: `edge_acut`(경계 날카로움) · `ao_lit`(빛 받는
쪽 폐색) · `rail_along`/`rail_pct`(살 길이방향 변화) · `gap_stages`/`gap_dark`(틈 구조·깊이) ·
`center_off`(중심 이탈). 이 중 **명확히 미달인 건 `rail_pct` 하나**였다(골든 8.5~104,
우리 3). 나머지는 전부 골든 범위 안이었고, `edge_acut` 의 이상치 2건은 **벽과 살 대비가
4밖에 안 되는** 측정 불가 케이스였다(가드를 20 으로 올려 해소).

`rail_pct` 는 `presence.py` 의 상시 지표로 들어갔다(≥8.0, 골든 최저 8.5).

### 4차 라운드 — 합성 해상도·그림자 꼬리·디버그 마스크

`node masks.mjs <case>` → `python masksheet.py <case>` 가 **엔진이 직접 뽑은** 여섯 마스크
(outer / front face / inner bevel·rabbet / mat / artwork opening / 접지·투영·반그림자)와
BEFORE·AFTER 를 한 장으로 만든다. 마스크를 하니스에서 다시 계산하지 않는 게 핵심 —
`?debug=1` 훅이 **본 렌더와 같은 quad·같은 워프**로 만들어 준다. 상호배타·판 밖 유출을
검사해 어긋나면 실패한다.

⚠️ **스윕(`sweep/`)은 JPEG 0.9 로 저장한다.** 얇고 얕은 골(슬림 액자 + 넓은 매트의
리베이트 21레벨)에서는 압축 잡음이 지표를 몇 % 흔든다 — 10케이스 스위트(`renders/`, PNG)
쪽이 판정 기준이다.

`artpreserve.py` 는 작품 자체를 원본 파일과 직접 대조한다(비율≤0.5% · ΔRGB≤14 ·
채도비 0.90~1.15 · 구조상관≥0.985). **ΔRGB 를 0 으로 만들려 하지 말 것** — 방 조명과
전혀 무관한 작품은 오려 붙인 것으로 보인다.

면제 규칙(임계 완화가 아니라 **해당 없음**):
- `rail_span` — 살 12px 미만은 프로파일을 담을 자리가 없다(레퍼런스도 8px 짜리는 6.8).
- `rebate_ratio` — 액자 없는 캔버스랩엔 리베이트가 물리적으로 없다(FrameIt 캔버스랩 0.133).

## ⚠️ 측정에서 겪은 함정 (다시 밟지 말 것)

1. **조각 사각형을 이미지에서 다시 찾지 말 것.** 색으로 찾으면 투영 그림자가 조각으로
   잡혀 경계가 16px 밀린다(case_00 실측). 우리 렌더는 계측 훅이 정확히 알려준다.
2. **두께가 있으면 실루엣 ≠ 앞면.** 캔버스랩은 뒷면이 더 나가므로 앞면만 재면 멀쩡한
   렌더가 검은 테를 가진 것처럼 나온다(실측 40.4 → 실제 결함 없음).
3. **비율 보존은 작품 사각형으로 잰다.** 실루엣으로 재면 옆면 때문에 어긋난다.
4. **리베이트를 작품 안에서 재지 말 것.** 어두운 그림이면 그림 자체가 최소값이라
   액자와 무관하게 큰 값이 나온다.
5. **절대값 지표는 재질·벽 밝기에 흔들린다.** 검은 액자는 옳게 그늘져도 절대 낙차가
   작고, 어두운 방에서는 `dir_tb` 가 줄어든다. 그래서 리베이트는 비율로 본다.
6. **우리 워터마크를 벽으로 세지 말 것.** 우하단 'ArtLink' 는 획 굵기가 딱 2~8px 대역이라
   대비 100 이 넘는 글자가 표본에 섞인다 — 흰 벽돌(원본 결 3.22)이 **5.29** 로 측정된
   원인이 이것이었다. 벽이 시끄러운 게 아니라 글자를 벽으로 세고 있었다.
7. **접지 그림자를 깊게 훑지 말 것.** 실내 장면은 작품 아래에 협탁·콘솔이 있어서 띠의
   끝이 가구다(t03 실측: 벽 158 → 159px 아래부터 34). 기준 밝기가 무너져 멀쩡한 그림자가
   **−104** 로 뒤집힌다. 지금은 ①물체를 만나면 띠를 끊고 ②'회복해 도달한 밝기'(띠의
   최댓값)를 기준으로 삼고 ③낙차는 **고정 위치가 아니라 가장 어두운 지점**에서 잰다
   (조각이 커지면 '폭의 1%'가 이미 절반쯤 회복된 자리가 된다 — stone 실측 15 → 7).

## 남은 예외 (렌더 결함이 아님 — 확인 끝난 것들)

| 케이스 | 지표 | 이유 |
|---|---|---|
| t03·boutique·collector-salon | `recover_pct` | 광원이 거의 수평이라 **그림자가 옆으로** 진다. 지표는 아래만 훑는다 |
| collector-salon·collector-home | `dir_tb` | 방의 벽이 아래로 갈수록 밝아(바닥 반사) 액자 프로파일 명암을 상쇄한다 |
| stone | `contact_drop` | 벽 자체에 세로 기울기가 있어 국소 기준이 낮게 잡힌다(실제 낙차 15) |
| 화이트 @ 흰 벽돌 | `rail_pct` | 6.1~7.7 vs 8.0. 흰 살 + 흰 벽이라 길이방향 변화가 가장 작다. 같은 액자가 다른 벽(t02)에서는 통과 |
| 블랙 슬림 + 넓은 매트 | `rebate_ratio` | 0.233 vs 0.24. 스윕(JPEG)에서만 나오는 경계값 — 같은 액자가 PNG 스위트(t09)에서는 통과 |

## 조명 지표 (2026-08-31) — `lightcheck.py`

레퍼런스는 FrameIt Pro 조명 샘플 16장(`~/ArtLink/light/`, **커밋 금지**).
프리셋 12 + **같은 프리셋의 강도 4단계**(20/50/70/100)가 있어 설계를 수치로 정할 수 있었다.

| 지표 | 뜻 | 강도 100 실측 | 임계 |
|---|---|---|---|
| `작품 dE` | 조명 0 대비 작품 픽셀의 평균 \|ΔRGB\| | 0.7~2.8 | ≤12 |
| `채도비` | 작품 채도 비 | 0.90~1.00 | 0.90~1.10 (artpreserve 와 **같은 값**) |
| `대비비` | 작품 지역 대비 비 | 0.98~1.00 | ≥0.90 |
| `그늘구석` | 광원 **반대쪽** 구석의 밝기 배율 | 0.67~0.70 | 0.62~0.84 |
| `방향성` | 광원 쪽 구석 배율 ÷ 그늘 쪽 배율 | 1.42~1.49 | ≥1.06 (1 이면 그냥 비네트다) |
| `경계 spike` | 조각 경계의 검은 테 (조명이 마스크 테를 만드는가) | 2.3~4.6, 무변화 | ≤12 |

⚠️ **FrameIt 과 같은 값을 목표로 삼지 말 것.** 그쪽은 강도 50 부터 작품이 무너진다
(작품 채도 92.6 → 63.7 → 46.5, 지역 대비 14.2 → 9.4). 우리 강도 100 은 그쪽 30~40 쯤이고,
그 대신 작품 보존 약속(채도비 0.90)을 **조명을 끝까지 올려도** 지킨다.

⚠️ **구석 배율을 네 구석 평균으로 재지 말 것** — 편심 필드라 광원 쪽 구석은 거의 안 어두워져
평균이 희석된다. 조명 세기는 **그늘 쪽 구석**으로 본다.

## 6차 라운드 (2026-08-31) — 물리 일관성

브리핑: "효과를 더 세게 넣지 말고, **하나의 촬영 환경에서 액자만 교체된 것처럼** 만들라."
앞선 라운드가 전부 통과하는데도 남은 지적이라, 지표를 **세기에서 관계로** 바꿨다.

| 지표 | 뜻 | 골든 | 전 | 후 |
|---|---|---|---|---|
| `dir_cos` (physics) | 그림자가 광원 **반대쪽**으로 지는가 | — | (미측정) | **9/9**, 최소 0.969 |
| `aniso` | 한쪽으로 몰렸나 / 사방 헤일로인가 | 3.5~71 (중앙 16.2) | 중앙 **5.6** | 중앙 7.2 |
| `gap_floor_rel` | 액자↔작품 틈 바닥 ÷ 살 | 0.11~0.38 | **0.002** (플로터 3종) | **0.12~0.55** |
| `rail_line` | 살의 1~2px 임펄스(=그어 넣은 선) | 2.1~14.4 | 최대 **60.6** | 최대 30.7 |
| `Δ` (framelight) | 액자가 광원에 반응하는 폭 | — | 플로터 **0** | 전 18종 5.1~37 |
| 작품 밝기 편차 | 액자를 바꿀 때 작품이 변하나 | 0 이어야 | 1.6 | **0.5** |

### ⚠️ 지표를 만들다 두 번 틀렸다 (같은 실수를 반복하지 말 것)

1. **`gap_step`(한 표본 최대 낙차)은 버렸다.** 밝은 매트 옆 어두운 그림, 검은 살 옆 흰
   매트 같은 **정당한 재질 경계**를 stroke 와 구분하지 못한다. 실제로 브리핑이 시킨
   "작품 둘레 검은 stroke 제거"를 했더니 매트가 밝아져 이 값이 71→106 으로 **올랐다**
   — 고칠수록 나빠지는 지표였다. 구분하는 건 **중앙값 필터 잔차**다(단조 경계는 통과,
   1~2px 임펄스만 남는다).
2. **`dir_ok`(가장 어두운 변이 기대한 변인가)도 버렸다.** 좌상단 광이면 아래·오른쪽이
   **정확히 동점**이라 1% 차이로 성패가 갈려 멀쩡한 렌더가 4/9 로 나왔다. 네 변의 세기를
   **벡터로 합쳐 각도**를 본다(`dir_cos`).

⚠️ `aniso` 는 **벽 결이 바닥을 만든다** — 벽돌 줄눈이 있으면 그림자가 없어도 3~6% 가
나온다. 그래서 비(比)만 보지 말고 `dir_cos` 를 함께 볼 것. 우리 광원 쪽 변은 이미
2~6% 로 사실상 깨끗하고, 골든과의 차이는 대부분 **그늘 쪽 세기**(22 vs 27~50)다 —
브리핑이 "더 dramatic 하게 만들지 말라"고 했으므로 거기까지 올리지 않았다.

⚠️ `rail_line` 의 잔량은 **우리가 그린 선이 아니다.** 판(plate)만 재면 화이트 4~6 인데
장면 합성 후에는 22~25 다 — 워프·축소가 만드는 것이고, 골드(30.7)는 액자 **사진 자체의
반사 줄무늬**다(위치가 매번 달라진다. 우리가 그린 선은 늘 같은 자리에 나온다 — 그렇게
오크의 88% 스톱 꺾임을 찾았다). 재질 텍스처는 브리핑 7번이 보존하라고 한 것이다.

## 7차 라운드 (2026-09-01) — 덜어내기

기준이 바뀌었다: **'입체감이 더 강해졌나'가 아니라 '아무것도 안 한 것처럼 자연스러운가'.**
사용자가 든 실패 조건 — [액자 → 흰 테두리 → 검은 선 → 흰 매트 → 작품] 으로 층이 보이면 실패.

⚠️ **바닥값(`>=`) 임계를 골든 밴드로 바꿨다.** 바닥값은 "효과를 더 넣어라"로 민다 —
`rail_span` 은 바닥 20 을 통과하면서 골든(7~54)의 1.7배까지 부풀어 있었다.

⚠️⚠️ **절대값으로는 "합성이 지배적인가"를 못 가른다.** `rail_span` 이 밴드를 넘길래 음영을
더 깎으려 했는데, **합성을 완전히 끄고 재도 73.5**(켜면 75.2)였다 — 액자 **사진 자체**의
대비였다. 그래서 `syncheck.py` 는 **같은 입력으로 합성만 끈 렌더와 비교**한다.

| 지표 | 뜻 | 임계 | 현재 |
|---|---|---|---|
| `syn_delta` | 합성이 살 단면 대비를 몇 % 바꾸는가 | ≤25% | 0.9~20% |
| `dir_gain` | 장면 광원 추종이 **생기는가**(위살↔아래살 증가분) | ≥8 | +15~32 |

면제(임계 완화가 아니라 **해당 없음**): `syn_delta` 는 "원본 사진 보존"을 재는 것이라
**사진 액자에만** 뜻이 있다(절차적 액자는 보존할 원본이 없다). `dir_gain` 은 SYN 노브로
끄고 켜서 재는데 캔버스 랩의 옆면 음영은 그 노브를 타지 않는다(실제로는 광원을 따른다).

전/후 비교는 `?syn=`·`?syndir=` 쿼리로 같은 입력에서 뽑는다.

## 8차 라운드 (2026-09-01) — 상수를 재서 조건부로

브리핑: "모든 규칙은 **조건부**다. SCENE/GEOMETRY/MATERIAL/VISIBILITY 에 따라 달라야 한다.
자산에 이미 있으면 다시 만들지 말고(SOURCE-FIRST), 확신이 없으면 덜 해라(CONFIDENCE-AWARE)."

7차까지의 `SYN_DIR`·`SYN_EDGE` 는 **전역 상수 하나**였다. 그래서 액자 18종 × 장면 15개에
같은 세기를 걸었고, 노브를 돌리면 어떤 조합은 좋아지고 어떤 조합은 나빠졌다(= 라운드마다
오락가락한 이유). 8차는 그 상수들을 **측정값에서 유도**한다.

### 매트 '없음' 복원 (2026-09-01) — `nomat.mjs` / `nomat.py`

44e 의 '매트 필수'를 되돌리면서 브리핑의 **NO-MAT SAFETY SYSTEM** 을 지표로 만들었다.
매트를 빼면 **매트가 가려 주던 것이 드러난다** — 실측(흰 벽돌, 작품 경계 임펄스):

| | 매트 좁게 | 매트 없음(수정 전) | 매트 없음(수정 후) |
|---|---|---|---|
| 검정 흰 테두리 | 12.2 | **40.2** | **8.1** |
| 월넛 흰 테두리 | 15.6 | **34.2** | **2.7** |
| 샴페인 플로터 검은 선 | 10.3 | **19.5** | 12.1 |
| 아이보리 박스 검은 선 | 14.6 | **23.5** | 11.6 |

원인 둘 — ①사진 액자의 **밝은 안쪽 립**(촬영 때 개구부에 댄 회색판의 반사광)이 어두운
작품에 직접 닿는다 ②플로터의 트레이 폐색과 작품 접지 그림자가 **같은 자리에 겹친다**.
고친 방향은 브리핑 그대로 — **테두리를 더하지 않고 물리적으로 없어야 할 것을 덜어낸다**.

⚠️⚠️ **절대값으로 판정하지 말 것.** 합성을 끄고 재도 골드 42.8 · 블랙 40.2 다 —
골드의 42.8 은 그 액자의 **금장 비드**(재질)다. `syncheck.py` 와 같은 논리로 **합성이
더한 몫만** 본다(≤8). 현재 18/18, 기여 중앙 **−1.0**(사진에 있던 걸 덜어낸다).

⚠️ `suite.json` 의 `mat: 0` 여섯 케이스는 44e 이후 조용히 0.05 로 클램프돼 있었다.
'없음'이 살아나면서 **진짜 매트 없음**이 됐다 — 그런데도 `run.py` 8/10 동률이고
액자 전수 스윕은 19/36 → **24/36** 으로 올랐다.

### 8차 하니스

```bash
node assetlight.mjs     # ★ 액자 자산에 **이미 구워진** 조명 vs 우리가 더하는 양
node scenelight.mjs     # ★ 장면별 벽 밝기·조명 낙차·기록된 광원과의 일치도(신뢰도)
node levels.mjs         # 폴백 사다리 ?level=0..3 을 네 장으로
python3 ab.py A B out.png t01 t02 …   # BEFORE/AFTER 를 **세 배율**로 (썸네일/보통/확대)
```

### 측정이 뒤집은 전제 두 개

1. **CLAUDE.md 37 의 "사진 액자는 네 변이 똑같이 밝다"가 5종 전부에서 틀렸다.**
   원본 PNG 실측 위살−아래살: `black +27.8 · gold +15.8 · walnut +20.6`(위에서 온 빛),
   **`oak −17.3 · white −41.2`(아래에서 온 빛)**. 그래서 우리 음영은 액자마다 다른 일을
   하고 있었다 — 앞의 셋은 **중복**(이미 있는데 또 +7~21), 뒤의 둘은 **싸우다 지고**
   (−41 → −12) 모순된 두 램프가 겹친 뿌연 중간을 남겼다.
   지금은 잔차로 계산한다: 이미 충분하면 **손대지 않고**(실측 add=0.0), 어긋나면
   **한 번에** 목표까지 옮긴다(상한 ±16%).
2. **장면 15개 중 6개는 광원 방향의 근거가 없다.** 벽 낙차 실측 0.0~4.5(흰 벽돌은 정확히
   0.0)인데 전부 손으로 적은 `[-1,-1]` 이었다 — 근거 없이 45° 대각광을 **발명**해
   액자 음영과 그림자를 그 방향으로 그려 왔다. `sceneLightModel` 이 낙차를 재서
   **가로 성분만** 줄인다(세로=위에서, 는 실내의 보편 가정이라 유지).

### 지표에서 고친 것

| 지표 | 무엇이 문제였나 | 어떻게 |
|---|---|---|
| `dir_tb` → `dir_rel` | **절대값**이라 어두운 액자가 재질 때문에 실패했다. 골든 5장의 살 평균이 120~209 라 **어두운 액자가 한 장도 없다** | 골든을 같은 코드로 재면 상대값 0.146~0.747 → 비율로 판정 |
| `syncheck` 의 `dir_gain` | "우리가 방향광을 **더했는가**"를 재서, 자산이 이미 맞아 아무것도 안 한 최선의 상태를 FAIL 로 잡았다 | **결과물이** 장면 광원을 따르면 통과 |
| `physics` 의 `dir_cos` | 하니스가 `scenes.json` 날것을 읽어 엔진의 보정된 방향과 어긋났다(3건 오검출) | 렌더가 **실제로 쓴 광원**을 geometry.json 에 기록해 그걸 쓴다 |
| `physics` 의 그림자 방향 | 두께가 있는 조각(캔버스 랩)에서 **실루엣**으로 재니 뒷면이 더 나간 쪽만 그림자가 삐져나와 기하가 방향으로 오독됐다(cos 0.32) | 계측 훅이 **뒷면 사각형**(`back`)을 알려준다 → 10/10, 최소 0.920 |

### 시도했다가 되돌린 것 (같은 실수를 반복하지 말 것)

- **매트 사면을 물리 치수(4겹 1.4mm ⇒ 4.5%)로 좁히기** → `bevel_abs` 47→71 · 96→120 ·
  74→93 으로 **골든(33)에서 더 멀어졌고** `gap_pct` 도 64→69 로 밴드를 벗어났다.
  넓은 코어가 매트와 작품 사이의 **완충**이다(실제 8겹 매트는 3mm). 8.5% 유지.
- **폐색 램프를 넓히기(0.74→0.58)** → `rebate_ratio` 는 오르지만 `gap_pct` 가 밴드를
  벗어난다(= 사용자가 실패라고 한 [액자|검은 띠|매트] 방향). 통과 개수는 같고 사고만
  옮겨진다. 결정 근거는 **자산의 실제 기하** — 사진 5종의 골이 0.70~0.80 에 있다.
- **그림자 세기를 신뢰도로 깎기** → `contact_drop` 이 전 케이스에서 내려갔다(t07 19.9→17.6).
  근거 없는 방향 문제는 이미 방향 보정이 해결했다. **방향 = 신뢰도, 세기 = 물리.**
- **가로 성분을 0 까지 죽이기** → 완벽한 좌우 대칭 자체가 CG 신호다(캔버스 랩 왼쪽 옆면이
  0.91→0.79 로 떨어져 세로 검은 띠로 보였다). 40% 를 바닥으로 남긴다.

### 결과 (7차 → 8차)

| | 7차 | 8차 |
|---|---|---|
| `syncheck` 합성 기여 | 0.9~20% (10/10) | **0.0~17.7%** (10/10) |
| `physics` 그림자 방향 | 10/10, 최소 0.774 | 10/10, **최소 0.865** |
| `physics` aniso 중앙 | 4.71 | **6.03** (골든 16.16) |
| `mat` 매트 방향광 | 8/9 | **9/9** |
| `run.py` | 8/10 | **8/10** (동률) |
| `artpreserve` | 10/10 | 10/10 |

남은 실패는 문서화된 둘 — t03(어두운 벽 + 거의 수평 광), t09(경계값 0.212).

---

## 9차 — 매트 없는 액자의 리베이트 (2026-09-01)

새 하니스 셋:

```
node matrix.mjs                 # 브리핑 TEST MATRIX 16케이스 (밝은/어두운 나무·흰·플로터 × 매트 × 장면)
python3 sheet.py matrix out.png [--vs 기준디렉터리] [키워드…]
                                # ⭐ 썸네일 | 실물크기 | 4배 확대 를 한 장에 — **매 실험마다 이걸 본다**
python3 profile.py matrix --all # 살을 가로지르는 단면을 숫자로 (벽|액자|매트|작품)
node plate.mjs                  # 장면 합성 **이전**의 판 단면 — 결함이 판인지 합성인지 가른다
```

**기준선을 남기는 법** (브리핑 REGRESSION PROTECTION):

```
cp -r matrix matrix_best                                  # 지금이 CURRENT BEST
cp frontend/public/artlook/{index.html,scene.js} best/    # 엔진도 함께
# 실험 후
python3 sheet.py matrix sheet_ab.png --vs matrix_best
```

옛 엔진으로 다시 렌더해 비교해야 할 때는 `best/index.html` 을 제자리에 넣고 돌린 뒤 **반드시
복원 확인**할 것(`grep -c REB_TROUGH`). 예전에 `cp` 를 `scratchpad/vt` 에서 돌려 파일이 엉뚱한
데 떨어진 적이 있다.

### 이번에 배운 것 — **지표가 결함을 못 보는 경우가 있다**

- `nomat.py` 의 임펄스는 중앙값 필터(size 5) 잔차라 **3px 넘는 띠를 못 본다**.
- `rebate_ratio` 는 골의 **최솟값만** 본다.
- 그런데 눈에 '층'으로 보이는 건 골 **다음에 되올라오는 폭 4~6px 의 띠**였다(검정 1.54배).
  8라운드를 살아남은 이유다. **`sheet.py` 를 눈으로 본 뒤에야** 찾았다.

### 한계값은 반드시 레퍼런스로 잴 것

둘 다 상상해서 적은 값이었고 둘 다 틀렸다.

| 지표 | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| `syncheck` 의 `rail_span` | 골든은 살을 **앞면만** 재서 넣는데(`golden.py` case_00: rail 16 / gap 36) 우리 `railPx` 는 리베이트를 **포함**한다 — 다른 구간을 비교하고 있었다 | `metrics.py` 에 `face_span`(리베이트 앞까지) 추가, `syncheck` 을 그걸로. 그 값으로는 우리 합성이 앞면 대비를 **줄인다**(−26%·−7%) |
| `nomat.py` "더한 임펄스 ≤8" | **골든 사진도 전부 탈락**한다(밝은 1.5~27.4 · 어두운 12.4~24.7). 매트 없는 액자의 안쪽 턱은 원래 또렷한 어두운 전이다 | 골든을 같은 표에 찍고 그 범위(×1.5)로 판정. 그 기준으로도 실제 결함(33~37)은 잡힌다 |

⚠️ 잘못된 잣대를 들고 튜닝하면 **헛돈다** — 실제로 슬림 액자 임펄스가 33→21→33 으로 세 번
왕복했다. 브리핑 "FAILURE IS A LEARNING SIGNAL: 80%·60%·40% 를 차례로 시도하지 말고
**이 효과가 있어야 하는가**를 물어라" 가 여기서는 "**이 지표가 맞는가**" 였다.

### 되돌린 것 / 하지 않은 것

- **작품 가장자리 접지 그림자 강화** → 하지 않았다. 골든의 그 값은 0.82~0.92(1~2px)로 우리와
  차이가 거의 없고, 깊이 신호는 이제 리베이트가 만든다. 같은 성질을 두 기전으로 말하지 않는다.
- **원근 시차(작품을 액자보다 뒤에 두고 따로 워프)** → 검토 후 기각. `scenes.json` 15개가
  전부 `region`(정면 평면 벽)이라 시차가 0 이다. 옛 `opening` 장면이 돌아오면 그때 다시.

### 결과 (8차 → 9차)

| | 8차 | 9차 |
|---|---|---|
| 무매트 사진 액자 끝 밝기 | 0.83~**1.54** | **0.52~0.60** (골든 0.43~0.63) |
| 앞면보다 밝은 픽셀 | 최대 5px | **0px** |
| 슬림 액자 골 | 0.80 | **0.34** (골든 0.15~0.40) |
| `rebate_ratio` 무매트 | 0.30~0.39 | **0.60~0.63** |
| `nomat` | 18/18 (기준 무효) | **18/18** (골든 기준) |
| `run.py` / `artpreserve` / 그림자방향 | 8/10 · 10/10 · 10/10 | 동률 |
| **매트 있는 렌더** | — | **픽셀 하나도 안 바뀜** |

---

## 10차 — '선인가 파인 자리인가' (2026-09-01)

새 하니스: **`edge.py`** — 선으로 읽히는 조건을 셋으로 나눠 잰다(세기가 아니라 **모양**).

```
python3 edge.py --golden      # 실제 사진 5장 (기준)
python3 edge.py matrix        # 우리 무매트 렌더
```

| | 뜻 | 골든 | 9차 우리 |
|---|---|---|---|
| `band` | 골↔끝 차이 ÷ 앞면. 작으면 **평평한 띠**=선 | 0.00~0.46 (중앙 0.28) | 중앙 0.07 |
| `cross` | 작품 첫 px ÷ 본문. 1.0 이면 경계에서 **뚝 끊김** | 0.76~1.06 (중앙 0.95) | 중앙 0.97 |
| `width` | 앞면의 90% 아래로 내려간 폭(px) | 2.5~20.5 | 2.5~12.5 |

**`cross` 가 이미 골든 범위**라 "폐색이 작품 경계에서 끊긴다"는 가설은 기각됐다 —
작품 둘레 그림자를 더할 이유가 없다(9차·10차 두 번 확인. 다시 시도하지 말 것).
원인은 `width` 였다: 약한 케이스는 전부 **조각이 작게 그려지는 렌더**다.

### 이번에 배운 것

- ⚠️ **해상도 게이트를 판 좌표로 두면 한 번도 작동하지 않는다.** `kd` 가 그랬다 — 판은 작품
  원본 크기라 늘 넉넉했다. 화면에 몇 px 로 그려지는지를 넘겨야 한다.
- ⚠️ **좁을 때 '얕은 띠'로 수렴시키지 말 것.** 골만 얕게 하면 골=끝인 평평한 띠가 남는데
  그게 정확히 '그어 놓은 선'이다. **양 끝을 함께 1 로** 보내야 한다.
- ⚠️ **밝은 링의 원인을 자산의 립에서 찾지 말 것.** 얇은 실버 자산은 개구부 직전이 0.94~1.05 로
  깨끗한데 임펄스 44.6 이었고, 검정 자산은 3.68배인데 5.5 로 통과했다. 차이는 **회복 구간의 폭**
  (4.3px ↔ 2.6px)이다.
- ⚠️ **자산을 고치려다 멀쩡한 것을 깎을 수 있다.** 개구부의 촬영판을 `L ≥ 앞면` 으로 잘라내려
  했는데 기존 5종의 **문서화된 립**(검정 3.68 · 월넛 3.00배)과 구분이 안 돼 골드를 3px 깎았다.
  git 으로 되돌렸다. 렌더 시점 보정(44h)이 이미 레벨을 잡고 있으므로 자산은 건드리지 않는다.

### 보호 대상 확인 방법

사용자가 "지금 자연스러운 것들"이라고 지목한 상태를 **먼저 렌더해 두고** 픽셀로 대조한다.

```
node matrix.mjs matrix_r9        # (엔진을 그 상태로 되돌린 뒤)
python3 sheet.py matrix out.png --vs matrix_r9 m11 m13 m21
```

10차 결과: 매트 전 케이스 · 실버 플로터 무매트 · 캔버스 랩 **0px**,
큰 사진 액자 5종 최대 4~14레벨(0.2~1.0% 픽셀).
