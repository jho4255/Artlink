# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

ArtLink — 갤러리-아티스트 매칭 모바일웹 플랫폼 (PWA). 3가지 역할: Artist(포트폴리오/지원/리뷰), Gallery(갤러리/공모 등록), Admin(승인/운영). Frontend와 Backend가 분리된 모노레포 구조.

> 🔑 **새 세션/계정 인계 시**: `HANDOFF.md`의 **0장(계정 인계)** 을 먼저 읽으세요. 현재 상태·최근 변경 이력·다음 작업·운영(배포/계정) 정보가 정리돼 있습니다.

## Commands

```bash
# 전체 실행 (PostgreSQL 시작 → 마이그레이션 → 시드 → 백엔드+프론트엔드 동시 실행)
bash run_web.sh

# Frontend (frontend/ 디렉토리에서)
cd frontend && npm run dev           # 개발 서버 (localhost:5173)
cd frontend && npm run build         # 프로덕션 빌드
cd frontend && npm run lint          # ESLint
cd frontend && npm test              # Vitest 전체 테스트
cd frontend && npx vitest run src/__tests__/show.test.ts  # 단일 테스트 파일

# Backend (backend/ 디렉토리에서)
cd backend && npm run dev            # tsx watch 개발 서버 (localhost:4000)
cd backend && npm test               # Vitest 전체 테스트 (순차 실행, artlink_test DB 사용)
cd backend && npx vitest run src/__tests__/gallery.test.ts  # 단일 테스트 파일

# Database (backend/ 디렉토리에서)
cd backend && npx prisma migrate dev    # 마이그레이션 생성+실행
cd backend && npx prisma generate       # Prisma 클라이언트 재생성
cd backend && npx prisma studio         # DB GUI (localhost:5555)
cd backend && npx tsx prisma/seed.ts    # 시드 데이터 실행

# WSL2 필수: PostgreSQL 재부팅 후 수동 시작
sudo service postgresql start
```

## Architecture

**모노레포**: `frontend/` (React+Vite+TS) + `backend/` (Express+Prisma+PostgreSQL)

### Frontend (`frontend/src/`)
- **pages/**: 11개 라우트 페이지 (MyPage.tsx ~1400줄이 가장 큼)
- **components/**: layout(Navbar), home(HeroSlider, QuickActionCards), gallery(InstagramFeed), shared(ProtectedRoute, ImageUpload, ImageLightbox, ConfirmDialog)
- **stores/**: Zustand — `authStore` (JWT + localStorage persist)
- **lib/**: `axios.ts` (JWT 인터셉터, 15s 타임아웃), `queryClient.ts` (staleTime 5min, retry 3), `utils.ts`
- **types/**: 전역 TypeScript 타입

### Backend (`backend/src/`)
- **routes/**: 14개 모듈 — auth, gallery, exhibition, show, review, favorite, portfolio, approval, hero, benefit, galleryOfMonth, upload, notification, inquiry
- **middleware/**: `authenticate`(필수 JWT), `optionalAuth`(선택), `authorize(roles)`(역할 검증), `errorHandler`
- **lib/**: `prisma.ts` (싱글톤), `mailer.ts` (nodemailer), `logger.ts` (파일+콘솔 로깅)
- **prisma/**: `schema.prisma` (18개 모델, Single Source of Truth), `seed.ts` (4 users, 3 galleries, 2 exhibitions, 2 shows 등)

### Key Patterns
- **Vite proxy**: `/api` → `localhost:4000`, `/uploads` → `localhost:4000`
- **Auth flow**: Axios 인터셉터가 JWT 자동 첨부, 401 → `authStore.logout()`
- **TanStack Query**: `invalidateQueries` on all mutations (cross-component 포함)
- **Optimistic updates**: 모든 찜 토글에 적용 — `onMutate`(cancelQueries + setQueryData flip) → `onError`(rollback) → `onSettled`(invalidate)
- **MyPage 찜 삭제**: `setQueriesData`로 galleries/exhibitions 캐시까지 직접 수정 (stale 캐시 깜빡임 방지)
- **Review CUD → gallery rating 자동 재계산** (aggregate)
- **GotM**: GET에서 만료된 항목 where 필터 (deleteMany 아님, DB 레코드 유지)
- **Favorite toggle**: existing→delete, not→create, return `{favorited: bool}`
- **Cascade delete**: Gallery→Images/Exhibitions/Shows/Reviews, Exhibition→PromoPhotos/Applications
- **AppError class**: `statusCode` 포함 구조화된 에러
- **ImageLightbox**: Portal 기반, `initialIndex` prop 사용 (NOT `currentIndex`), 부모에서 `AnimatePresence`로 감싸야 exit 애니메이션 동작

## Testing

- **1829 tests**: Backend 1197 (supertest, `artlink_test` DB 순차), Frontend 632 (jsdom)
- **E2E**: `e2e/` Playwright 41개 파일(부하·신뢰성 4종 포함 — `38-newfeature-reliability`·`39-load-community-story`·`40-load-chat`·`41-load-artlook`). 🚨 **DB 를 확인하고 돌릴 것** — `global-setup` 이 `prisma migrate reset --force` 로
  대상 DB 를 통째로 지운다. `backend/.env` 가 실서버 복제본(`artlink_prod`)을 가리키면 **실제 가입자 데이터가 사라진다**.
  `DATABASE_URL=...localhost:5432/artlink` 를 명시해 로컬 데모 DB 로 돌릴 것(백엔드도 같은 DB 로 띄운다). 자세한 건 `e2e/README.md`
- **Backend**: `artlink_test` DB 사용, `fileParallelism: false` 순차 실행, `setup.ts`에서 migrate deploy
- **Frontend**: jsdom 환경, 순수함수(utils) + zustand store 테스트
- **Test helper** (`backend/src/__tests__/helpers.ts`): `cleanDb` (TRUNCATE CASCADE), `seedUsers` (id 1-4), `seedGallery`, `seedShow`
- **Backend index.ts**: `NODE_ENV=test` 시 listen/rateLimit/morgan 비활성화
- **모든 코드 수정 완료 후** `cd backend && npm test` + `cd frontend && npm test` 실행 필수. 기존 코드 수정 시 변경된 로직에 대응하는 테스트가 없으면 추가 작성 필수.

## Critical Constraints

1. **Prisma v5만 사용** — v7은 `datasource url` 제거 등 breaking change, 절대 업그레이드 금지
2. **Tailwind CSS v4** — `@import "tailwindcss"` 문법 사용 (구 `@tailwind` 디렉티브 아님), `@tailwindcss/vite` 플러그인
3. **Express v5** — SPA wildcard `/{*path}` 문법 필수 (`*` 단독 사용 불가)
4. **seed.ts upsert 규칙**: 스키마에 새 필드 추가 시 `update` 블록에도 반드시 해당 필드 포함 (Render DB는 유지되므로 기존 레코드는 update 경로를 탐. 로컬 migrate reset에서는 드러나지 않음)
5. **architecture.md 업데이트** — 코드 변경 시 반드시 갱신
6. **승인 거절 시 rejectReason 필수** — 없으면 400 에러
7. **Admin은 찜 버튼 미표시** — GalleriesPage, GalleryDetailPage, ExhibitionsPage, ExhibitionDetailPage, ShowsPage, ShowDetailPage에서 `!isAdmin` 조건
8. **gradient overlay에 `pointer-events-none` 필수** — `absolute inset-0` 오버레이가 아래 요소 클릭 차단
9. **Framer Motion drag+animate 동시 사용 금지** — `drag="x"` + `animate={controls}` 이중 x 제어 충돌. AnimatePresence+variants 방식 사용
10. **찜 연동은 invalidate만으로 부족** — cross-cache `setQueriesData`로 즉시 수정해야 stale 깜빡임 방지
11. **ImageLightbox**: `initialIndex` prop 사용 (NOT `currentIndex`), 부모에서 `AnimatePresence`로 감싸야 exit 애니메이션 동작
12. **ImageUpload**: `placeholder` prop 사용 (NOT `label`)
13. **Show artists 필드** — DB에 JSON string 저장 (`JSON.stringify`), API에서 `JSON.parse`로 배열 반환. null 허용
14. **날짜 경계는 KST 기준** — `<input type="date">`→`new Date("YYYY-MM-DD")`는 UTC 자정(=KST 09:00)로 저장됨. 마감/만료/전시상태 판정은 반드시 KST 달력 날짜 단위로: 백엔드 `lib/kstDate.ts`(`startOfTodayKstAsUtc`/`endOfTodayKstAsUtc`/`isDeadlinePassedKst`), 프론트 `utils.ts`(`getDday`/`getShowStatus`). 순수 `new Date()` 비교 금지(마감일 당일 09시 소멸 버그)
15. **작가 제출자료 완료 판정** — cv/note는 빈 객체도 저장되므로 `!!`로 판정 금지. `lib/submission.ts`의 `hasSubmissionContent`(내용 존재 검사)로 통일. 프론트도 동일 predicate 사용
16. **R2 이미지를 `fetch`로 받을 땐 `cache: 'reload'` 필수** (`lib/imageFetch.ts`) — 화면이 같은 이미지를 평범한 `<img src>`(crossorigin 없음)로 먼저 그리면 브라우저 캐시에 **CORS 정보 없는 항목**이 남고, 이후 CORS 모드 `fetch`가 그 항목을 재사용하며 차단된다(서버 헤더는 정상). 그러면 R2 직접 경로가 죽고 **에러 없이 조용히** 백엔드 프록시로 되돌아가 느려진다. `force-cache` 제거만으로는 해결되지 않음(실측). 썸네일이 떠 있는 상태로 다운로드해야 재현된다
17. **`R2_PUBLIC_URL`은 쉼표 구분 목록** — **첫 번째**가 신규 업로드용 정식 주소, **전체**가 프록시 허용·파일 삭제로 인정할 주소(`backend/src/lib/r2Urls.ts`). 2026-08-04 개발용 `pub-*.r2.dev` → `img.artlink.cc` 전환 때문. **DB에 옛 주소가 남아 있는 한 목록에서 빼지 말 것** — 빼면 ①프록시가 옛 주소를 400으로 막아 폴백이 죽고 ②`deleteUploadedFile`이 옛 주소를 못 알아봐 **에러 없이 조용히** 고아 파일을 남긴다. 코드에서 `process.env.R2_PUBLIC_URL`을 직접 접두사 비교하지 말고 `r2CanonicalBase()`/`matchR2Base()`를 쓸 것
18. **작품 이미지는 자르지도 늘리지도 말 것** — 회화에서 비율은 작품 그 자체다. 포맷 엔진의 `img()` 헬퍼가 `object-fit:contain`을 강제하고, 크기는 `max-width`/`max-height`로만 준다. 화면 쪽도 마찬가지(`aspect-square + object-cover` 금지). 회귀 방지 테스트가 `portfolioFormats.test.ts`에 있다
19. **포트폴리오 포맷 페이지는 `PAD` 상수에서만 여백을 읽을 것** (`lib/portfolioFormats.ts`) — 페이지별로 숫자를 손으로 적었더니 포맷 D에서 머리말과 작품이 **겹치고**, 포맷 C 페이지가 하단 연락처 줄을 **뚫고 나갔다**. 이미지 높이는 `availH()`/`captionH(theme, items)`로 계산한다. 대각선 장식은 `transform` 금지 → **`linear-gradient`로** (html2canvas가 transform을 정확히 재현하지 않음). 렌더 전 `await document.fonts.ready` 필수 — 없으면 표지 큰 글씨가 폴백 글꼴로 찍힌다.
    **높이 추정값은 반드시 브라우저 실측과 맞출 것** (2026-08-16 전수조사에서 4개 포맷 17장이 넘쳐 잘림). 셋 다 "글자 수만 보고 줄 수를 셌다"가 원인이다 — ①약력·문단 높이는 `\n`을 세야 한다(화면에선 `<br/>`로 나간다). `estimateParaH`/`splitParagraphs`의 `takeLines`가 그 규칙이고, 새로 만들지 말고 이걸 쓸 것. ②`captionH`는 작품마다 캡션 줄 수가 달라 **상수로 두면 안 된다**(포맷 D는 한 장에 2점이라 오차가 두 배). ③머리말 상수는 실측 기준(아이브로우 18 / 이름 51·48). 추정치엔 `SAFETY`(24px) 쿠션을 남길 것 — **쿠션을 산수 오류로 소진하지 말 것**(2026-08-31: 캡션 글자폭을 55% 과대평가해 놓고 넘침이 0이라 안전한 줄 알았는데, 실은 SAFETY 가 그 초과분을 먹고 있었다). 페이지가 `overflow:hidden`이라 넘치면 **에러 없이 조용히 잘린다** — 회귀는 `portfolioFormats.test.ts`(문자열)와 `scratchpad/pf/`(브라우저 실측)로 잡는다. ⚠️ vitest 는 jsdom 이라 **레이아웃을 못 잰다** — 72개가 140ms 에 통과하는 건 한 번도 렌더하지 않기 때문이다. 넘침·크기·대비는 반드시 하니스로 볼 것
20. **경력(`Career`)의 `education`/`award`는 선택 항목** — 기존에 저장된 JSON엔 없다. 직접 `career[key].length`로 접근 금지, 반드시 `lib/artwork.ts`의 `normalizeCareer()`를 통과시킬 것 (안 그러면 옛 데이터에서 런타임 에러)
21b. **작품 격자는 t800, 목록칸은 t240** (2026-08-27) — 240px 는 28~120px 칸용이라 작가 홈페이지 작품 격자
    (실측 표시 PC 377px / 레티나 754px / 모바일3x 486px)에 쓰면 0.32~0.64배로 늘어나 **뭉개진다**.
    `backend/src/lib/thumb.ts` 의 `THUMB_SPECS`(t240·t800)를 업로드 때 함께 만들고, 프론트는 `<Thumb size="grid">`.
    실측: 작품 30장 한 페이지 **원본 28.1MB → t800 4.3MB**(6.5배), t240 이면 0.3MB 지만 화질 불가.
    기존 372장은 `backend/scripts/backfill-thumbs.ts` 로 백필한다(**Render 셸에서** — 로컬엔 R2 키가 없다).
    ⚠️ **백필을 먼저 돌리고 배포할 것** — 순서가 뒤바뀌면 장당 t800 404 + 원본 으로 **요청이 두 배**가 된다(화면은 폴백으로 정상).
    ⚠️ `Thumb` 의 `THUMB_SIZES[*].backfilled` 는 "그 폴더에 옛 파일까지 채웠는가" 다. t240 은 아직 false —
    true 로 바꾸면 백필 안 된 옛 파일마다 헛된 404 가 늘어난다. t240 을 백필하면 그때 바꿀 것.

21. **목록 썸네일은 `Thumb` 컴포넌트로** (`components/shared/Thumb.tsx`) — 목록이 원본을 받아 한 페이지에 96MB를 쓰던 문제. 업로드 시 `t240/`에 240px를 함께 생성(`backend/src/lib/thumb.ts`), 없으면 원본 폴백. **확대·라이트박스·PDF는 원본 유지**(키우면 뭉개짐). 썸네일 키를 `String.replace`로 만들지 말 것 — 디렉터리가 중복돼 **R2에서만 조용히** 깨진다
22. **공모 운영 권한은 `lib/exhibitionAccess.ts`로만 판정** — 아트링크(Admin) 주최 공모(`hostType='ADMIN'`)는 admin이 지정한 여러 갤러리(`ExhibitionManager`)가 오너처럼 운영한다. **위임은 `hostType==='ADMIN'`일 때만 인정**할 것 — 갤러리 주최 공모에 위임 행이 섞여도 권한을 주면 **남의 공모 지원자 개인정보가 통째로** 열린다. 새 엔드포인트에서 `gallery.ownerId !== req.user.id` 를 직접 쓰지 말고 `assertCanManageExhibition`(Admin 포함)/`operableExhibitionWhere` 를 쓸 것. **지원자 관리·초대·홍보사진 라우트에 `authorize('GALLERY')` 만 걸면 Admin이 막힌다** — 실제로 관리자가 수락/거절을 못 했다. `galleryId`(주관 갤러리)는 기존 코드 79곳이 전제하므로 nullable로 바꾸지 말 것
23. **공개 상세 라우트는 `status !== 'APPROVED'` 를 먼저 막을 것** — 목록만 거르고 상세를 열어두면 목록에 없는 심사중·반려 항목이 **주소로 id 만 치면 비로그인에게 다 보인다**(2026-08-15 수정). 당사자·Admin 만 열고 **404**로 응답(403은 존재를 알려준다). 탈퇴(WITHDRAWN)는 Admin 전용 유지. 새 공개 상세를 만들면 `private-detail.test.ts` 에 케이스를 추가할 것
24. **공개 목록에 마감분을 노출할 땐 `status: 'APPROVED'` 를 절대 빼지 말 것** — `GET /exhibitions?scope=closed`(마감된 공고 탭)는 수동마감·전시종료·마감일경과를 모으는데, 승인 필터가 빠지면 심사중·반려·탈퇴 공모가 **공개된다**(23번과 같은 사고). 검색어가 있으면 `where.OR` 가 `AND` 로 감싸지므로 scope 조건이 유지되는지 함께 확인할 것.
    **상세의 지원 차단을 마감일로만 판정하지 말 것** — `recruitmentClosed`/`ended` 를 빼면 마감일이 남은 채 마감된 공고에 [지원하기]가 뜬다(`?apply=1` 자동 오픈 경로 포함). 서버는 4중으로 막고 있으니 데이터는 안전하지만, 눌러보고 400 을 받는 화면이 된다
25. **`frontend/index.html`의 SEO 마커 삭제 금지** — `<!--SEO_META_START-->`~`<!--SEO_META_END-->` 사이를 서버(`lib/seoMeta.ts`)가 상세 페이지 요청 시 교체한다. 마커가 사라지면 **에러 없이 조용히** 기본 meta로 되돌아감(카톡 공유 미리보기·검색 노출 전부 무력화). meta 값을 코드로 만들 때는 반드시 `buildMetaTags()`를 통과시킬 것 — 직접 문자열 연결 시 XSS. 킬스위치 `SEO_META=off`
26. **정산 재확인은 `lib/settlementFingerprint.ts` 로만 판정** — 작가가 응답할 때 본 금액을 `SettlementApproval.snapshot` 에 지문으로 남기고, 저장할 때 대조해 **금액이 바뀐 작가만** 재확인 대상으로 되돌린다(그 사람에게만 알림). 지문에 `ArtworkSale.title` 을 넣지 말 것 — 화면 제목은 작가 출품목록에서 오므로, 넣으면 금액이 그대로인데도 멀쩡한 수락이 풀린다. `findMany` 순서를 믿지 말고 반드시 index 정렬 후 생성.
    되돌리면 안 되는 것 둘: ①`PUT /settlement` 에 **요청 중 잠금(403)을 다시 걸지 말 것** — 그러면 한 명을 고치려고 요청 전체를 내려야 하고, 아직 검토 중이던 작가의 화면까지 닫힌다. ②`request/cancel` 에서 **승인 행을 지우지 말 것** — 예전엔 지워서 한 명이 문제를 제기하면 전원이 다시 확인해야 했다(18명 단체전에서 17명이 헛수고).
    요청 중 수정이 열려 있으므로 `respond` 의 **지문 대조(409)는 필수** — 없으면 작가가 옛 화면을 보고 수락해 **본 적 없는 금액에 동의한 기록**이 남는다. 정산은 돈 문제라 그게 곧 분쟁 근거다.
    **무응답 3일 자동 수락**(`lib/settlementDeadline.ts`)의 기준은 작가별 `askedAt` 이다. PENDING 을 만드는 모든 경로에서 갱신할 것 — 특히 **금액을 고쳤을 때 갱신을 빼먹으면 마감 직전에 금액을 바꿔놓고 자동 통과시키는 게 가능해진다**. ISSUE 는 자동 수락 금지(이의를 낸 사람을 침묵으로 볼 수 없다). 자동 수락 시 `autoApprovedAt` 과 지문을 함께 쓸 것(지문을 빼면 곧바로 '변경됨'으로 잡혀 완료가 막힌다). `askedAt` 이 NULL 인 옛 행은 대상에서 제외 — 안내를 못 받은 사람에게 소급 적용하지 않는다.
    화면에서 **작가에게 보내는 버튼은 입력값을 먼저 저장할 것** — 저장을 안 걸었더니 옛 금액이 그대로 나가고, invalidate 후 refetch 가 입력 중이던 값을 조용히 되돌렸다. 반대로 [정산 완료]·[요청 취소]는 미저장이면 막는다(옛 금액 확정·입력 유실). 미저장 판정은 `frontend/src/lib/settlement.ts` 의 `settlementFormSignature`
30. **작가가 넣는 글에는 `break-keep` 만으로 부족하다** — `word-break: keep-all` 은 낱말 안에서 안 끊는데,
    **공백 없이 이어 쓴 한글 한 덩어리는 통째로 한 낱말**이라 아무 데서도 안 끊긴다.
    실측(2026-08-28, 실서버 작가노트 432자 무공백): 글상자가 4848px 넘치고 **페이지 전체가 가로로 4224px 밀렸다**.
    글이 들어가는 자리마다 `[overflow-wrap:anywhere]` 를 함께 줄 것(`components/shared/HomepageView.tsx`).
    `break-words`(break-word)로는 **min-content 가 안 줄어** 부모를 계속 밀어낸다 — `anywhere` 여야 한다.
    ⚠️ **포트폴리오 PDF 엔진(`lib/portfolioFormats.ts`)도 똑같다** (2026-08-28 전수조사) — keep-all 만 있고
    overflow-wrap 이 없어 무공백 장문이 가로로 뻗어 페이지(overflow:hidden)에서 **조용히 잘렸다**. 작가노트·약력·
    태그라인·시리즈명/소개·작품제목·경력·연락처 **모든 `esc()` 텍스트 자리**에 `overflow-wrap:anywhere` 를 붙였다.
    높이 추정(`estimateParaH`)은 원래 글자 수 기준이라 줄바꿈을 전제하므로, CSS 를 맞추면 추정과 렌더가 일치한다.
    회귀는 `portfolioFormats.test.ts` 가 **전 포맷에서 keep-all 은 반드시 anywhere 와 짝** 인지 검사해 잡는다.
31. **홈페이지 편집은 저장하면 공개 페이지로 돌아간다** — 그 화면은 공개 페이지의 [수정]으로만 들어오는
    편집 전용이라, 저장 후 남으면 '나만 보는 옛 관리 화면'에 갇힌 것처럼 보인다.
    돌아갈 때 `['portfolio', userId]` 캐시도 함께 invalidate 할 것 — 안 하면 방금 저장한 게 아니라 옛 화면이 뜬다.

28. **`?tab=` 이 없어질 때도 탭을 되돌릴 것** — MyPage 의 searchParams effect 가 `if (t && …)` 라
    값이 없으면 아무것도 안 했다. 프로필은 기본 탭이라 주소에 쿼리를 안 붙이므로(`myPageHref`),
    사이드바에서 [프로필]을 누르면 **주소만 바뀌고 화면은 이전 탭에 머물렀다**(2026-08-28 신고).
    `const t = rawTab ?? 'profile'` 로 항상 판정할 것.
29. **한 작품의 상태(좋아요·관심작품)를 두 곳에 나눠 들지 말 것** — `ArtworkDetailModal` 이
    스크랩만 별도 state 로 뒀더니, 좋아요가 `image` 를 갈아끼울 때 부모→`initial` 동기화 effect 가
    **옛 `isScrapped(false)`** 로 되돌려 저장해 둔 관심 작품의 불이 꺼졌다(2026-08-28 신고).
    진실은 `image` 하나여야 한다.

27. **`truncate` 는 min-content 를 줄이지 않는다** — `overflow:hidden` 은 flex 자동 최소치의 '바닥'만 없앨 뿐이다. min-content 로 트랙 폭이 정해지는 grid 안에서는 nowrap 텍스트가 폭을 그대로 밀어내므로, **grid 아이템마다 `min-w-0` 을 빠짐없이** 붙일 것. 하나만 빼먹어도 375px 에서 페이지 전체가 가로로 밀린다(실측 447px). 상태 배지처럼 짧은 칩은 `shrink-0 whitespace-nowrap` 이 없으면 '대 기 중' 으로 세로로 쪼개진다

### 대화 — 갠톡(1:1)·단톡(공모방) (2026-08-28 전면 개편)
- 예전 `Message`(제목+본문 쪽지)는 라우트마다 "작가는 갤러리에게만" 같은 **역할 규칙이 박혀 있어 작가끼리 대화가 불가능**했다.
  새 구조(`Chat` / `ChatParticipant` / `ChatMessage`)는 **방에 들어가 있는가** 하나로만 권한을 판정한다.
- ⚠️ **역할로 막지 말 것.** 대신 **방이 생기는 길목**을 좁힌다 — 임의 검색으로 아무에게나 말을 걸 수 없다.
    - 갠톡: 둘러보기 작품 모달 · 작가 홈페이지의 [메시지] → `POST /chats/direct`
    - 단톡: **공모 승인 시 서버가 자동 생성**(`lib/chat.ts` `ensureExhibitionChat`).
      갤러리 오너(+아트링크 주최면 운영 갤러리)와 **수락된 작가**가 자동 참여자. 작가 수락 때도 다시 부른다(멱등).
    - 화면에 임의 검색으로 아무나 찾는 '새 대화'는 **없다** — 만들면 이 설계가 무너진다.
      **딱 하나 예외(2026-08-29)**: 이미 **서로 이웃**(양방향 팔로우)인 사람에겐 메시지창 목록 위 [이웃에게 메시지]로 바로 갠톡을 연다
      (`GET /follow/mutuals` 로 교집합을 받아 고르면 `POST /chats/direct`). 임의 검색이 아니라 이미 맺어진 관계라 전제를 안 깬다.
      ⚠️ `frontend/src/__tests__/retiredApis.test.ts` 의 `/chats/direct` 허용 목록에 `MessagesPage.tsx` 를 넣어 뒀다(빼면 실패).
      금지 문자열('새 대화'/'새 채팅'/'대화 상대 검색')은 여전히 쓰지 말 것 — 그 가드는 살아 있다.
- ⚠️ 남의 방은 **404**로 답한다(403은 방의 존재를 알려준다). Admin 도 예외가 아니다.
- ⚠️ 수락이 풀려도 **방에서 빼지 않는다** — 지난 대화를 읽을 수 있어야 하고, 말없이 사라지면 남은 사람이 영문을 모른다.
- **카카오톡처럼 이어 보낸 말은 묶는다** — 이름은 묶음 **첫 줄**에만(단톡의 남의 말), 시각·읽음은 **마지막 줄**에만.
  묶음 = 같은 사람 + 같은 분(minute). 예전엔 메시지마다 이름·시각이 다 붙어 한 사람이 세 줄 쓰면 이름 3번·시각 3번이 나왔다.
- 읽음은 참여자별 `lastReadAt` **하나**로 판정한다(메시지마다 읽음 행을 쌓으면 20명×메시지 수만큼 불어난다).
    - 갠톡 `read` : 상대의 lastReadAt 이 그 메시지보다 뒤면 읽음
    - 단톡 `unreadBy` : 보낸 사람을 뺀 참여자 중 아직 안 읽은 수
- ⚠️⚠️ **방의 메시지를 전부 내려주지 말 것 — 화면이 그걸 8초마다 다시 받는다** (2026-09-04).
  `readChat` 에 `take` 가 없어서 방에 쌓인 걸 통째로 보냈는데, `MessagesPage` 가 `refetchInterval: 8000` 으로
  같은 응답을 계속 다시 받는다. 실측 **메시지당 287 bytes** 라 방이 오래될수록 무거워지는 구조였다 —
  233개 65KB(8.2KB/s) · 1,000개 280KB(35KB/s) · **5,000개 1.4MB(175KB/s, 1시간 616MB)**.
    - 지금은 방을 열 때 **최근 `CHAT_PAGE_SIZE`(150)개**만 주고, 폴링은 **`?after=<마지막 id>` 로 새 것만** 받는다.
      조용하면 응답이 `[]`(실측 **391 bytes**)라 **방에 몇 개가 쌓였든 폴링 비용이 같다**.
      지난 메시지는 `?before=<첫 id>` 로 [이전 메시지 더 보기].
    - ⚠️ 커서는 `createdAt` 이 아니라 **`id`** 로 잡을 것 — 동시에 들어온 메시지는 `createdAt` 이 같을 수 있어
      시각 커서로는 건너뛰거나 되받는다(정렬 자체는 화면 순서와 맞추려고 createdAt 을 쓴다).
    - ⚠️⚠️ **화면은 폴링 응답만 그리면 안 된다.** `after` 응답은 새 메시지 몇 개뿐이라, 그걸 그대로 렌더하면
      대화가 통째로 사라진다. `MessagesPage` 는 누적 state 가 진실이고 모든 응답을 **id 로 병합**한다
      (`lib/chatView.ts` `mergeMessages` — 겹치면 **나중 것이 이긴다**. 읽음 표시가 갱신돼야 하므로
      '있으면 건너뛰기'가 아니라 덮어쓰기다). 대화에 삭제 라우트가 없어 병합만으로 안전하다.
    - ⚠️⚠️ **`after` 응답에는 지난 메시지가 없어서 '읽음' 표시가 안 바뀐다.** 상대가 옛 메시지를 읽어도
      캐시에 든 값은 옛날 그대로다. 그래서 서버가 참여자별 `lastReadAt`(**`readers`**)을 **매번 함께** 주고
      화면이 들고 있는 **모든** 메시지를 다시 판정한다(`applyReadState`). ⚠️ 그 규칙은 서버 `readChat` 과
      **같아야 한다** — 어긋나면 화면과 서버가 다른 답을 낸다(회귀는 `chatView.test.ts`).
    - ⚠️ 맨 아래로 스크롤하는 조건을 **개수**로 두지 말 것 — [이전 메시지]로 위를 채웠을 때도 맨 아래로 튕긴다.
      **마지막 메시지 id** 를 본다. 위에 붙일 땐 늘어난 높이만큼 `scrollTop` 을 되돌려 보던 자리를 지킨다.
- ⚠️ **안읽음을 방마다 세지 말 것(N+1)** (2026-09-04). `unreadChatCount` 는 **로그인한 모든 사용자가 30초마다**
  (Navbar 배지), `listChats` 는 15초마다 부른다. 예전엔 둘 다 방 수만큼 쿼리를 돌아, 방이 늘고 사용자가 늘수록
  **배경 폴링이 DB 커넥션을 다 먹는** 구조였다(실측 방1 13ms → 방17 25ms). 기준 시각(`lastReadAt`)이 방마다
  다르므로 `unreadWhere` 로 **`OR` 한 덩어리**를 만들어 `groupBy` 한 번에 묻는다 — `@@index([chatId, createdAt])`
  를 그대로 탄다.
- **첨부 — 사진·동영상·파일** (2026-08-28). `ChatMessage` 에 `attachmentUrl/Type(IMAGE|VIDEO|FILE)/Name/Size` 를 얹었고 `content` 는 첨부만 있는 메시지를 위해 `@default("")` 로 바꿨다(마이그레이션 `20260828140000_add_chat_attachment`).
    - **먼저 업로드하고 그 url·메타를 함께 보낸다** — 화면이 `/api/upload/image`(사진 15MB)·`/api/upload/video`(동영상 25MB, 신규)·`/api/upload/file`(파일 20MB, PDF/DOC/HWP/ZIP)로 올린 뒤 `POST /chats/:id/messages` 에 `{attachmentUrl, attachmentType, attachmentName?, attachmentSize?}`.
    - ⚠️ **서버비 고려한 용량 상한**은 multer 가 최종 판정(초과 시 400). 동영상은 이미지·파일보다 짧게(25MB). 프론트도 선검사해 헛된 업로드를 줄인다.
    - ⚠️ 첨부 주소는 `safeFileUrl` 로 검증해 저장한다 — `javascript:` 등 이상한 스킴 400. `attachmentType` 은 IMAGE/VIDEO/FILE 만.
    - 본문·첨부 **둘 다 없으면 400**. 첨부만 있으면 텍스트 말풍선을 그리지 않는다(빈 회색 상자 금지). 목록 미리보기는 `[사진]/[동영상]/[파일]`(`lib/chat.ts attachmentLabel`).
- 회귀는 `backend/src/__tests__/chat.test.ts`(36개)와 `e2e/tests/29-chat.spec.ts`(14개, 실제 파일 업로드 포함)가 잡는다 — 특히 '남의 방 차단'·'작가↔작가 허용'·첨부.

### 초대 → 참여 (2026-08-28)
- [받은 초대] 메뉴 탭은 없앴다. **[내 전시]의 첫 탭 '초대받은 전시'** 로 합쳤다(초대도 내 전시의 한 단계다).
- **수락 = 지원 없이 바로 참가** (`POST /exhibitions/invites/:id/accept`) — `status='ACCEPTED'` 지원이 만들어져
  그대로 진행 프로세스(자료 제출 → 전시 → 정산)에 들어간다. 약력·작품은 포트폴리오에서 가져온다.
  ⚠️ **정원·마감은 그대로 지킨다** — 초대가 있었다고 정원을 넘겨 받을 수는 없다.
- `MyExhibitionBucket` 에 `INVITED` 가 있지만 `groupMyExhibitions` 는 **채우지 않는다**(목록 API 가 다르다).

### 커뮤니티 · 소셜 (2026-08-28~29)
- **세 가지가 서로 다르다 — 헷갈리지 말 것.**
    - **커뮤니티**(`routes/community.ts`, `/community`) = **글로벌 게시판**(블라인드식). 글마다 **실명/익명** 선택, 좋아요·댓글·조회수. 홈 [인기글] 위젯의 출처.
    - **스토리**(`routes/story.ts`, `/feed` = [소식]) = **개인 피드**. **내가 이웃한 사람 + 나** 의 글만 뜬다. **익명 없음**(소식은 '누가'가 핵심).
    - **방명록**(`routes/guestbook.ts`) = **작가 홈페이지**(`/portfolio/:id`) 하단. 공개, 답글은 방 주인만.
- **이웃 = 단방향 팔로우**(`routes/follow.ts`, `Follow`). 수락 불필요, 추가하면 상대에게 **알림**(`NEIGHBOR_FOLLOW`, 내 프로필로). 멱등 — 두 번 눌러도 알림 1건. 역할 무관(작가↔갤러리도).
- ⚠️ **스토리 공개범위는 글마다** (`PUBLIC` | `NEIGHBORS`). [소식] 피드는 내가 팔로우한 사람 것을 **공개범위 무관하게** 다 보여준다(팔로우 = 이웃이므로). 판정은 **작가 홈페이지 조회**(`/stories/user/:id`)에서만 갈린다 — 비팔로워는 PUBLIC 만.
- ⚠️ **방명록 비밀글은 본문만 가린다 — 신원은 보인다**(`locked` 플래그). 방 주인·작성자만 본문을 읽는다. 답글(`parentId`)은 **방 주인만**, 최상위 글에만(1단계). 삭제는 글쓴이·방 주인·Admin.
- **글/스토리 사진은 우리 저장소 주소만** — `ownImageUrls`(community.ts·story.ts)가 `safeFileUrl` + (`/uploads/` | `matchR2Base`)로 외부 URL 을 거른다(주입 방지).
- [소식] 진입점은 **마이페이지 사이드바 맨 아래 링크**(`MYPAGE_FOOTER_LINKS`) — 로그인 필요라 Navbar 가운데 메뉴엔 못 둔다(비로그인이 눌렀다 튕긴다).
- **스토리에도 좋아요·댓글**(`StoryLike`/`StoryComment`, 비정규화 카운트). 댓글 달면 스토리 주인에게 `STORY_COMMENT` 알림(/feed). 댓글 삭제는 작성자·스토리 주인·Admin.
- **소식 탭 이름은 ArtStory** (로고 규칙: Art**Story**, 사이드바 픽토그램은 사람 상반신 `User`). 진입점은 사이드바 footer 링크.
- **커뮤니티 탭·공지·고정 — 전부 Admin 전용** (2026-09-04, `PostCategory` + `Post.notice`/`pinnedAt`,
  마이그레이션 `20260904120000_add_community_tabs_and_notice`)
    - **탭(말머리)은 Admin 이 만든다** — 이름·순서·숨김·삭제. 목록은 `?category=<slug>` 로 거른다.
      ⚠️ **`/categories` 라우트를 `/:id` 보다 먼저 선언할 것** — 뒤에 두면 `parseInt('categories')=NaN`
      으로 404 가 나고 에러 메시지가 엉뚱해 원인을 못 찾는다.
    - ⚠️ **탭을 지워도 글은 안 지운다** (`onDelete: SetNull` → 미분류). 삭제 응답이 몇 개가 내려왔는지
      (`movedToUncategorized`) 돌려주고 화면이 미리 경고한다. 되돌릴 수 없으므로 **숨기기(active:false)를 먼저 권한다.**
    - ⚠️ **`slug` 는 이름을 바꿔도 안 바꾼다** — 주소·북마크가 죽는다. 한글만인 이름은 `tab-N` 로 떨어진다.
    - **공지(`notice`)와 고정(`pinnedAt`)은 다른 것이다.** 공지는 글의 성격(배지), 고정은 위치.
      남의 글도 고정할 수 있다. ⚠️ `pinned` 를 Boolean 이 아니라 **시각**으로 둔 이유: 고정 글이 여럿일 때
      순서가 있어야 한다(최근 고정이 위).
    - ⚠️⚠️ **목록 정렬에 `nulls: 'last'` 필수** — Postgres 는 `DESC` 에서 NULL 을 **먼저** 놓는다.
      빼면 고정 안 된 글이 맨 위로 올라온다(정반대). 회귀 테스트가 잡는다.
    - ⚠️ **'내 글/내 댓글' 필터에는 고정을 적용하지 않는다** — 내 활동 목록에 남이 고정한 글이 끼면 안 된다.
    - ⚠️⚠️ **클라이언트가 보낸 `notice`/`pinned` 를 믿지 말 것.** 스키마는 값을 받기만 하고
      권한은 핸들러가 `req.user.role` 로 판정한다. 작가가 보내면 **조용히 무시**된다(회귀 테스트 있음).
      탭 CUD·고정·공지·탭 이동은 전부 `authorize('ADMIN')`.
    - **공지는 익명일 수 없다** — 누가 공지했는지 모르면 공지가 아니다(서버가 `anonymous` 를 끈다).
    - ⚠️ **삭제는 원래 서버가 작성자·Admin 둘 다 허용했는데 화면이 `author.mine` 일 때만 버튼을 그렸다.**
      그래서 관리자가 신고받은 글을 못 지웠다 — 권한은 있는데 손이 닿지 않았다. 목록(hover)·상세·댓글 모두 열었다.
    - **탭마다 `writeAdminOnly`** (2026-09-04, 마이그레이션 `20260904140000_add_category_write_admin_only`) —
      켜면 **Admin 만 그 탭에 글을 쓴다**(공지 탭). **읽기는 그대로 공개**다.
      ⚠️ **탭 이름이 '공지'인지로 판단하지 말 것** — 관리자가 '안내'로 바꾸는 순간 조용히 풀린다. 플래그로 둔다.
      ⚠️ 제한 탭에 일반 사용자가 글을 보내면 **403 으로 막는다 — 조용히 미분류로 내리지 않는다.**
      조용히 옮기면 작성자는 공지 탭에 올렸다고 믿는데 실제로는 미분류에 있다(에러 없이 어긋난다).
      ⚠️ 화면에서는 **쓸 수 없는 탭을 선택지에서 아예 뺀다** — 고르게 해 놓고 등록에서 403 을 주면 함정이다.
      기본값 false 라 기존 탭 동작은 그대로.
    - ⚠️ **새 테이블을 만들면 `helpers.ts` 의 `cleanDb()` 에 반드시 추가할 것.** `PostCategory` 를 빠뜨렸더니
      테스트 간에 탭이 새어 '같은 이름 409' → 뒤 테스트가 id 없는 응답을 받아 400·빈 목록으로 실패했다.
      **증상이 원인과 전혀 안 닮았다.**
- 회귀: 백엔드 `follow-story.test.ts`·`guestbook.test.ts`·`community.test.ts`(42개)·`ad.test.ts`, E2E `36-community`·`37-social`.

### 홈 레이아웃 (2026-08-29)
- ArtWorks → 배너(HeroSlider) → **1행 [좌 인기글 / 우 진행중인 전시]** → **2행 [좌 마감임박 공모 / 우 주목할 갤러리] 1:1**.
- 진행중인 전시(`OngoingShows`) = `/shows?showStatus=ongoing` + `getShowStatus==='ongoing'`. 마감임박 공모(`ClosingSoonExhibitions`) = `/exhibitions?scope=open`(이미 마감일 오름차순)에서 dday≥0 앞부터. 둘 다 좁은 레일, 썸네일은 `Thumb`.
- **히어로 배너**(`HeroSlider`, 스크롤-스냅 슬라이딩): 자동 전환 **10초**, 슬라이드 애니메이션 rAF 900ms ease-in-out(브라우저 smooth 는 속도를 못 정한다). 좌우 화살표는 호버 때만 또렷이. (가로 split-flap 플립을 시도했다가 **롤백** — 슬라이딩이 낫다는 판단, 2026-08-29)
- **광고 슬롯**(`AdBanner`, `routes/ad.ts`, `AdSlot`): Admin 이 [광고 관리] 탭에서 등록. 사이드바 **로그아웃 아래**에 활성 배너 1개(position 순). 이미지·링크는 우리 저장소·안전 스킴만. 우상단 'AD' 라벨.
- **커뮤니티·스토리 이미지는 `Thumb`** 로 — 목록 t240 / 상세·피드 t800. 업로드(`/upload/image`)가 두 썸네일을 함께 생성하므로 추가 작업 없음.
- **작가 약력도 `text-justify`**(양쪽맞춤) — 작가노트와 동일(`HomepageView`).
- **방명록 discoverability**: 작가 홈페이지 헤더에 [방명록] 버튼(→ `#guestbook` 스크롤), 방명록 제목 옆 글 수 배지.

32. **화면에서만 뗀 API 는 소스로 감시할 것** (`frontend/src/__tests__/retiredApis.test.ts`) —
    대화가 옛 쪽지(`Message`)에서 ArtTalk(`Chat`)으로 바뀐 뒤에도 **갤러리 상세·공모 상세의 [쪽지 보내기]가
    옛 `POST /messages` 를 계속 부르고 있었다.** 서버는 200 을 주고 화면은 "쪽지가 전송되었습니다"를 띄우는데
    ArtTalk 은 `Chat` 만 읽으므로 **그 글을 아무도 볼 수 없었다** — 에러가 없어 아무도 몰랐다(2026-08-28 수정).
    주소가 문자열이라 타입도 테스트도 못 잡는다. 그래서 소스를 훑는 가드 테스트를 둔다.
    같은 파일이 **갠톡 진입점 목록**도 고정한다 — 방을 여는 곳이 늘면 "아무나 말 걸 수 없다"는 설계가 조용히 무너진다.
33. **⚠️ 메시지 신고가 화면에서 끊겨 있다** (2026-08-28 발견, 미해결) —
    `routes/report.ts` 는 여전히 옛 `Message` 의 `messageId` 만 받는데 화면 대화는 `ChatMessage` 로 바뀌었고
    **말풍선에 신고 버튼이 없다.** 즉 지금은 부적절한 메시지를 **신고할 방법이 없고**, Admin '신고 관리' 탭에는
    새 신고가 들어올 경로가 없다. 되살리려면 ①`report.ts` 가 `ChatMessage` 를 받게 하고 ②말풍선에 신고 버튼·마스킹을 붙인 뒤
    ③`e2e/tests/04-report-moderation.spec.ts` 의 `test.fixme` 를 되돌린다.
34. **테스트 픽스처의 공모 등록은 `exhibitionDates()`/`createExhibition()` 으로만** (`e2e/lib/helpers.ts`) —
    2026-08-19 에 `submissionDeadline` 이 필수가 됐는데(지원마감 < 자료제출 < 전시시작 순서까지 검사) 픽스처들이 그걸 안 보내
    **공모 생성이 전부 400** 이 났고, 그 뒤의 지원·수락·운영·정산이 줄줄이 실패했다(2026-08-28: 30여 개).
    필수 필드가 또 늘면 이 헬퍼 한 곳만 고치면 된다.
35. **마이페이지 탭을 라벨로 클릭하지 말 것** — 같은 라벨이 이제 **세 곳**에 있다(Navbar [메뉴]·본문 가로 탭바·우측 사이드바).
    `getByText('내 갤러리').first()` 는 보이는지 따지지 않아 모바일에서 `hidden lg:block` 인 사이드바를 집어 타임아웃 난다.
    `openMyPageTab(page, label, role)` 을 쓸 것. **역할마다 라벨의 뜻이 다르다** — '내 전시'는 작가에게 `applications`,
    갤러리에게 `my-shows` 다.

36. **ArtLook 장면(Scene) 모드 — 액자를 그리지 말고 사진에서 가져올 것** (2026-08-30, `public/artlook/scene.js`)
    예전 방식의 한계가 셋이었다. ①액자를 Canvas 그라디언트로 **그려서** CG 티가 났다
    ②벽은 사진인데 작품만 축정렬 사각형이라 비스듬한 방에서 떠 보였다 ③그림자가 곱연산 얼룩 하나였다.
    장면 모드는 **Flux 로 사전 생성한 "빈 액자가 걸린 방" 사진**의 구멍에 원본을 **원근 워프**해 끼운다.
    ⚠️ **작품 픽셀은 워프+합성만 한다** — 생성 모델이 작품을 다시 그리지 않는다(원본 보존).
    ⚠️ **`contain` 만 쓰고 남는 자리는 매트(대지)가 먹는다** — 18번(자르지도 늘리지도 말 것)이 여기에도 적용된다.
    - **원근은 WebGL2 프래그먼트 셰이더의 역호모그래피로.** Canvas2D `setTransform` 은 **아핀**이라 원근이 안 되고,
      삼각형 둘로 쪼개 그리면 대각선에서 텍스처가 꺾인다(원근보정 없음). 밉맵+이방성 필터 필수(구멍은 판보다 작아 축소가 일어난다).
      WebGL2 가 없으면 `supported()` 가 false → **토글 자체를 감춘다**(빈 탭은 고장으로 보인다). 기본 벽이 폴백.
    - ⚠️ **액자를 다 그린 뒤 벽 위에 얹지 말 것 — 액자째로 엔진에 통과시킨다.**
      절차적 액자(`buildFramedCore`)는 늘 좌상단 고정광으로 칠해져 있어서, 어둡고 따뜻한 방에
      그냥 얹으면 **혼자 노랗게 떠 스티커처럼 보인다**(2026-08-30 A/B 실측). 판(insert)에 액자를
      포함시키면 액자도 벽 명암·방 색·그레인·원근을 함께 받아 자연스럽게 앉는다.
      즉 문제는 '절차적으로 그렸다'가 아니라 **빛이 방과 안 맞았다**는 것 — 사진 액자라도 마찬가지다.
      사진에 이미 액자가 있는 장면(`opening`)에는 우리 액자를 겹치지 않는다(`sc.frameless` 로 판정).
      액자를 통과시킬 땐 매트·캔버스결을 다시 입히지 않는다(액자가 이미 갖고 있다).
    - ⚠️ **입체(두께)가 없으면 무엇을 해도 '벽에 인쇄한 그림'이다** (2026-08-30, 경쟁사 결과물 확대 분석).
      FrameIt 샘플을 확대하면 갤러리랩 캔버스의 **감싸진 옆면**과 액자의 **바깥 측면**이 원근으로 보인다.
      우리는 납작한 링 하나였다. `boxFaces()` 가 작품을 **벽에서 떠 있는 상자**로 보고
      뒷면(벽)·앞면(두께만큼 앞)·보이는 옆면을 만든다. 옆면 텍스처는 `edgeStrip()` 으로
      **판의 가장자리 픽셀 띠**를 뽑아 쓴다(갤러리랩은 실제로 그림이 모서리를 감싸므로 물리적으로 맞다).
      셋을 반드시 함께 둘 것 — ①앞→벽 방향 어두워지는 낙차 ②앞면·옆면 경계의 얇은 그늘 선
      ③2겹 그림자(넓은 투영 + 좁은 접지). 하나만 빠져도 종잇장처럼 보인다.
      ⚠️ `edgeStrip` 은 **맨 바깥 몇 px 을 건너뛴다**(`inset`) — 액자 앞면의 어두운 외곽선을 그대로
      뽑으면 옆면이 새까매져 '검은 틈'이 된다(샴페인 플로터에서 드러났다).
      ⚠️⚠️ **액자에는 3D 옆면을 만들지 말 것** (`depth:0`). 액자는 거의 정면으로 보이고 측면은
      나무 한 줄이라, 가장자리 픽셀을 문질러 옆면을 만들면 **액자 옆으로 그림이 번져 나온다**
      (2026-08-30 신고). 경쟁사도 액자는 정면 + 그림자만 쓴다. 옆면은 **캔버스 랩**(액자 없음)에서만.
      액자의 두께감은 ①단면 명암 ②그림자 ③**플로터 갭의 안쪽 벽**이 만든다 —
      갭을 검게 칠하면 액자가 아니라 잘라낸 구멍처럼 보인다. 앞이 밝고 안쪽이 그늘진
      **오목면**으로 그릴 것(`drawFloaterTray`).
    - **매트(대지)는 모든 평면 액자에서 고를 수 있다** (`state.matWidth` 0/5%/10% + 매트색 5종).
      경쟁사(FrameIt) 예시 4개 중 3개가 넓은 흰 매트를 쓴다 — '고급스럽다'는 인상의 큰 부분이 여기서 온다.
      예전엔 '화이트 매트' 스타일에서만 나왔다. **플로터·캔버스랩에서는 줄째로 감춘다**(`updateMatRow`) —
      플로터는 갭이 그 역할을 하므로 겹치면 액자가 두 겹으로 보인다.
      화면 구성은 경쟁사와 같은 **Frame / Mat / Effect** 세 축으로 맞췄다.
    - **사진 액자(`kind:'photo'`)가 최상위 품질이다** (2026-08-30). GPT 로 뽑은 *정면·빈 회색·흰 배경*
      액자 사진에서 바깥/개구부 사각형을 검출해 **개구부가 투명한 알파 PNG** 로 자르고,
      **9-slice**(네 모서리는 원본 비율 유지, 네 변만 늘림)로 그린다. 추출은
      `frontend/scripts/extract-photo-frames.py`(원본은 `gptsamplecase/`, **파일명에 재질 이름**을 넣을 것).
      ⚠️ **9-slice 안에서 배율을 다시 계산하지 말 것** — 호출부가 이미 그 두께로 캔버스를 잡아 놨다.
         따로 계산하면 액자가 작품을 덮거나 살과 캔버스 사이에 틈이 생긴다(실제 버그).
      ⚠️ **잘라낼 때 바깥 2px 을 깎을 것**(`E=2`) — 흰 배경에서 오려낸 가장자리는 흰색과 섞여 있어
         어두운 벽에서 **액자 둘레에 흰 테**가 뜬다.
      ⚠️ **흰 액자는 배경(254,255,255)과 차이가 10 남짓**이라 임계값을 크게 잡으면 액자를 배경으로
         오인해 엉뚱한 곳을 바깥 경계로 잡는다. `min<=250` + 연속 4px 로 판정.
    - ⚠️ **액자는 '부드러운 리본'이 아니라 평면 세 개가 각을 이룬 막대다** (`PLANE`/`planeShade`).
      ①바깥면(위·바깥을 향함) ②앞면(정면) ③안쪽 사면(작품으로 내려감). **각 면은 평평하고 경계는
      또렷해야** 한다 — 부드러운 그라디언트로 이으면 인쇄한 띠로 보인다(경쟁사와 같은 배율로
      모서리를 확대해 비교한 결과, 이게 결정적 차이였다. 2026-08-30).
      면 밝기는 광원(좌상단) 기준: 윗변 바깥면은 위를 향해 가장 밝고 안쪽 사면은 그늘, 아랫변은 반대.
      면 경계에 얇은 아리스 선(밝은 선/어두운 선)을 그어야 '깎은 각'이 생긴다.
    - ⚠️ **액자 텍스처를 밴드에 통째로 욱여넣지 말 것** (`drawTextureSide`, index.html).
      1200px 나뭇결 사진을 폭 13~70px 밴드에 `drawImage(...,0,0,W,b)` 로 밀어 넣어 **17~60배 압축**했다.
      결이 사라지고 색만 남은 띠가 되는데 이게 "액자가 납작하다"의 정체였다(2026-08-30 FrameIt 확대 비교).
      **텍스처에서 작은 조각만 잘라 밴드에 맞추고(=결을 확대) 좌우 반전 타일링**한 뒤,
      그 위에 **밴드 폭 기준 절차적 결**을 얹는다 — 판은 화면까지 2~3배 더 축소되므로
      사진 결만으로는 또 사라진다.
      액자 두께도 함께 키웠다(`state.frame` 0.03→0.051, K≈1.46). 경쟁사는 작품 짧은 변의 3.5~6%인데
      우리는 1.5~2.4%라 프로파일이 보일 여지가 없었다. 대신 **플로터 갭(`gap`)은 얇게** 유지할 것 —
      두께와 함께 커지면 검은 띠가 작품을 둘러싼다.
    - ⚠️ **벽 기준 밝기(`wallRef`)는 실제 벽을 샘플링해 구할 것.** 고정값으로 두면
      **흰 벽에서 작품이 통째로 밝아져 뿌예진다**(화이트 브릭 1.18배 부양, 실측).
      `region` 장면엔 `opening` 이 없으므로 `scene.region || scene.opening` 으로 샘플할 것 —
      예전엔 undefined 라 항상 기본값 0.5 로 떨어졌다.
    - ⚠️ **벽 명암은 반드시 흐린 밉맵 단계(`textureLod`, 기본 5)로 읽을 것** — 원본 해상도로 읽으면
      **벽돌·돌 무늬가 작품 위에 그대로 찍힌다**. 우리가 원하는 건 넓은 조명 낙차지 벽의 표면 무늬가 아니다.
    - ⚠️ **[기본 벽]과 [장면]으로 나누지 말 것** (2026-08-30). 예전엔 기본 벽이 옛 평면 합성 경로를
      그대로 쓰고 장면만 새 엔진을 타서, **정작 대부분이 보는 기본 벽이 훨씬 나빴다**.
      지금은 `scenes.json` 15개가 벽 목록을 **대체**하고 토글은 감춘다. 옛 경로는
      **WebGL2 가 없을 때의 폴백**으로만 남는다(그때만 `#walls` 가 보인다).
    - ⚠️ **자동 프레이밍은 사용자 조절(휠·드래그)에 반응하면 안 된다.** 조절 후 배치로 배율을
      다시 정하면 **휠로 작품을 키울 때 배경이 거꾸로 줄어든다**("배경이 같이 움직인다" 신고).
      중립 배치(`solve(fit, null)`)로 배율을 정한 뒤 사용자 조절을 얹는다.
    - ⚠️ **판(insert) 해상도 상한은 비율을 지켜서 걸 것.** 가로·세로에 각각 `min(cap, …)` 을 걸면
      큰 작품에서 한쪽만 잘려 판이 정사각형이 되고, 작품이 contain 되어 생긴 검은 여백이
      **옆면 텍스처로도 뽑혀 작품 오른쪽에 검은 막대가 붙는다**(휠로 키우면 나타남).
    - **장면은 '평평한 매크로 벽 텍스처'가 인테리어 렌더보다 훨씬 잘 먹는다.** 경쟁사(FrameIt)도 전부 그것이다 —
      벽돌·스투코·콘크리트를 정면에서 크게 찍은 사진. 조명이 단순해 합성이 자연스럽고, 작품이 화면을 크게 차지한다.
      우리 `wall01~10`(2600×2600)이 정확히 그 종류라 12개 장면 중 10개가 이것이다.
    - **"붙여넣은 티"의 최대 원인은 벽 명암 불일치다.** 셰이더가 장면사진의 같은 자리 밝기를 읽어 작품에 곱한다(`wallAmt`).
      스포트라이트 낙차·창빛이 작품 위로 이어지지 않으면 작품만 균일하게 떠 보인다. 그레인도 맞춘다.
    - **Flux 장면은 액자 속을 '균일한 빈 회색 캔버스'로 생성할 것** — 그 명암이 곧 **액자 그림자 + 그 방의 조명**이라
      그대로 뽑아 `occlusion`(multiply)으로 쓰면 절차적 흉내와 달리 방의 실제 빛과 어긋나지 않는다.
      액자 속에 그림이 생긴 생성물은 **버린다**(가장 흔한 실패). 장면 JSON 형식은 `scenes/scenes.json` 의 `_readme`.
    - **크기는 '실제 크기' 하나뿐이다** (2026-08-31 사용자 요청으로 단일화). `sizeText`(→`parseSizeCm`)를
      **압축 없이** 벽 대비 실제 비율로 건다 — 30호와 100호가 같은 방에서 정말로 다르게 보인다.
      실측(흰 벽돌, 163×154cm 벽): 4호 5.7% · 10호 14.8% · 30호 43.6% · 100호 44%(영역 상한).
      실내 장면은 확대 여유가 적어 더 작다(갤러리 살롱 4호 1.4%).
      ⚠️ **`sizeMode` 토글도, '실제 크기입니다' 안내문도 없앴다** — 고를 게 없는데 상태를 설명하면
      잔소리다. 남긴 안내는 둘: ①치수 미입력 안내 ②장면 자리보다 커서 줄여 걸었다는 경고(둘 다 행동 가능).
      ⚠️ **작아 보이는 문제는 크기를 부풀려 풀지 말 것** — 카메라를 당겨서(자동 프레이밍) 푼다.
      그래서 `placeInRegion` 의 `gain` 은 **실치수를 아는 작품엔 적용하지 않는다**(`trueScale` 로 판정).
      **`MyPage` 가 `sizeText` 를 안 넘기면 이 기능이 조용히 죽는다**(기본 채움 비율로 앉아 티가 안 난다).
    - ⚠️ **장면 사진을 원본 해상도 이상으로 늘리지 말 것** — 1254px 사진을 2600px 로 뽑으면 2.1배 확대라
      벽 질감이 뭉개진다('확대하면 깨진다'의 원인). `compose()` 가 출력 긴 변을 **장면 원본 크기로 상한**하고,
      자동 프레이밍의 확대도 `maxSrcScale`(1.15배)로 막는다. SNS 는 1080 이면 충분하다.
    - **출력 비율 기본은 1:1, '원본'은 없앴다** — 결과물이 SNS 홍보 이미지라서다. 작품 비율은 액자 안에서 이미 지켜진다.
    - **작품이 없으면 데모 작품(`demo/*.jpg`)을 띄운다** — 예전엔 '불러온 작품이 없습니다' 한 줄만 남아
      아무것도 못 해 보고 나갔다. 데모 그림은 절차적으로 그린 것이라 개인정보가 아니다.
      ⚠️ **실제 가입자 작품(`backend/uploads`)을 `public/` 으로 옮기지 말 것** — 배포물에 개인정보가 들어간다.
    - 자리는 둘 중 하나. **`opening`**(사진에 액자가 있다 — 구멍 4점, 매트가 비율차를 먹는다) /
      **`region`**(액자 없이 캔버스만 — 작품 비율 그대로). ⚠️ `opening` 은 액자가 고정이라 **드래그·휠을 막는다**
      (움직이면 액자 밖으로 삐져나온다). `adjTarget()` 이 null 을 돌려 판정한다.
    - **전경(`foreground`) 레이어는 반드시 워프·오버레이 다음에** 그린다 — 순서가 바뀌면 가리는 게 아니라 가려진다.
      화분·의자가 작품 앞을 지나가면 깊이감이 즉시 생긴다(경쟁사 Frameit 의 간판 기능).
    - 회귀: `frontend/src/__tests__/artlookScene.test.ts`(호모그래피 정확도·실치수·비율 보존·파서·WebGL 없는 폴백).
      **WebGL 이 필요한 부분은 jsdom 에서 못 돈다** — 실제 픽셀 검사는 브라우저 하니스로 따로 돌린다.

37. **ArtLook 화질은 눈이 아니라 지표로 지킨다** (2026-08-30, `scratchpad/vt/`, 사용법은 그 안 README)
    "비슷해 보인다"로 두 번 잘못 판단했다. 이제 FrameIt 결과물을 골든으로 고정해 두고
    **같은 코드로 재서** 임계 안에 있는지 본다(`node render.mjs && python run.py`).
    액자 렌더를 건드리면 **`--vs` 로 이전 기록과 대조**할 것. 임계값은 레퍼런스 실측에서
    나왔으니 **통과시키려고 느슨하게 고치지 말 것** — 그 순간 표가 장식이 된다.
    이 루프로 잡은 실제 결함 다섯(전부 '오려 붙인 티'의 원인이었다):
    - ⚠️⚠️ **Canvas 그림자를 본 캔버스에 바로 `fill()` 하지 말 것.** 그림자는 도형을 같이
      칠하므로 quad 가 **통짜 검정**으로 덮이고, 그 위에 워프된 판을 얹으면 가장자리
      AA 픽셀(알파 .5)이 검정과 섞여 **조각 둘레에 1px 검은 테**가 생긴다
      (실측: 살 167 × .5 = 83, 다음 픽셀 167 × .7 = 117 — 정확히 일치).
      해법은 **캐스터를 캔버스 밖(−2W)에 두고 shadowOffsetX 로 그림자만 끌어오는 것**.
      ⚠️ 파내기(`destination-out`)로 해결하려 하면 이번엔 **밝은 테**가 생긴다(그림자가
      없는 띠가 남는다 — 실측 벽 177 인데 경계 밖이 217). 검은 테를 밝은 테로 바꿀 뿐이다.
    - ⚠️ **판(insert) 둘레에 투명 픽셀을 남기지 말 것.** 워프는 축소 때 밉맵을 쓰는데
      투명 픽셀의 RGB 는 (0,0,0) 이라 가장자리 텍셀이 **검정과 평균**된다.
      contain 반올림으로 생기는 **1px 여백만으로도** 둘레가 어두워진다(실측 182 vs 정상 210).
    - ⚠️ **사진 액자는 스튜디오 확산광으로 찍혀 네 변이 똑같이 밝다.** 그대로 방에 걸면
      위살·아래살 밝기차가 0 이라 아무리 결이 좋아도 **인쇄한 띠**로 읽힌다(FrameIt 은 30~90).
      `shadeFrameProfile()` 이 몰딩을 **세 면**(바깥 모따기 / 앞면 / 안쪽 사면)으로 보고
      장면의 `lightDir` 로 다시 비춘다. 코너는 마이터라 **사다리꼴로 클립**할 것 —
      사각형으로 칠하면 코너가 두 번 어두워진다. 앞면도 완전히 평평하면 안 된다(실측 183→165).
    - ⚠️ **리베이트(작품이 액자 뒤로 들어간 그늘)가 깊이의 유일한 신호다.** 없으면 액자와
      작품이 같은 평면에 인쇄된 것으로 보인다. **파인 홈이라 방향광보다 폐색이 지배** —
      빛을 향한 변에서도 어두워야 한다(진폭 .22 / 기본 그늘 .58).
    - ⚠️ **접지 그림자를 진하게 만들지 말 것.** 같은 흰 벽돌벽에서 FrameIt 은 벽 밝기를
      18~22 레벨만 떨어뜨리는데 우리는 73 이었다. 진한 접지선은 '닿았다'가 아니라
      **오려 붙였다**로 읽힌다. 넓고 옅은 투영이 주역이고 접지는 거들 뿐이다.
    - ⚠️ **옆면 조명 부호를 뒤집지 말 것.** `lightDir` 은 빛이 **오는** 방향이므로
      면 법선과의 내적이 크면 밝다. 예전엔 `-(n·ld)` 라 좌상단 조명인데 아래·오른쪽
      옆면이 밝게 칠해져 두께가 거꾸로 읽혔다. 그리고 **보이는 옆면은 빛을 받는 쪽**이어야
      한다(`view [0.58,0.32]`) — 그늘진 면이 보이면 그림자와 섞여 두께가 안 읽힌다
      (FrameIt 캔버스랩도 왼쪽·위 면이 보인다: 그쪽 단면 낙차 42~59, 반대쪽 5~8).
    - 살 두께는 **조각 폭 대비 4~6%** (FrameIt 실측 2.8~7.6, 평균 4.5). 홍보 이미지의
      주인공은 작품이지 액자가 아니다.

38. **매트(대지)와 액자↔작품 '틈'** (2026-08-30, 지표는 `scratchpad/vt/mat.py`)
    "매트가 무슨 그냥 하얀 색", "틈이 조악하고 평면적"이라는 지적에서 나왔다.
    레퍼런스는 FrameIt Pro 결과물 4장(`sampleframe/sample2/3/4.png`, 저작권상 커밋 금지).
    - ⚠️ **매트는 색면이 아니다.** 확대하면 ①직조 결 ②방향광 기울기 ③개구부 45° 사면
      ④액자가 드리우는 그림자 넷이 다 있다. 우리는 결 std 0.92(골든 6.4~13.5)로
      사실상 아무것도 없었다.
    - ⚠️ **결을 `multiply` 로 얹지 말 것 — 매트 색이 통째로 어두워진다**(흰 매트 231→204).
      screen 을 더해도 흰색은 밝아질 여지가 없어 못 되돌린다. **평균이 매트색과 같은
      불투명 타일**을 만들어 덮는다(`matPaper`/`paperTile`). 결 세기는 매트 밝기에 비례
      (골든도 흰 매트가 더 세다 — 어두운 종이는 그늘이 얕아 결이 덜 드러난다).
    - ⚠️ **직조 성분을 세게 주면 체크무늬 천이 된다.** 규칙적 격자가 눈에 띈다 —
      직조는 거들고 랜덤이 주다. 다만 직조는 **주기가 있어 축소돼도 남으므로** 빼면 안 된다.
    - ⚠️ **매트 음영에 대각선 그라디언트를 쓰지 말 것.** 좌상단 '모서리'만 최대라 매트
      위쪽 중앙이 이미 30% 지점이 되어 위↔아래 낙차가 4분의 1로 죽는다. 세로를 지배시킨다.
    - ⚠️ **액자가 매트에 드리우는 그림자**를 빼먹지 말 것(골든 11~26레벨). 없으면 액자와
      매트가 같은 평면에 인쇄된 것처럼 보인다.
    - **틈은 다섯 마디다** (sample4 흰 액자 실측):
      앞면 255 → 급락 110(그늘) → 회복 171(반사광) → **밝은 립 255**(개구부 모서리)
      → 작품 위 그림자 102 → 작품 122. 우리는 급락 하나뿐이라 '대충 어둡게 칠한 테두리'였다.
      **밝은 립**이 '세심하게 만든 액자'의 인상을 만든다(`LIP` 상수).
    - ⚠️⚠️ **`shade()` 의 반환값을 다시 `shade()` 에 넣지 말 것.** 반환이 `rgb(…)` 문자열인데
      `hexToRgb` 는 16진수만 파싱한다 → NaN → `NaN|0` = 0 → **새까만 검정**. 실제로 이 실수로
      매트 개구부가 통째로 검게 칠해져 틈이 '오려 낸 구멍'이 됐다(낙차 90%, 골든 48%).

39. **장면의 `lightDir` 을 손으로 적지 말 것 — 사진에서 재라** (2026-08-30, `scratchpad/vt/lightdir.py`)
    17개 장면 전부 `[-1,-1]`(좌상단)로 적혀 있었는데, 실제 사진은 달랐다 —
    컬렉터 살롱은 벽 위쪽 110 / 아래쪽 159 로 **반대**였다. 그러면 액자 살·매트 음영이
    방과 거꾸로 칠해진다. 실내 장면 대부분이 어긋나 있었다(11개 중 10개).
    ⚠️ 다만 **측정값을 그대로 쓰면 안 된다** — 벽 밝기 기울기는 '조명 낙차'지 '광원 위치'가
    아니다. 실내 사진은 바닥·가구 반사로 벽 아래가 밝은 경우가 많은데, 그대로 쓰면
    **그림자가 위로 지고 접지 그림자가 사라진다**(실측 48→1.7). 가로 성분(창이 어느 쪽인가)은
    믿되 **세로는 항상 위**로 둔다. 평평한 매크로 벽은 기울기가 약해(<4) 기존 값을 유지한다.

40. **벽 명암 밉맵 단계를 고정값으로 두지 말 것** (2026-08-30, 사용자 신고
    "그림이 투명해져서 벽문양이 비친다") — 5단계 고정은 '1/32 축소'라는 **상대** 값이라
    사진이 클수록 덜 흐려진다. 2600px 매크로 벽(스톤·벽돌)에서는 81텍셀이 남아 줄눈이
    작품 위에 그대로 곱해졌다(단색 작품으로 실측: 스톤 20%·회벽돌 25% 통과,
    인테리어 사진 1254px 은 6%). **사진 크기와 무관하게 같은 굵기(≈11텍셀)로** 뭉갠다 →
    유출 5.6% 이하, 조명 낙차는 유지(5.3→5.0). 검증은 `scratchpad/vt/leak.mjs`+`leak.py`
    (**단색 작품**을 걸면 작품 영역에 남는 무늬가 곧 유출량이다).

41. **ArtLook 은 '액자가 주인공인 제품 사진'이다 — 배경이 아니라** (2026-08-30 3차, 지표는
    `scratchpad/vt/presence.py`). 사용자 지적: "벽 배경 위에 액자를 합성한 AI 이미지 같다,
    벽이 너무 세고 액자는 너무 작다". 넷 다 **실측으로 확인된 결함**이었다.
    - ⚠️ **자동 프레이밍의 목표는 높이가 아니라 '화면에서 차지하는 면적'이다** (`u.frameArea`,
      기본 0.44). 예전엔 "작품 **높이**가 화면의 52%"였는데, 세로 작품(4:5)이면 면적은
      21% 뿐이다 — 실측 중앙값 **16.9%**, FrameIt Pro 결과물은 **44.1%**. 높이로 잡으면
      가로·세로·정사각의 존재감이 제각각이 된다.
      키우는 순서도 중요하다. ①**영역 안에서**(`placeInRegion` 의 `gain`) 먼저 키우고
      ②모자란 만큼만 카메라를 당긴다 — ①은 벽 화질 손실이 0이다.
      ⚠️ `gain` 은 **실제 크기 모드에서 쓰지 않는다**(벽 대비 비율이 곧 정직한 치수다).
      ⚠️ `gain` 과 사용자 조절(`scale`)은 별개 축이다. 섞으면 휠 확대에 자동 프레이밍이
      반응해 배경이 거꾸로 움직인다(36번 참고).
    - ⚠️⚠️ **출력 크기를 장면 원본과 같게 두면 확대 여지가 사라진다.** 자동 프레이밍은
      `zoom × (출력/원본) ≤ 1.15`(원본 이상으로 안 늘림) 안에서만 커질 수 있는데, 출력을
      원본 크기로 잡으면 그 비가 1 이라 **한도가 1.15배**뿐이다. 그래서 액자가 16% 에서
      더 못 컸다. 출력을 원본의 1/1.6 로 잡으면(2600→1625) 같은 화질 보증 아래 1.84배까지
      당긴다(`SCENE_HEADROOM`/`SCENE_MIN_LONG`). SNS 홍보물이라 1080~1650px 이면 충분하다.
    - ⚠️ **벽 텍스처가 액자보다 세면 안 된다.** 우리 벽 사진의 결(1200px 정규화 std)이
      beige-plaster 15.6 · stone 14.4 · travertine 12.4 인데 FrameIt 기준 벽은 **2.9** 였다.
      같은 사진의 **흐린 판을 알파로 덮어**(`wallCalm`) 결만 지우고 조명 낙차는 남긴다
      (`원본×(1−c)+흐림×c` = 언샤프의 역). **반드시 화면 전체에 균일하게** — 액자 둘레만
      흐리면 그게 곧 마스크 테·헤일로다. 조각은 그 다음에 그리므로 작품·액자는 안 흐려진다.
      세기는 손으로 적지 말고 `wallcalm.py` 로 **재서** 넣을 것. **실내 사진은 목표를 느슨하게**
      (벽 3.3 / 방 4.0) — 방의 결에는 화분·조명 같은 진짜 물체가 섞여 있어 세게 흐리면
      가짜 아웃포커스가 된다.
    - ⚠️ **우리 액자는 실제 액자보다 쨍하다.** 살의 채도 폭이 오크 60.4 · 골드 97.3 인데
      FrameIt 은 24.5 · 36.1 이다. 원색에 가까운 주황 오크·놋쇠 골드가 곧 "plastic wood",
      **'AI 가 그린 액자'** 의 정체였다. 재질별 [채도, 밝기]를 `FRAME_GRADE` 로 **로드 때
      한 번만 구워** 둔다(매 렌더 필터가 아니라 오프스크린 — 액자칩까지 같은 색이 된다).
      ⚠️ **hex 색과 텍스처 이미지 둘 다** 보정할 것 — 하나만 하면 밴드와 결의 색이 어긋난다.
      ⚠️ 구운 액자는 `<canvas>` 라 `naturalWidth`·`complete` 가 없다. 안 채우면
      `drawTextureSide` 가 로딩 실패로 보고 **결 텍스처가 조용히 단색으로 폴백**한다.
    - ⚠️ **사진 액자의 개구부는 정중앙이 아니었다** — GPT 원본이 완전한 정면이 아니라
      오크는 위살 119 / 아래살 91(**26.7% 차이**)이었다. 렌더에서는 '작품이 액자 안에서
      아래로 내려앉은' 것으로 보인다. `frontend/scripts/symmetrize-photo-frames.py` 가
      **개구부를 고정하고 바깥을 깎아** 네 살을 같게 맞춘다(멱등). 그리는 쪽에서 밴드 폭만
      대칭으로 주는 방법은 **기각** — 코너 조각이 눌려 마이터가 45°에서 기운다.
      추출기를 다시 돌리면 `artlookScene.test.ts` 가 먼저 깨진다. 그때 이 스크립트를 한 번 더.
    - **작품은 그대로다** — `artpreserve.py` 가 원본 파일과 직접 대조한다(실측: 비율오차
      ≤0.17% · 평균 ΔRGB 1.6~8.4 · 채도비 0.98~1.10 · 구조상관 ≥0.992). 즉 바뀌는 건
      **사진의 노출·화이트밸런스 수준**이지 색·디테일이 아니다. ⚠️ ΔRGB 를 0 으로 만들려
      하지 말 것 — 방 조명과 무관한 작품은 오려 붙인 것으로 보인다(1차 라운드에서 확인).

42. **합성은 출력보다 크게, 줄이는 건 맨 마지막 한 번뿐** (2026-08-30 4차)
    - ⚠️ 예전엔 출력 크기 캔버스에 **바로** 합성했다. 즉 마스크·그림자·워프·워터마크가
      전부 최종 해상도에서 계산됐다. 지금은 `SUPERSAMPLE`(1.6) 배 캔버스(`stageCanvas`)에서
      그리고 `finish()` 가 **한 번만** 내린다. 실측: 그림자 계조 거칠기 10/10 개선
      (119→136 → 94~186 범위 전부 감소), 워터마크 글자 경계가 매끈해졌다.
      ⚠️ **기하는 절대 변하면 안 된다** — 10케이스 조각 사각형이 픽셀 단위로 동일한지
      확인할 것(실측 Δ 조각 안 평균 1.39 / 벽 0.33, 큰 차이는 워터마크 글자뿐).
    - ⚠️ **확대 한도(`maxSrcScale`)에 SS 를 곱해 넘길 것.** 합성 캔버스가 커진 만큼
      `base = 합성W/원본` 도 커지므로, 그대로 두면 한도가 SS 배 빡빡해져 **액자가 다시
      작아진다**. 곱해 주면 최종 벽 배율이 정확히 그대로다.
    - ⚠️ **그레인은 SS 를 곱할 것.** 합성 픽셀 단위 잡음이라 축소하면 그만큼 옅어진다.
    - ⚠️ **화면에 넘기는 좌표는 출력 좌표로 되돌릴 것**(`toOut`, `artlookProbe(…, SS)`).
      안 그러면 드래그 판정과 회귀 하니스가 SS 배만큼 어긋난다.
    - **그림자는 세 겹**(`LAYERS`): 넓은 반그림자 0.160/0.065 · 투영 0.050/0.170 ·
      접지 0.014/0.140. 두 겹만 썼을 때 감쇠 곡선을 골든과 비교하니 가까이는 같은데
      **꼬리가 일찍 죽었다**(조각 폭 8% 지점 0.02~0.06 vs 골든 0.10~0.12) — 벽으로
      퍼지는 느낌이 없었다. ⚠️ 겹을 더할 땐 **가까운 쪽 총량이 늘지 않게** 주 투영을
      줄일 것(접지가 진해지면 '오려 붙인 티'로 되돌아간다).
    - **벽 진정 세기는 렌더를 보고 되먹임으로 맞춘다**(`scratchpad/vt/calmtune.py`).
      원본 결만 보고 계산하면 빗나간다 — 자동 프레이밍이 카메라를 당기면서 벽 무늬가
      측정 대역 밖으로 밀려나기 때문(gallery-living 실측: 예상 4.0 → 실제 2.0, 과하게 뭉갬).
      ⚠️ 목표는 **밴드**(2.5~4.5)다. 위로 넘치면 시끄럽고 **아래로 빠지면 배경이 정보 없는
      면**이 된다 — 그것도 합성 티다(골든 최저 2.86).
    - ⚠️⚠️ **명암 기울기를 판(plate)에만 걸지 말 것 — 판 경계에서 벽과 어긋나 검은 테가 된다.**
      골든의 윗살 밝기를 길이 방향 9곳에서 재면 160·183·178·178·166·172·169·176·**155**
      로 8.5~104% 흔들리는데 우리는 **3%**, 사실상 상수였다("액자가 따로 조명된 물체 같다"의
      실체). 고치려고 판에 곱셈 램프를 걸었더니 빛을 등진 아래·오른변에서 판이 12% 어두워져
      **벽 228 / 판 147 의 검은 선**이 생겼다(사용자가 금지한 dark outline).
      골든을 다시 보면 **단조 기울기가 아니라 양 끝이 어둡다** — 광원 램프가 아니라
      **마이터 코너의 폐색**이다. 그래서 `composeScene` 이 **화면 좌표에서** 조각 네 모서리에
      부드러운 곱연산 폐색을 건다(`cornerAO`). 액자와 그 옆 벽이 **함께** 어두워지므로
      단차가 없고, 브리핑이 요구한 "frame 주변 벽의 미세한 ambient occlusion"도 이걸로 채운다.
      ⚠️ 반경·감쇠 모양을 함께 볼 것 — 넓고 완만하면 **둥근 얼룩**으로 보이고(0.16×조각에서
      실제로 그랬다), 좁으면 살 끝에 닿지 않아 아무 효과가 없다(0.085 에서 지표 무변화).
      현재 0.13×조각 + 급→완 4단 감쇠. 렌즈 비네팅(`VIGNETTE`)도 **화면 전체**에만 건다.
    - ⚠️ **흰색 알파로 밝은 쪽을 올리지 말 것.** `source-atop` 의 흰색은 `base×(1−a)+255a` 라
      **어두울수록 상대 상승폭이 크다**(살 40 → +32%, 리베이트 골 20 → +70%). 검은 슬림
      액자의 리베이트 대비가 0.25 → 0.23 으로 깎였다. 검은색만 써서 **곱셈**으로 어둡게 할 것.
    - **조각은 정중앙에 두지 않는다** — 자동 프레이밍이 도입되며 `fitScene` 의 `zoom>1.02`
      분기가 항상 타서 중심 이탈이 **0.0%** 가 됐다(골든 0.1~3.4, 중앙 0.7). 눈높이 카메라
      기준으로 조금 위(0.485)에 둔다 → 1.5%.
    - **경계 날카로움은 이미 사진 수준이다** — `edge_acut` 골든 0.58~1.29 / 우리 0.50~0.79.
      ⚠️ 이 지표는 **벽과 살의 밝기가 비슷하면 분모가 0 에 가까워 폭발한다**(실측 t08:
      벽 152·살 156 인데 2.67 로 나왔다. 경계는 멀쩡했다). 대비 20 미만은 측정 불가로 뺀다.
    - **디버그 마스크**: `?debug=1` 로 열면 `window.__artlookDebug(override)` 가 생긴다.
      outer/front/bevel/mat/opening 마스크 + 접지·투영·반그림자 레이어 + BEFORE/AFTER 를
      돌려준다. ⚠️ 마스크는 **렌더와 같은 quad 로 워프**해 만든다(`u.maskPlate`) —
      따로 계산하면 디버그 그림이 거짓말이 된다. 하니스는 `scratchpad/vt/masks.mjs` +
      `masksheet.py`(상호배타·유출 검사로 실패를 낸다).

43. **ArtLook 조명 — 밝히지 말고 주변을 어둡게 하라** (2026-08-31, 지표 `scratchpad/vt/lightref.py`·`lightcheck.py`)
    레퍼런스는 FrameIt Pro 조명 샘플 16장(`~/ArtLink/light/`, **커밋 금지**) — 프리셋 12개 +
    같은 프리셋의 강도 4단계(20/50/70/100). 강도 시리즈에서 나온 것이 설계를 그대로 정했다.
    - **강도를 올릴 때 일어나는 일의 주역은 '밝아짐'이 아니라 '주변이 어두워짐'이다.**
      opacity100 의 (조명후/조명전) 배율을 광원 기준 거리별로 재면 중심 1.44 → 1.07 → 0.76 →
      **0.65 에서 평평**, 화면 평균 변화는 **−58 레벨**이었다. 흰빛을 더하는 방식으로 만들면
      그게 곧 라운드 3~5 에서 금지한 HDR·글로우·헤일로다.
    - ⚠️⚠️ **밝히는 항을 조각 위에 얹지 말 것.** 조각이 화면의 44%(41번)라 `screen` 웅덩이를
      조금만 안쪽으로 놓아도 그림 위에 앉는다 — 실측 **작품이 76% 밝아졌다**. `screen` 은
      어두운 픽셀을 가장 크게 올리므로 그림이 제일 심하게 뜬다. 밝히는 항(`spill`)은
      **조각의 변** 기준으로 바깥에만 두고, 반반경(대각선) 기준으로 잡지 말 것.
    - ⚠️ **균일 캐스트에 `soft-light` 를 쓰지 말 것 — 검정이 들린다**(Cb=0.1 이 +11%).
      어두운 방에서 낙차를 그대로 상쇄해 조명이 무효가 됐고(그늘구석 0.97), 작품 채도비도
      0.83 까지 떨어졌다. 곱연산은 밝기 비를 보존한다.
    - ⚠️ **곱연산 그라디언트의 끝 색은 어두워야 한다.** 밝은 색(232,236,255)을 적어 놓고
      어두워지길 기대했더니 `base·(1−a(1−C))` 라 아무 일도 안 일어났다(그늘구석 1.00 = 무효).
    - ⚠️ **낙차 타원을 늘였으면 반경을 그 좌표계에서 다시 계산할 것.** 변환만 걸고 반경을
      두면 화면 구석이 그라디언트 끝에 못 닿아 **어두워짐이 0.67 → 0.95 로 사라진다**.
      늘이는 방향은 **광원 축에 수직**(aniso<1) — 광원 축으로 늘이면 그늘 쪽까지 밝게 남아
      방향성이 1.48 → 1.06 으로 죽는다.
    - **작품 보호는 마스크가 아니라 기하로.** 낙차의 안쪽 반경이 조각 네 귀퉁이를 다 덮으므로
      작품 영역은 정확히 배율 1 이다 — 경계가 없으니 마스크 테도 없다(경계 spike 무변화).
    - **FrameIt 은 강도 50 부터 작품을 망가뜨린다** — 작품 채도 92.6 → 63.7(50) → 46.5(100),
      지역 대비 14.2 → 9.4. 홍보 이미지의 주인공은 작품이라 **거기까지 따라가지 않는다**.
      우리 강도 100 = 작품 dE 0.7~2.8 · 채도비 0.90~1.00 · 대비비 0.98~1.00,
      그늘 쪽 구석 0.67~0.70(FrameIt 70% 가 0.69) · 방향성 1.42~1.49.
      회귀는 `lightcheck.py` — **작품 채도 한계는 `artpreserve.py` 와 같은 0.90** 이다
      (조명을 100 까지 올려도 작품 보존 약속은 그대로여야 한다).
    - 화면은 **강도 0~100 슬라이더 하나**다. 종류(없음/스포트/소프트)는 없앴다 — 세 값이
      같은 일의 강약이었고, ⚠️ **옛 라이팅맵은 장면 분기에서 호출조차 되지 않아** 장면이
      유일한 배경이 된 뒤로는 무엇을 골라도 화면이 그대로였다(에러 없이 조용히).
      기본값 0. 회귀는 `artlookScene.test.ts` 가 소스에서 호출부를 확인한다.
    - 조명 색은 **그 장면 사진의 밝은 10% 색**(`sceneLightColor`) — 방마다 전구색이 다른데
      따뜻한 색을 고정으로 박으면 화이트큐브에서 혼자 노래진다. 편차 상한 30레벨.
    - 옛 라이팅맵 `public/artlook/effects/`(eff1·eff2)는 **FrameIt 자산이라 상용 금지**라고
      ATTRIBUTION 에 적혀 있었는데 `frontend/public/` 이라 배포물에 들어가고 있었다. 삭제했다.

44. **ArtLook 매트는 캔버스 랩만 빼고 전 스타일에서 쓴다** (2026-08-31)
    옛 주석("플로터는 갭이 매트 역할을 하므로 제외")은 틀렸다 — **갭과 매트는 다른 물건**이다.
    갭은 액자 안쪽 벽이 뒤로 물러난 *면*, 매트는 그 안에 깔린 *종이판*이고, 둘이 함께 있는 게
    실제 액자업의 **플로트 마운트**(작품을 보드 위에 띄워 네 변을 다 보이게 하는 마감)다.
    바깥부터 [몰딩] → [갭(오목한 안쪽 벽)] → [매트 보드] → [떠 있는 작품] 순.
    - ⚠️ 순서를 지킬 것 — 보드를 먼저 깔고 갭을 그려야 갭의 그늘 선이 보드 위에 얹힌다.
    - ⚠️ 플로터에는 **개구부 사면(`matOpeningBevel`)을 그리지 말 것** — 작품이 보드 위에
      얹혀 있지, 뚫린 창으로 들여다보는 게 아니다. 있지도 않은 종이 두께가 작품 위에 생긴다.
    - **캔버스 랩만 못 한다** — 액자가 없어 매트를 받칠 것이 없다. 넣으면 벽에 흰 종이가
      혼자 떠 있는 그림이 된다. 못 고치는 제약이라 비활성 + 이유를 적어 둔다.
    - '화이트 매트'(kind:'mat')도 슬라이더를 따른다(하한 3%). 예전엔 style 고정값만 봐서
      **그 스타일에서만 매트 줄이 안 먹었다** — 같은 컨트롤이 스타일마다 먹었다 말았다 하면 고장으로 보인다.
    - 액자 이름에서 **'(사진)'을 뗐다** — 고르는 사람에게 구현 방식(9-slice 사진인가 절차적인가)은
      알 바가 아니다. 구분은 `kind:'photo'` 로 이미 명확하다.

44b. **ArtLook 물리 일관성 — 세게 넣지 말고 관계를 맞춘다** (2026-08-31 6차, 지표 `scratchpad/vt/physics.py`·`framelight.mjs`)
    브리핑: "shadow·shading 이 실제 물체의 물리적 관계를 따르지 않는다. 효과를 더 넣지 말고
    **한 촬영 환경에서 액자만 교체된 것처럼** 만들라." 3~5차 지표가 전부 통과하는데도 남은 지적이라,
    잣대를 **세기에서 관계로** 바꿨더니 결함 다섯이 전부 실측으로 드러났다.
    - ⚠️⚠️ **`hexToRgb` 는 `rgb(...)` 도 받아야 한다.** `gradeHex()`(재질 색 보정)와 `shade()` 가
      **둘 다 `rgb(…)` 문자열을 돌려주는데** 파서는 16진수만 읽었다 → `parseInt('rgb(44,40,35)',16)`
      = NaN → `NaN|0` = 0 → **순검정**. 그래서 플로터 트레이가 **오크·골드·월넛에서만** 새까맸다
      (틈 바닥 휘도 0~2, 골든 최저는 살 대비 0.110). 검정·실버·아이보리는 보정값이 [1,1] 이라
      hex 가 그대로 나와 멀쩡했고, 그래서 '일부 액자만 이상하다'로 보였다. 같은 함정이 38번
      (매트 개구부가 통째로 검게)에도 있다 — **파서 쪽에서 막았다.**
    - ⚠️⚠️ **깊이를 stroke 로 만들지 말 것.** 지우고 나서야 얼마나 많았는지 드러났다 —
      작품 둘레 검은 선(리베이트·캔버스랩·플로터 3곳) · 작품 둘레 흰 선 · 액자 바깥 밝은 테
      (`c.edge`) · 살 안쪽 검은 선 · 갭 안쪽 검은 선(alpha .68) · 개구부 밝은 립.
      **1px 짜리는 무엇이든 확대하면 '그려 넣은 선'으로 읽힌다.** 깊이는 **면**이 만든다.
      ⚠️ 다만 **가짜를 지웠으면 진짜로 채울 것** — 검은 선을 지웠더니 얇은 액자의
      `rebate_ratio` 가 0.3 → 0.2(임계 0.24) 로 떨어졌다. `frameBand` 의 리베이트 **그라디언트**를
      깊게 해서 되찾았다.
    - ⚠️⚠️ **맞닿은 밴드를 여러 개 그리지 말 것 — 경계 1px 이 뜬다.** canvas 는 폴리곤 가장자리를
      안티에일리어싱하므로 공유 경계를 양쪽이 **절반씩** 덮고, 곱연산에서 절반 알파를 두 번 거는
      건 온전히 한 번보다 **덜 어둡다**(실측 골드 `118 151 120`). 1px 겹쳐 그리면 이번엔 그 자리가
      **두 번 어두워진다**(중앙값 잔차 15.9 → 19.9). 살 한 변은 **폴리곤 하나 + 다중 스톱
      그라디언트 한 쌍**(multiply/screen)으로 칠한다 — 면 구분은 **스톱 위치**로 준다.
      ⚠️ 스톱은 **꺾이지 않게** — 직선 두 토막이면 꺾이는 자리에서 2차 미분이 튄다(오크가 네 위치
      전부 **살의 88%** 에서 잡혔다. 같은 자리에서 반복되면 텍스처가 아니라 우리가 그린 것이다).
    - ⚠️ **개구부에 밝은 립을 두지 말 것**(38번의 `LIP` 폐기). 최댓값이 살의 **마지막 픽셀**에
      오고 그 다음은 곧바로 작품이라 무엇을 해도 1px 밝은 선이 된다(램프로 바꾸고 꼭대기를 안으로
      옮겨도 얇은 살에서 재발). 근거였던 sample4 는 한 장이고, **골든 5장을 같은 코드로 재면 그런
      임펄스가 없다**(2.1~14.4). 브리핑 3번의 모델도 앞면→사면→파인 자리→작품의 **단조**다.
    - ⚠️⚠️ **그림자는 블러보다 오프셋이 커야 한다.** 예전엔 반그림자 블러 0.160 에 오프셋 0.057
      — **번지는 폭이 밀려난 거리보다 넓어** 사방 균일한 회색 테(=브리핑이 금지한 generic drop
      shadow)였다. 실측 `aniso` 골든 중앙 16.2 ↔ 우리 5.6. 뒤집되 **가까운 쪽 총량은 그대로**
      두었다(0.216 → 0.214) — 광원 쪽만 0.024 → 0.002 로 걷어낸다. 즉 **더 진하게 만드는 변경이
      아니다.** 접지(contact)는 벽과 맞닿은 **틈의 폐색**이라 사방에 조금씩 있는 게 맞다(건드리지 말 것).
      `lightDir` 은 **정규화해서** 쓸 것 — scenes.json 의 절반이 `[-1,-1]`(크기 1.414)이라
      그 장면들의 그림자만 이유 없이 **41% 길었다**.
    - ⚠️ **광원 방향을 손으로 적은 자리를 남기지 말 것**(브리핑 5번 = 같은 방·같은 카메라).
      실제로 넷이 좌상단 고정이었다: 플로터 트레이(`[.16,.02,-.14,.08]`, **부호까지 물리와 거꾸로**)
      · 마이터 코너 하이라이트 · 작품 위 정면광 그라디언트 · 절차적 액자의 리베이트.
      **장면 15개 중 10개가 좌상단 광이 아니다.** 실측(`framelight.mjs`, 좌우 광을 뒤집어 비교):
      플로터가 광원에 **전혀 반응하지 않았다**(Δ≈0). 지금은 18종 전부 Δ 5.1~37.
      사진 액자·절차적 액자가 **다른 리베이트 모델**을 쓰던 것도 통일했다 — 그 탓에 액자만 바꿔도
      작품 밝기가 달라졌다(편차 1.6 → **0.5** 레벨).
    - ⚠️ 밝기를 **색에 더하지 말 것**(`shade(back, light+…)`) — 양 끝에서 뭉갠다. 아이보리 트레이
      (L≈216)는 밝은 쪽이 255 로 붙어 좌우 차이가 사라지고(반응 0), 월넛(L≈29)은 어두운 쪽이 0 으로
      붙는다. **곱/스크린은 비율**이라 트레이 색과 무관하게 같은 방향성을 준다.
    - 합성 해상도 `SUPERSAMPLE` 1.6 → **2.0**(브리핑 8번). ⚠️ 확대 한도에 SS 를 곱해 넘기는 규칙
      (42번)은 그대로 — 안 그러면 액자가 다시 작아진다.
    - 디버그(브리핑 9번): `?debug=1` → `window.__artlookDebug({...})`. 마스크·그림자 3겹에
      **조명 변조 레이어**(중간 회색 판에 같은 코드를 걸어 뽑는다)와 **토글 목록**을 더했다
      (`shadow`·`cornerAO`·`wallCalm`·`wallAmt`·`innerShadow`·`glass`·`mat`·`depth`·`autoFrame` 등).
    - 검증: `physics.py`(그림자 방향 9/9 · 틈 바닥 0.12~0.55) · `framelight.mjs`(전 액자 광원 반응) ·
      `run.py`(기존 7지표, 남은 실패는 문서화된 t03 하나) · `artpreserve.py`(**10/10**, 비율오차
      ≤0.20% · ΔRGB 1.3~7.8 · 채도비 0.98~1.09).

44c. **ArtLook 최소 크기 — 실치수를 지키되 바닥은 둔다** (2026-08-31 신고, `scratchpad/vt/minsize.mjs`)
    "어느 작가 첫 작품 · 오크 · 갤러리 살롱 → 보이지도 않는다". 그 작품은 **12×12cm** 이고,
    실치수대로 걸면 화면의 **0.2%**(짧은 변 40px)다. 실측 4호도 1.43%(113px/1080).
    - ⚠️⚠️ **딱딱한 바닥(clamp)으로 두지 말 것.** 처음에 "7% 미만이면 7%" 를 넣었더니 갤러리
      살롱에서 **12cm·24cm·31cm·45cm·60cm 가 전부 7.00%** 로 붙었다 — 고치려던 실치수 기능을
      그 구간에서 죽인 셈이다. **무릎(soft knee)** 으로 둔다: `reach ≥ KNEE`(11%)면 무보정,
      아래면 `[0,KNEE]` 를 `[FLOOR,KNEE]`(5.5%~) 로 **선형 사상**한다. 순서가 보존되고 KNEE 에서
      값이 이어져 단차가 없다. 결과: 12cm 5.65% · 4호 6.62% · 30호 10.95% · 100호 34.5%.
    - **위쪽도 무릎이다**(2026-08-31 신고: "아무리 커도 벽 한 면을 꽉 채우는 게 말이 되냐").
      예전 `maxT = min(1, aspect/A)` 이라 영역보다 큰 작품이 **벽 끝에서 끝까지** 걸렸다 —
      실측: 흰 벽돌 영역이 163×154cm 인데 100호(130×162)는 세로가 영역보다 커서 **100%**,
      50호(91×116)도 75% 였다. 무릎 `KH=0.55` 위로는 포화 곡선으로 눌러 천장 `CEIL=0.82`
      에 닿지 않게 한다(벽 여백 최소 18%). 30호 0.59→0.59(무변화) · 50호 0.75→0.67 ·
      100호 1.05→0.73.
      ⚠️ 여기서도 **clamp 는 기각**했다 — 0.72 로 자르니 50호와 100호가 똑같아졌다.
      ⚠️ **상한에 걸렸다고 카메라를 멈추지도 말 것.** 그렇게 했더니 100호가 30호보다
        **작아졌다**(16.3% vs 43.7%, 순서 역전). 자동 프레이밍은 그대로 두고 영역 비율만 누른다.
      ⚠️ 안내문(`over`)의 기준은 **물리 한계**(`physMax`)로 유지할 것 — 천장 기준으로 바꾸면
        멀쩡히 들어가는 작품에 "자리보다 커서 줄였다"가 뜬다.
    - ⚠️⚠️ **절대 상한은 '영역'이 아니라 '화면' 기준으로 걸 것** (2026-08-31 재신고).
      벽 영역을 70% 로 묶었는데도 자동 프레이밍이 목표 면적을 채우려고 카메라를 다시 당겨
      **화면 긴변이 74.9%** 였다. 사용자가 보는 건 화면이다 —
      `composeScene` 이 조각의 **긴 변 ≤ 화면의 70%**(`maxLongFrac`)로 zoom 을 깎고,
      zoom 을 1 까지 내려도 넘치면 영역 안에서 줄인다.
      ⚠️⚠️ **줄일 때 `gain` 을 낮추지 말 것 — 한 픽셀도 안 변한다.** 자동 프레이밍이 영역을
        꽉 채워 놓으면 `t` 가 이미 `physMax` 에 **포화**돼 있어, gain 을 줄여도 다시 클램프된다
        (갤러리 살롱에서 상한 0.49 를 걸었는데 54.6% 가 그대로 나왔다). `placeInRegion` 에
        `fillMax` 로 **t 자체의 상한**을 넘긴다.
      ⚠️ 화면 차지는 **바운딩 박스**로 잴 것 — `quadSize` 는 마주보는 변의 평균이라 원근이
        있는 실내 장면에서 작게 나온다(계측 훅도 사용자 눈도 바운딩 박스를 본다).
      ⚠️ **상한은 장면마다 다르다**(`scenes.json` 의 `maxLong`). 평면 매크로 벽(wall01~09)은
        벽면만 크게 찍은 사진이라 70% 도 '벽에 걸린 그림'으로 읽히지만, 방이 통째로 보이는
        실내 사진(wall11~17)에서 같은 비율이면 **벽 한 면을 삼킨 크기**다 → 0.49(0.70×0.70).
      ⚠️ 영역에도 같이 상한을 두지 말 것 — 자동 프레이밍 배수(`gain`)까지 깎여 이중으로 걸린다
        (기본 채움 0.5 가 0.47 로, gain 1.3 배가 1.13 배로 깎였다. 테스트가 잡았다).
      ⚠️ **치수 미입력 작품을 잊지 말 것** — 실서버 작품 372점 중 361점(97%)이 치수가 없어
        `fill × gain` 경로를 탄다. 천장을 실치수 배치에만 걸었더니 **정작 대부분이 보는 경로가
        그대로 벽을 채웠고**, 신고된 그림이 정확히 그 경우였다.
    - 순서는 **카메라 먼저**(36·41번의 원칙) — 바닥에 못 미치는 작품에서만 확대 한도를 1.25배
      늘리고(배경이 조금 무른 건 소품 근접 촬영에서 오히려 자연스럽다), 그래도 모자란 만큼만 부풀린다.
    - ⚠️ **부풀렸으면 반드시 알린다** — `fitNote.enlarged` 로 "N배 크게 걸었습니다 + 벽 장면을
      골라 보세요". 실치수가 이 기능의 약속이라 말없이 부풀리면 그 약속이 조용히 깨진다.
      `enlarged` 는 **결과에서 잰다**(gain 을 그대로 쓰면 `placeInRegion` 이 영역 밖으로 못 나가게
      다시 깎는 만큼 과대 보고된다).

44d. ⚠️ **ArtLook 장면 엔진은 `main` 에 있고 `deploy/render` 에는 아직 없다.**
    (2026-08-31 발견 → 그날 `main` 에 커밋. 배포는 사용자 지시로 보류)
    `scene.js` · `scenes/scenes.json` · `frames/photo/` 는 한 번도 add 된 적이 없어
    3~6차 개선이 전부 로컬에만 있었다. 지금은 33개 파일이 추적된다(`git ls-files
    frontend/public/artlook/`). 다만 **실서버에는 여전히 장면 모드가 없다** —
    `deploy/render` 가 `9ed4713` 에 멈춰 있어 `scenes.json` 이 404 나고 목록이 비어
    옛 평면 합성으로 폴백한다. 배포하려면 `git checkout deploy/render && git merge main`.

44e. **ArtLook 매트는 없음 / 좁게(5%) / 넓게(10%)** (2026-09-01 — 8-31 의 '필수'를 되돌림).
    캔버스 랩만 예외(액자가 없어 매트를 받칠 것이 없다). 기본값은 좁게 그대로.
    ⚠️ **매트 비율은 `buildFramedCore` 맨 위 `matFrac` 한 곳에서만 정할 것.** 예전엔 사진 액자
    분기가 `o.matWidth` 를 직접 읽고 절차적 분기는 따로 계산해서, 규칙을 바꿨더니
    **사진 액자 5종(오크·블랙·화이트·월넛·골드)에서만 매트가 안 나왔다** — 눈으로 렌더를
    보고서야 알았다(문자열 테스트로는 안 잡힌다). **이게 44e 의 진짜 값이다** — '필수'가
    아니라 '한 곳에서만'. '없음'을 되살릴 때도 그 구조는 그대로 뒀다.
    ⚠️⚠️ **'없음'은 그냥 매트를 빼는 게 아니다 — 두 가지를 함께 꺼야 한다**
    (하니스 `scratchpad/vt/nomat.mjs`·`nomat.py`, 브리핑 NO-MAT SAFETY SYSTEM):
    - **자산의 밝은 안쪽 립을 상쇄한다.** 사진 액자 5종 전부 개구부 직전이 앞면보다 밝다
      (실측 배수: 검정 2.74 · 월넛 2.72 · 오크 1.50 · 골드 1.40 · 화이트 1.28).
      매트가 있으면 그 립이 **밝은 매트 옆**이라 이어져 안 보이는데, 매트를 빼면
      **어두운 작품에 직접** 닿아 [액자 → 흰 테두리 → 작품] 이 된다(44f 의 실패 조건 그 자체).
      실측: 작품 경계 임펄스가 검정 **40.2** · 월넛 **38.1** 로 뛴다.
      왜 밝은가 — 자산을 찍을 때 개구부에 **밝은 회색판**을 대서 그 반사광이 사면을 들었다.
      실물에서 그 자리에는 (대개 더 어두운) 작품이 있으므로 그 바운스가 없다. 즉 립이 밝은 건
      **촬영 세팅의 흔적**이지 그 액자의 성질이 아니다.
      ⚠️ 고치는 방향이 중요하다 — 브리핑대로 **테두리를 더하지 않고**, 물리적으로 없어야 할
      밝기를 **던다**(`LIP_TARGET` 0.85 = 파인 사면은 앞면보다 밝을 수 없다).
      `A.lip>1.02` 일 때만 걸어서, 이미 어두운 자산(오크 0.93·화이트 0.74·골드 0.94)에는
      **아무 일도 일어나지 않는다.** 반사광 회복 스톱(`rb*1.12`)도 함께 끈다 — 되돌아올
      매트가 없는데 그대로 두면 방금 덜어낸 립을 다시 밝힌다.
    - **플로터는 트레이 안쪽 폐색을 끈다.** 매트가 있으면 그 폐색은 트레이↔매트 이음매에,
      작품의 접지 그림자는 작품 경계에 앉아 **떨어져** 있다. 매트를 빼면 `ax=border+gap` 이라
      둘이 **겹쳐** 작품 둘레에 검은 선이 된다(샴페인 10.3 → **19.5** · 아이보리 14.6 → **23.5**).
      브리핑 ANTI-COMPOUNDING — 같은 깊이 신호를 두 겹 쌓지 않는다. 떠 있다는 신호는 접지가 만든다.
    ⚠️ **판정은 절대값이 아니라 차등으로.** 합성을 완전히 끄고 재도 골드 42.8 · 블랙 40.2 다 —
    골드의 42.8 은 그 액자의 **금장 비드**고 브리핑 7번이 보존하라고 한 재질이다.
    `nomat.py` 는 `syncheck.py` 와 같은 논리로 **합성이 더한 몫만** 본다(≤8).
    현재 18/18, 우리 합성의 기여 중앙 **−1.0**(= 사진에 있던 걸 덜어낸다).

44f. **ArtLook — 평가 기준이 바뀌었다: "아무것도 안 한 것처럼 자연스러운가"** (2026-09-01)
    ⚠️ **더 이상 '입체감이 더 강해졌나'로 판정하지 말 것.** 6차에서 물리 일관성을 맞췄더니
    오히려 "CG 합성 느낌이 강해졌다"는 지적이 왔다. 사용자가 든 실패 조건이 구체적이다 —
    **[액자 → 흰 테두리 → 검은 선 → 흰 매트 → 작품] 으로 층이 또렷이 보이는 순간 실패.**
    레이어가 몇 겹이든 눈에는 **하나의 실제 물체**로 보여야 한다.
    - ⚠️⚠️ **바닥값(`>=`) 임계는 "효과를 더 넣어라"로 민다.** 실제로 `rail_span` 은 바닥 20 을
      통과하면서 골든(7~54)의 **1.7배**(중앙 86)까지 부풀어 있었다. `run.py`·`mat.py` 의
      `rebate_ratio`·`dir_tb`·`grain_abs`·`dir_abs`·`bevel_abs` 를 **골든 범위 밴드**로 바꿨다.
    - ⚠️⚠️ **절대값으로는 "합성이 지배적인가"를 못 가른다.** `rail_span` 이 밴드를 넘길래
      음영을 더 깎으려 했는데, **합성을 완전히 끄고 재도 73.5 였다**(켜면 75.2) — 그 값은
      우리 음영이 아니라 **액자 사진 자체**의 대비였다(월넛 143·플로터 트레이 144).
      더 깎아 봐야 안 변하고 액자만 납작해진다. 그래서 `syncheck.py` 를 만들었다:
      **같은 입력으로 합성만 끈 렌더와 비교**해 ①합성 기여 ≤25% ②장면 광원 추종 증가 ≥8.
      현재 기여 0.9~20% · 추종 +15~32 (10/10). 하니스는 `SYN=0 SYNDIR=0 node render.mjs renders_off`.
    - ⚠️⚠️ **합성 세기를 한 노브로 일괄 조절하지 말 것.** 0.40 을 전부에 걸었더니 층은 줄었지만
      **장면 광원 추종까지 죽었다**(위살↔아래살 차이 골든 30~90 ↔ 19.8). 둘로 나눈다 —
      `SYN_DIR`(면의 방향광, 0.85: '층'을 만들지 않는다) / `SYN_EDGE`(아리스·매트 개구부 사면·
      리베이트·액자가 매트에 지는 그늘, **0.35**: 사용자가 본 테두리·선이 전부 여기서 나온다).
      ⚠️ 방향광을 **바깥 모따기에만** 싣지 말 것 — 살 폭의 18% 짜리 좁은 띠가 ±29% 로 흔들려
      액자 둘레의 밝은/어두운 **테두리**로 읽힌다. 살 전체에 걸리는 **균일 항**(`uni`)으로
      나눠 실으면 가로지르는 대비 없이 위↔아래 차이만 생긴다(실측 dir_tb 28 → 36.6).
    - ⚠️ **넓은 반그림자(penumbra)를 뺐다.** 방향을 줘도 결국 조각을 둘러싼 회색 헤일로로
      읽혔다. 남은 건 **접지(짧고 또렷) + 아주 옅은 투영** 둘뿐이고 **투영이 접지보다 약하다**.
    - ⚠️ **액자가 매트에 드리우는 그늘을 넓게 두지 말 것.** 매트 폭의 62%(실측 28px)를
      어둡게 칠해 [액자|검은 띠|매트] 로 층이 생겼다 → 20%. 리베이트도 **평평한 띠가 아니라
      좁고 깊은 V + 회복**이어야 한다(골든 case_01: 살 166 → 37 급락 → 62 회복). 골이 좁으면
      '층'이 아니라 두 면이 만나는 **이음매**로 읽힌다.
    - ⚠️ 매트 결 진폭 48 은 **체크무늬 천**이었다(주기 성분이 격자로 보인다). 골든 std 6.4~13.5
      에 맞추고 직조(주기) 성분을 0.26 → 0.10 으로 — 결은 랜덤이 주고 직조는 거들 뿐.
    - 전/후 비교는 `?syn=`·`?syndir=` 쿼리로 **같은 입력에서** 뽑는다(브리핑 "동시에 테스트").
    - 검증: `syncheck.py` 10/10 · `run.py` 8/10(문서화된 t03·t09) · `artpreserve.py` 10/10 ·
      그림자 방향 10/10 · 프론트 569 통과.

44g. **ArtLook 합성 세기는 상수가 아니라 측정값에서 나온다** (2026-09-01 8차, 하니스
    `scratchpad/vt/assetlight.mjs`·`scenelight.mjs`·`levels.mjs`·`ab.py`)
    브리핑: "모든 규칙은 **조건부**다. 자산에 이미 있으면 다시 만들지 말고(SOURCE-FIRST),
    확신이 없으면 덜 해라(CONFIDENCE-AWARE)." 7차까지 `SYN_DIR`·`SYN_EDGE` 는 **전역 상수
    하나**라 액자 18종 × 장면 15개에 같은 세기를 걸었다 — 그래서 노브를 돌릴 때마다 어떤
    조합은 좋아지고 어떤 조합은 나빠졌다(라운드마다 오락가락한 이유). 재서 유도한다.
    - ⚠️⚠️ **44번(=옛 37번)의 전제가 5종 전부에서 틀렸다.** "사진 액자는 스튜디오 확산광이라
      네 변이 똑같이 밝다"고 적혀 있지만, 원본 PNG 를 재면 위살−아래살이
      `black +27.8 · gold +15.8 · walnut +20.6`(위에서 온 빛)이고
      **`oak −17.3 · white −41.2` 는 아래에서 온 빛**이다. 그래서 우리 음영은 액자마다
      다른 일을 했다 — 앞의 셋은 **중복**(이미 있는데 또 +7~21), 뒤의 둘은 **싸우다 져서**
      (−41 → −12) 모순된 두 램프가 겹친 뿌연 중간을 남겼다(브리핑이 "incorrect adaptive
      lighting can be worse than doing almost nothing" 이라 한 그 상태).
      지금은 `measureAssetLight()` 가 로드 때 한 번 재고, `shadeFrameProfile` 이
      **모자란 만큼만** 채운다: 이미 충분하면 **아무것도 안 한다**(실측 add=0.0),
      어긋나면 **한 번에** 목표까지 옮긴다(상한 ±16%). 살 **단면**도 같다 — 자산의
      프로파일 낙차가 살 평균의 64~217% 인데 우리가 얹던 건 ±12% 였다. `modeled≈1` 이면
      단면을 통째로 끈다. → **"가장 낮은 단계를 고른다"가 액자별로 자동으로 일어난다.**
    - ⚠️⚠️ **밝히기를 `screen` 으로 하지 말 것 — 밝은 액자에서 헤드룸이 없어 뭉갠다.**
      화이트 위살(200)에 f=1.16 을 걸었더니 실제로는 **+3.8%** 밖에 안 올라 잔차 보정이
      무력했다. `applyRailGain` 이 `lighter` 로 **자기 자신을 alpha(f−1) 만큼 더해**
      정확한 곱셈을 만든다(투명한 개구부는 알파 0 이라 안전).
    - ⚠️⚠️ **곱연산 그늘색은 어두운 액자에서 '바닥'이 된다.** `rgba(28,22,16)` 는
      `base·(1−a)+22a` 라, 검은 살(41)에서는 22 가 절반이라 아무리 걸어도 안 어두워진다
      (폐색 19% 를 걸었는데 실제 낙차 6%). 깊은 폐색에는 **근사 검정**(`SHADE_OCCL`).
      순수 곱셈은 전 채널 같은 배율이라 **채도를 빼지 않는다**.
    - ⚠️ **장면 15개 중 6개는 광원 방향의 근거가 없다** — 벽 낙차 실측 0.0~4.5(흰 벽돌은
      정확히 0.0)인데 전부 손으로 적은 `[-1,-1]` 이었다. `sceneLightModel(scene)` 이
      낙차를 재서 신뢰도를 만들고 **가로 성분만** 줄인다(세로=위에서, 는 실내의 보편 가정).
      `composeScene` 과 `compose()` 둘 다 이 모델을 거친다 — 날것의 `scene.lightDir` 금지.
      ⚠️ 가로를 **0 까지 죽이지는 말 것** — 완벽한 좌우 대칭 자체가 CG 신호다(캔버스 랩
      왼쪽 옆면이 0.91→0.79 로 떨어져 세로 검은 띠로 보였다). 40% 를 바닥으로 남긴다.
    - ⚠️ **방향 = 신뢰도, 세기 = 물리.** 그림자 알파까지 신뢰도로 깎아 봤다가 되돌렸다
      (`contact_drop` 이 전 케이스에서 내려갔다). 근거 없는 방향 문제는 방향 보정이 이미 푼다.
    - ⚠️ **리베이트의 방향 성분과 폐색 성분을 갈라 놓을 것.** 방향은 자산에 이미 있으므로
      `P` 로 끄고, **폐색은 자산에 없으므로**(개구부에 밝은 회색판을 대고 찍어 사면이
      들렸다) `P` 와 무관하게 건다. 안 그러면 빛 받는 변에서 사면이 통째로 사라진다
      — 실측 변별 `rebate_ratio` 위 0.46~0.59 ↔ **아래 0.00~0.10**(골든은 네 변 0.63~0.89).
    - **폴백 사다리 `?level=0..3`** — 0 원본만 / 1 최소 장면 통합 / 2 기하 / 3 방향광(기본).
      `node levels.mjs` 로 네 장을 한 번에 뽑는다. 디버그 훅은 `lightModel`(쓴 광원·신뢰도·
      자산 실측치)도 돌려준다.
    - ⚠️ **되돌린 것 셋** — ①매트 사면을 물리 치수로 좁히기(`bevel_abs` 가 골든에서 더
      멀어졌다. 넓은 코어가 매트와 작품 사이의 완충이다) ②폐색 램프 넓히기(`gap_pct` 가
      밴드를 벗어난다 = 사용자가 실패라 한 방향. 자산의 골이 0.70~0.80 에 있으니 거기서
      시작한다) ③그림자 세기 신뢰도 게이팅. **되돌린 이유를 지우지 말 것** — 셋 다
      '이론적으로 옳아 보이는' 변경이었다.
    - 검증(7차→8차): `syncheck` 합성 기여 0.9~20% → **0.0~17.7%**(10/10) · 그림자 방향
      최소 0.774 → **0.865**(10/10) · aniso 중앙 4.71 → **6.03** · 매트 방향광 8/9 → **9/9** ·
      `run.py` 8/10 동률 · `artpreserve` 10/10 · 프론트 **580 통과**.

44h. **매트 없는 액자의 깊이는 '리베이트(안쪽 사면) 프로파일' 하나가 다 만든다** (2026-09-01 9차,
    하니스 `scratchpad/vt/matrix.mjs`·`sheet.py`·`profile.py`)
    브리핑: "NO-MAT ↑ · WITH-MAT = 또는 ↑. 가짜 매트 띠를 넣지 말고 **액자 자신의 몰딩·리베이트·
    폐색**에서 깊이를 얻어라." 8차까지의 지표가 다 통과하는데도 매트 없는 렌더가 납작했다.
    - ⚠️⚠️ **지표 둘이 이 결함을 구조적으로 못 봤다.** ①`nomat.py` 의 임펄스는 중앙값 필터(size 5)
      잔차라 **3px 이상 넓은 띠를 못 본다** ②`rebate_ratio` 는 골의 **최솟값만** 본다.
      그런데 눈에 '층'으로 보이는 건 골 다음에 **되올라오는 폭 4~6px 의 띠**다.
      그래서 8라운드 동안 살아남았다. 4배 확대 시트(`sheet.py`)를 눈으로 보고서야 찾았다.
    - **골든 5장(`cases/case_0*/frameit_reference.png`)은 전부 매트가 없다** — 즉 이 구성의
      정답지가 이미 있었는데 아무도 단면을 재 본 적이 없었다. 재면 한결같다:
      앞면(평탄) → 3~5px 에 **깊은 골 0.15~0.40** → 3~5px 에 **회복 0.43~0.63** → 작품.
      우리는 골 0.58~0.67(오크·골드)로 얕고, 끝이 0.83~**1.54**(검정)였다.
      **1.54 = 앞면보다 밝다** — 파인 자리가 노출면보다 밝을 수 없으므로 물리적으로 불가능하고,
      화면에서는 검은 액자와 작품 사이의 **회색 줄**로 보인다.
    - ⚠️⚠️ **자산의 밝은 립을 스칼라 하나로 누르지 말 것 — 모양이 그대로 남는다.**
      8차의 `LIP_TARGET` 은 리베이트 구간 전체에 같은 배율을 걸었다. 검정 자산은 3.62배까지
      **올라가는 램프**라 0.65 를 곱해도 1.54배가 남는다. 위치마다 다른 값을 상쇄해야 하므로
      **자산의 단면 모양**을 재서(`prof4`) 그 위치의 값으로 나눈다.
      판정식은 `f(d) = min(1, 목표(d) / 자산(d))` — **한 방향으로만** 작동한다.
      결과는 `min(자산, 목표)` 라, 이미 더 깊은 골이 있으면 살리고(SOURCE-FIRST) 밝아지는
      구간만 끌어내린다. 그래서 오크(1.02배)·골드(1.17배)에는 **거의 아무 일도 안 일어난다**.
    - ⚠️ **네 변을 평균하지 말 것.** 골드의 봉우리는 **왼쪽 살에만** 있어 평균에서 묻혔고,
      검정은 왼쪽이 평균보다 밝아 보정이 약해졌다(골 0.30 목표 → 0.51). 변별로 잰다.
    - ⚠️ **곡선을 `FACE_FRONT+e` 에서 시작하지 말 것.** `e` 는 최대 0.05 라 d 0.74~0.79 가
      무주공산이 되는데, 골드의 봉우리가 정확히 거기 있었다(렌더에 1.16배가 그대로 남았다).
      종전 코드는 d0 에서 0.853 으로 시작해 **우연히** 덮고 있었다.
    - ⚠️ **얇은 액자에 같은 깊이를 주지 말 것 — 절벽이 된다.** 화이트 매트(살 15px)는 리베이트
      구간이 4px 뿐이라 233→70 이 **검은 선**이 됐다(임펄스 6.3 → 44.8). 구간 폭(px)으로
      깊이를 줄인다 — 몇 픽셀 안에 표현 못 할 깊이는 **안 그리는 게 낫다**.
    - ⚠️⚠️ **한 밴드에 프로파일 모델을 둘 두지 말 것.** 절차적(슬림) 액자는 `planeShade` 가
      앞면 끝을 **0.80**(PLANE_OUT+PLANE_FRONT)으로 선언하는데 사진 액자 상수(0.74)를 갖다 쓰면
      리베이트가 앞면 한가운데서 시작해 그 경계에 봉우리가 남는다(+37.5).
      게다가 `planeShade` 는 아랫변 사면을 **밝히고**(+.44) 리베이트는 폐색이라 **어둡게** 한다 —
      정반대다. 물리적으로는 리베이트가 옳으므로(파인 홈은 폐색이 지배, 라운드 8과 같은 결론)
      `rebOwn` 으로 **사면을 통째로 넘긴다**. 앞면↔사면 아리스도 같이 넘긴다(같은 모서리를
      두 번 긋지 않는다). 넘긴 뒤 임펄스 33~37 → **2~7**.
    - ⚠️ **소수점 좌표 액자는 곱연산을 이음매 너머까지 끌 것.** 절차적 액자는 `border` 가 소수라
      살의 마지막 픽셀이 폴리곤에 **절반만** 덮여 절반의 알파만 받는다 → 그 한 줄만 덜 어두워져
      **밝은 실선**이 된다(실측 오크 슬림 `80 81 89 **115** | 74`). 6% 더 끌고 그 구간에서
      배율을 1 로 되돌린다(사진 액자는 `l` 이 정수라 이 문제가 없어 여태 안 드러났다).
    - ⚠️⚠️ **지표의 한계값을 상상해서 적지 말 것 — 레퍼런스로 재라.** 둘 다 틀려 있었다:
      · `syncheck` 이 쓰던 `rail_span` 은 **살 전체**(리베이트 포함)인데 골든 쪽은 `golden.py` 가
        살을 **앞면만** 손으로 재서 넣는다(case_00: rail 16 / gap 36). 애초에 다른 구간을 재고
        있었다(우리 값이 늘 골든의 1.7배였던 이유). 리베이트를 옳게 파낼수록 '합성 과다'로
        잡혔다(+158%). **앞면만** 재는 `face_span` 을 따로 만들어 그걸로 묻는다 — 그 값으로는
        우리 합성이 앞면 대비를 **오히려 줄인다**(t08 −26% · t10 −7%). 재질을 안 덮어쓴다는 뜻이다.
      · `nomat.py` 의 "합성이 더한 임펄스 ≤8" 은 **골든 사진도 전부 탈락**시킨다 — 같은 코드로
        재면 실제 사진이 밝은 1.5~27.4 · 어두운 12.4~24.7 이다(매트 없는 액자의 안쪽 턱은 원래
        또렷한 어두운 전이다). 그 잣대로 튜닝하면 리베이트를 지워야 통과한다 — 실제로 세 번
        헛돌았다(33→21→33). 지금은 **골든을 같은 표에 찍고** 그 범위로 판정한다.
    - 🔒 **매트 있는 렌더는 픽셀 하나도 안 바뀐다**(실측: 전 케이스 최대 차이 0).
      매트 쪽은 이미 합격 판정을 받은 골든이라 이번 개선에서 건드리지 않았다 — 절대 프로파일은
      `absReb = noMat && SYN_EDGE>0` 로만 켜지고, 반사광 회복(1.12)은 매트 분기에만 남는다.
      실버 플로터 무매트(브리핑이 지목한 **성공 레퍼런스**)도 픽셀 동일하다.
    - 검증(8차→9차): 매트 없는 사진 액자 **끝 밝기 0.83~1.54 → 0.52~0.60**(골든 0.43~0.63) ·
      **앞면보다 밝은 픽셀 0px**(종전 최대 5px) · 슬림 액자 골 0.80 → **0.34** ·
      `rebate_ratio` 무매트 0.30~0.39 → **0.60~0.63** · `nomat` **18/18**(골든 기준 재보정) ·
      `run.py` 8/10 동률(문서화된 t03·t09) · `syncheck` 8/10(t02·t10 은 **기준선과 소수점까지
      동일** = 이번 변경과 무관) · `artpreserve` 10/10 · 그림자 방향 10/10 · 프론트 **587 통과**.
    - ⚠️ 하지 **않은** 것: 작품 가장자리의 접지 그림자를 키우지 않았다. 골든의 그 값은
      0.82~0.92(1~2px)로 우리와 차이가 거의 없고, 깊이 신호는 이제 리베이트가 만든다 —
      같은 성질을 두 기전으로 말하는 건 브리핑 ANTI-COMPOUNDING 위반이다.

44i. **'선으로 보인다'를 가르는 건 세기가 아니라 폭이다** (2026-09-01 10차, 하니스 `scratchpad/vt/edge.py`)
    사용자 지적: *"FRAME → INNER RECESS → ARTWORK 전이가 일부 렌더에서 아직 얇은 그래픽 선으로
    읽힌다. 더 진한 테두리·그림자·매트 흉내로 풀지 말고 **물리 해석**을 고쳐라. 지금 자연스러운
    실버/어두운/밝은 무매트는 **보호 대상**이다."*
    `edge.py` 로 골든과 우리를 같은 코드로 재서 셋을 비교했다 — `band`(골↔끝 차이) ·
    `cross`(작품 첫 픽셀 ÷ 본문) · `width`(앞면의 90% 아래로 내려간 폭, px).
    - ⚠️ **작품 쪽 그림자를 더하지 말 것 — 이미 골든 범위다.** `cross` 골든 0.76~1.06(중앙 0.95),
      우리 0.95~0.99(중앙 0.97). "폐색이 경계에서 끊긴다"는 가설은 **측정으로 기각**됐다.
      (9차에서 같은 결론을 냈고 10차에서 재확인 — 다시 시도하지 말 것)
    - ⚠️⚠️ **진짜 원인은 `width`.** 약한 케이스는 전부 **조각이 작게 그려지는 렌더**였다 —
      실내 장면(`maxLong` 0.49)·얇은 액자. 오크는 살 44px 라 리베이트가 10px 인데,
      갤러리 살롱의 같은 오크는 살 **20px**·리베이트 **4~5px** 다. 그 4px 에 온전한 깊이의
      V 를 넣으니 '파인 자리'가 아니라 **그어 놓은 선**이 된다.
    - ⚠️⚠️ **해상도 판정을 판(plate) 좌표로 하지 말 것.** `kd` 가 여태 판 픽셀을 보고 있었는데,
      판은 작품 원본 크기라 **늘 넉넉했다** — 즉 그 게이트는 한 번도 작동한 적이 없다.
      `compose()` 가 출력 크기를 **판을 만들기 전에** 구해 `state.outLongEst` 로 넘긴다
      (조각 긴 변 ≈ 출력 긴 변 × 그 장면의 `maxLong`. 자동 프레이밍이 거의 늘 천장에 붙어
      실측 오차가 작다 — 오크 추정 44 ↔ 실제 44. 판을 두 번 만들지 않는다).
    - ⚠️⚠️ **못 그릴 만큼 좁으면 '없음'으로 수렴시킬 것.** 예전엔 골만 `REB_TIP` 으로 붙여서
      좁아지면 골=끝인 **평평한 어두운 띠**가 남았다(실측 band 0.01~0.07, 골든 0.28) —
      평평한 띠가 정확히 '그어 놓은 선'이다. 양 끝(`trough`·`tip`)을 **함께 1 로** 보내면
      V 모양은 지킨 채 세기만 준다. 골든이 그 증거다 — 살 8px 짜리 case_06 은 **실제 사진인데도**
      골이 0.87(=거의 없음)이다.
    - ⚠️⚠️ **회복(골→개구부의 되밝아짐)에는 별도의 게이트가 필요하다.** 회복 자체는 물리적으로
      맞지만(골든 case_00 0.23 → 0.49) **2~3px 안에** 그리면 개구부 직전의 **밝은 링**이 된다.
      실측: 오크는 회복 구간 4.3px 라 자연스럽고, 얇은 실버는 2.6px 라 같은 비율(1.8배)인데도
      임펄스 44.6(골든 최대 27.4)이었다.
      ⚠️ **자산의 밝은 립 탓이 아니다** — 얇은 실버 자산은 개구부 직전이 0.94~1.05 로 **깨끗한데도**
        실패했고, 반대로 검정(2.6~3.7배)·월넛 자산은 살이 넓어 **통과한다**. 원인은 밝기가 아니라 폭이다.
    - ⚠️ 마디를 **직선으로 잇지 말 것** — 리베이트는 두 평면이 만나는 각이 아니라 **오목한 곡면**이다.
      꺾인 자리는 축소될 때 대비가 몰려 가장자리가 뚜렷한 띠로 보인다. smoothstep 으로 잇는다.
    - ⚠️ **되돌린 것: 자산 개구부의 '촬영판 잘라내기'.** 새 얇은 액자는 개구부를 **흰 판**으로 찍어
      마지막 5px 이 앞면의 1.01·1.25·**3.39배**까지 오른다. 자산에서 깎으려 했으나 판정 기준
      (`L ≥ 앞면`)이 **기존 5종의 문서화된 립**(검정 3.68·월넛 3.00배)과 구분되지 않아 골드를
      3px 깎아 버렸다 — git 복원. 렌더 시점의 한쪽 방향 보정(44h)이 이미 레벨을 잡고 있으므로
      **자산은 건드리지 않는다.**
    - 🔒 **보호 대상은 픽셀 단위로 지켰다**: 매트 있는 전 케이스 · 실버 플로터 무매트(사용자가
      지목한 레퍼런스) · 캔버스 랩 = **0px 변화**. 자연스럽던 큰 사진 액자 5종은 최대 4~14레벨
      (전체의 0.2~1.0%, smoothstep 때문) — 눈으로 구분되지 않는다.
    - 검증(9차→10차): 얇은 오크 임펄스 32.7 → **17.8** · 샴페인 슬림 28.0 → **20.3** ·
      실버 슬림 26.3 → **13.6** · 화이트 매트 25.3 → **4.6** · `nomat` **21/21**(주의 1) ·
      `run.py` 8/10 동률 · `artpreserve` 10/10 · 그림자 방향 10/10 · `syncheck` 8/10 동률 ·
      프론트 **590 통과**.

44j. **추가 자산 — 얇은 사진 액자 3종 · 실내 장면 1 · 배경 [벽]/[공간] 탭** (2026-09-01)
    - **얇은 오크·월넛·실버**(`oak-thin`·`walnut-thin`·`silver-thin`). native 살이 5.0~5.7% 로
      기존 5종(9.0~9.8%)의 절반이라 **같은 재질의 다른 제품**이다. 그래서 추출기에 한정어
      (`QUALS`)를 넣어 `oak-thin` 처럼 키를 나눈다 — 안 그러면 `oak` 를 덮어쓴다.
      ⚠️ **추출기를 다시 돌리면 `gptsamplecase/` 에 원본이 없는 액자가 `frames.json` 에서
        사라진다**(실제로 `walnut` 이 그랬다 — 파일은 남고 메타만 증발). 재추출 후에는
        `git checkout frames/photo/` 로 되돌리고 **새 키만 병합**한 뒤 `symmetrize` 를 돌릴 것.
      ⚠️ `FRAME_GRADE` 는 자산마다 **재서** 넣는다(살 채도폭 실측 oak-thin 49.6 · walnut-thin
        34.2 · silver-thin 4.9 → 각각 0.55 / 0.72 / 보정 없음). 실버는 이미 무채색이라 손대지 않는다.
    - **콰이어트 스터디**(`walls/wall18-quiet-study.jpg`). ⚠️ 원본이 **382×332** 뿐이라 3배
      확대해 넣었다 — 벽이 매끈한 회벽(고주파 std 0.84)이라 확대해도 잃는 게 없음을 재고 결정했다.
      다만 그래서 **벽 결이 0.44 로 목표 밴드(2.5~4.5) 아래**다 → 배경이 다른 장면보다 평평하게
      읽힌다. `wallCalm:0`(흐릴 것이 없다). **더 큰 원본이 생기면 교체할 것.**
      `lightDir` 은 손으로 적지 않고 쟀다(rule 39): 좌−우 −3.4(오른쪽이 밝다) · 위−아래 −29.3
      이지만 **세로는 항상 위**로 둔다(바닥 반사를 광원으로 읽으면 그림자가 위로 진다).
    - **리넨 리빙 · 클레이 살롱**(`frameit_test_assets.zip` 의 room_01·room_02, 2026-09-01).
      우리 장면 중 **방향광 근거가 가장 뚜렷하다** — 영역만 재면 좌−우 +18.4 · +33.5 로,
      대부분의 장면(<4 = 근거 없음)과 다르다. 둘 다 빛이 **왼쪽 창**에서 온다.
      ⚠️⚠️ **광원 기울기를 이미지 전체로 재지 말 것 — 부호가 뒤집힌다.** 리넨 리빙은 왼쪽에
        어두운 화분이 있어 전체로 재면 −10.7(오른쪽이 밝다)로 나오지만, 실제 벽 영역만 재면
        **+18.4**(왼쪽이 밝다)다. 반드시 `region` 안에서 잴 것.
      ⚠️ 클레이 살롱은 결 1.81 로 밴드 아래 → `wallCalm:0`. 흐릴 것이 없다.
    - ⛔ **같은 zip 에서 쓰지 않은 것과 이유** (다시 넣지 말 것):
      · `frame_01_oak`·`frame_02_walnut`·`frame_03_silver` — 앞서 넣은 1024px 판과 **같은 액자**를
        271px 로 담은 것이다(살 5.5 ↔ 5.7% · 5.2 ↔ 5.5%). 9-slice 가 3.7배 늘려야 해서 손해만 본다.
      · `wall_01/02/03` — 결이 1.07~1.37 로 목표 밴드(2.5~4.5)의 **3분의 1**이다. 색(웜화이트·
        베이지·그레이)도 기존 8종에 이미 있다. 늘리면 목록만 길어지고 배경은 더 평평해진다.
      · `room_03` — **이미 작품이 걸려 있는** 버전이다. 우리가 쓰는 건 `_updated` zip 의
        `room_03_no_artwork`(= 콰이어트 스터디).
    - **배경 탭**: `scenes.json` 의 각 장면에 `group:'wall'|'space'`. 평면 매크로 벽 8 · 실내 8.
      고르는 기준도 결과 인상도 다르고(실내는 `maxLong` 때문에 훨씬 작게 걸린다) 한 줄에 16개는
      너무 많다. ⚠️ `state.sceneIdx` 는 **전체 목록 인덱스**를 유지할 것 — 걸러진 목록의 인덱스로
      바꾸면 탭을 옮길 때마다 가리키는 장면이 달라진다. 처음 열 때는 **지금 고른 장면이 속한 탭**을
      편다(활성 칩이 안 보이는 탭에서 시작하면 아무것도 안 고른 것처럼 보인다).
      ⚠️ 회귀 테스트는 파일명이 아니라 **`group` 으로** 실내를 가른다 — 정규식(`wall1[1-7]`)이었을 때
      새 장면 `wall18` 이 조용히 검사 밖으로 빠졌다.

44k. **어두운 벽·색 있는 벽 6종 추가 + 크기가 거짓말하던 두 곳** (2026-09-03)
    벽 6종(`wall21~26`: 차콜·올리브·월넛슬랫·테라코타·블루그레이·화이트플라스터)을 넣고,
    실내 장면 3종(quiet-study·linen-living·clay-salon)은 사용자 요청으로 뺐다(원본 382px 화질 부채).
    - **1254px 원본을 ×2 로 늘려 넣는다.** 같은 표시 크기에서 벽 결 손실이 **0.0%** 인데
      (실측: 두 경로 모두 최종적으로 원본을 1.15배로 보여 준다) 출력이 1080→**1568px**,
      자동 프레이밍 확대 한도가 1.34→**1.84배**가 된다. 규칙 41 이 말한 그 여유다.
    - ⚠️ **월넛 슬랫은 `wallLod:9` 를 박아 둘 것.** 세로 슬랫이 폭 200px 짜리 주기 무늬라
      자동 LOD(≈11텍셀)와 **주기가 겹쳐 앨리어싱**한다 → 단색 작품에 줄무늬가 그대로 비쳤다
      (규칙 40 의 재현: 작품 무늬 **3.41**, 평면 벽은 0.39~0.46). LOD 를 올리면
      슬랫이 평균으로 뭉개져 **1.49** 로 떨어지고 조명 낙차는 13.3→10.6 으로 남는다.
      새 벽에 규칙적 무늬(슬랫·타일·벽돌)가 있으면 `leak.mjs` 에 **반드시 태울 것**.
    - ⚠️ **`contact_drop`(접지 그림자) 임계 20~85 는 밝은 벽에서만 성립한다 — 통과시키려고
      그림자를 진하게 만들지 말 것.** 절대 밝기 낙차라 벽이 어두우면 구조적으로 못 넘는다
      (차콜 벽 87 · 낙차 7.7). **비율은 정상**이다 — 벽의 세로 기울기를 나눠 정규화하면
      새 벽 −14~−21% ↔ 기존 −15~−29% 로 겹친다. 진하게 만들면 규칙 37 이 금지한
      '오려 붙인 티'로 곧장 되돌아간다. `sweepcheck scenes` 의 이 실패는 **문서화된 예외**다.
    - ⚠️ `lightdir.py --write` 는 **전 장면을 덮어쓴다.** 그 추정기는 region **바깥 띠**를
      재는데 실내 사진은 그 띠에 화분·가구가 들어와 부호가 뒤집힌다(규칙 44j 가 리넨 리빙
      실측으로 적어 둔 그대로). 새 장면을 넣고 돌린 뒤 **기존 장면의 diff 를 반드시 확인**할 것.
    - ⚠️⚠️ **크기 무릎(KNEE/FLOOR)을 '화면 절대 면적'으로 두지 말 것 — 실내에서 전 구간을 삼킨다.**
      실내는 `maxLong` 0.49 라 조각이 화면 면적의 24% 를 못 넘는데 KNEE 가 11% 였다. 즉
      12cm 소품부터 100호까지 **전부** 보정 구간에 들어가 실치수가 절반으로 압축됐다 —
      실측 컬렉터 살롱 **12×12cm 27.1% ↔ 130×162cm 38.2%**(13.5배 차이가 화면에서 1.41배).
      평면 벽은 2.3배로 멀쩡했다. **방에는 소파·콘솔이 함께 찍혀 크기 기준이 눈에 보이므로
      그 압축이 평면 벽과 달리 즉시 '너무 크다'로 읽힌다**(2026-09-03 신고).
      `maxLong²` 에 비례시켜 무릎이 장면마다 같은 **상대 위치**에 오게 했다 → 19.3% ↔ 38.2%(1.98배).
      평면 벽은 계수 1 이라 **픽셀 단위로 그대로**다.
    - ⚠️⚠️ **치수를 모른다고 '최대치'로 걸지 말 것.** 예전엔 `fill`(0.62)에 자동 프레이밍
      `gain` 까지 걸려 화면 상한까지 커졌고, 그 결과 **크기를 모르는 작품이 100호보다 크게**
      걸렸다(흰 벽돌 70.0% ↔ 100호 65.7% · 컬렉터 살롱 49.0% ↔ 38.2%). 모르는 값을 최댓값에
      놓는 셈이라 앞뒤가 안 맞는데, 실서버 작품의 **97% 가 이 경로**다.
      지금은 **30호(높이 90cm)로 가정**하고 실치수와 같은 경로를 태운다(`ASSUMED_CM`) —
      `regionCm` 이 장면마다 다르므로 그 방에 맞는 크기가 자동으로 나온다(58.5% / 32.7%,
      둘 다 30호 바로 아래). `trueScale` 로는 치지 않는다(안내문이 거짓말하면 안 된다) —
      대신 화면이 **"크기를 몰라 30호로 걸었습니다"** 라고 밝힌다.
      ⚠️ `gain` 에서도 빼야 한다. 안 빼면 가정이 무의미해지고 다시 상한까지 커진다.
    - ⚠️ 이 크기 변경으로 **골든 스위트 10케이스가 전부 움직인다**(`render.mjs` 가
      `selectedWork=null` 로 돌려 미입력 경로를 타기 때문). `run.py` 8/10 → 7/10:
      새 실패 t07 은 **캔버스 랩 경계 오측**이라는 문서화된 위양성이고(2026-08-30 에도
      40.4 로 나왔다가 '결함 없음' 판정), 실제로 확대해 보면 어두운 띠는 작품 자신의 어두운
      가장자리가 감긴 것이다(반대편 밝은 모서리는 밝은 띠). t03 은 rebate 로 옮겨 갔는데
      조각이 작아져 살이 얇아지면 리베이트를 죽이는 게 **규칙 44i 가 의도한 동작**이다.
      `contact_drop`·`recover_pct` 는 오히려 9/10 → **10/10** 으로 좋아졌다.
      나머지는 전부 기준선 동률: `artpreserve` 10/10 · 그림자 방향 10/10 · `syncheck` 8/10 ·
      `nomat` 21/21 · 프론트 **590 통과**.

45. **포트폴리오 PDF 는 '템플릿 채우기'가 아니라 '편집'이다** (2026-09-04, 상세는 `architecture.md`
    「포트폴리오 아트디렉션 층」, 채점은 `scratchpad/pf/score.mjs`)
    엔진(표지 15종·작품 7종·색·글꼴·판형)은 잘 만들어져 있었는데 **편집을 하지 않았다** —
    페이지 구성의 전부가 `chunk(images, worksPerPage)` 한 줄, 즉 작품 수를 상수로 나눈 것이었다.
    - ⚠️⚠️ **엔진이 작품의 모양을 몰랐다.** `PortfolioImage` 에 폭·높이가 없어 배치 근거가
      "몇 점인가" 하나뿐이었다. 칸은 늘 같은 크기고 작품은 그 안에서 contain 돼 **빈 자리가 남았다**
      (채움률 67~85%). 회색 패널이 그 빈 자리를 그려 "비어 있다"가 눈에 보였다 — 그게
      "템플릿을 채웠다"는 인상의 정체다. 지금은 `lib/artworkAnalysis.ts` 가 비율을 재고
      **정렬 격자**(한 행 같은 높이, 폭은 비율만큼)로 놓는다 → 채움 **99.9%**.
    - ⚠️ **배치에 쓰는 비율은 실치수가 아니라 사진이다.** 지면에 놓이는 건 사진이라 실치수를
      먼저 믿으면 사진이 상자 안에서 다시 레터박스돼 회색 여백이 생긴다. 실치수는 크기 위계(`areaCm2`)에만.
    - ⚠️ 측정은 화면이 하고 결과만 `PortfolioBookData.aspects` 로 넘긴다 —
      `buildPortfolioPages` 는 **순수 동기 함수여야** 미리보기와 PDF 가 어긋나지 않는다.
      비율을 몰라도 문서는 그대로 나온다(전부 정사각 = 옛 동작).
    - ⚠️ **시리즈마다 따로 자르면 꼬리가 텅 빈다** — 7점@6 = 6+1 이고 남은 1점이 6칸 격자의
      한 칸 크기로 혼자 한 장을 먹었다(실측 17장 중 3장이 지면점유 5%). `balancedSplit` 으로 4+3.
      다만 남는 게 정원의 60% 이상이면 꽉 찬 페이지를 지킨다(11@6 → 6+5).
    - ⚠️ **자동 편집(`design.auto`)의 밀도는 전체 작품 수로 정한다.** 시리즈만 보면 27점을
      5시리즈로 나눈 작가가 "시리즈마다 6점이니 넉넉하게"가 되어 **27장이 전부 같은 구성**이 된다(실측 20장 연속).
    - ⚠️ **격자 밀도를 여러 개 섞지 말 것** — 2점→4점→6점 교대는 리듬이 아니라 '일관성 없음'이다.
      변주는 `feature`(1점 크게+2점 작게)와 **그 지면에서 가장 좋은 격자 하나**의 교대가 만든다.
    - ⚠️ **배열(열×행)은 판형만 보고 정하면 안 된다** — 같은 '2점씩'이 A4세로 21% / A4가로 45% 다.
      행이 늘면 캡션 자리를 **행 수만큼** 무는데 그 비용을 아무도 안 재고 있었다. `solveGrid` 가
      후보를 만들어 재고, **렌더와 같은 함수**를 자동 편집도 부른다(판단과 결과가 어긋나지 않게).
      ⚠️ 점수를 **면적만으로** 두지 말 것 — 2열×1행이 3% 넓다고 뽑혔는데 결과는 아래 절반이 통째로 빈 페이지였다.
      ⚠️ 배열은 **정원 기준으로 한 번만** 푼다(장마다 풀면 쪽마다 작품 크기가 달라진다).
    - ⚠️ **캡션 예약은 그 장의 작품이 아니라 전체 작품에서 뽑는다** — 장마다 잡으면 캡션 긴 작품이
      있는 장만 칸이 작아진다(실측 272 ↔ 294px). 4점 이상 격자는 보조 정보를 **한 줄**로 접을 것
      (세 줄이면 2행에서 A4가로 본문의 32%). 6점 목록은 제목도 한 줄로 자른다.
    - ⚠️ **2점이 남았을 때 '2점씩'은 지면을 못 채운다** — A4세로에 정사각 둘을 쌓으면 각 점이
      폭의 44% 밖에 못 쓰고 좌우가 빈다(27%). '크게+작게'로 놓으면 32% 이고 의도가 보인다.
      사용자가 '2점씩'을 **직접** 골랐으면 그대로 존중한다.
    - ⚠️ **저장된 설정이 있으면 `auto` 를 켜지 말 것**(`normalizePdfDesign`) — 이미 배치를 고른
      사람에게 다른 배치가 나오면 "내가 만든 게 사라졌다"가 된다. 새 사용자만 자동이 기본.
    - ⚠️ **새 배치를 만들 때 캡션 줄바꿈을 세지 않으면 조용히 잘린다** — `titleLines` 는
      "제목 줄을 그리는가"이지 "몇 줄이 되는가"가 아니다. `feature` 의 보조 칸이 그 실수로
      17px 을 잘라먹었고 **채점 하니스가 잡았다**(눈으로는 못 본다).
    - ⚠️⚠️ **지표가 멀쩡한 디자인을 실패로 찍으면 없느니만 못하다.** 채점기를 만들며 지표 버그
      넷을 고쳤다 — ①여백 비대칭을 전 페이지에서 재면 러닝 머리말 때문에 세로 중앙 정렬 페이지가
      전부 최악으로 나온다(작품 장만·본문 상자 기준) ②위계를 크기 편차로만 재면 1~6점 작가에게
      "작품을 더 그려라"가 된다 ③'빈 장'을 여백 합계로 재면 연락처가 걸린다(내용 높이 20% 미만으로)
      ④칸 높이 편차는 **의도**(파노라마가 든 행은 낮다)와 사고를 못 가려 폐기하고 단위 테스트로 옮겼다.
    - **임계값은 레퍼런스에서 나온다** — 지면점유는 실제 작가 포트폴리오 5종 실측
      (`golden2.py`: 1점 42.2% / 2점 48.7% / 3점 49.9% / 4점 56.1%). **6점/쪽 골든은 n=5 에
      116.5% 라 못 믿는다**(양면 스프레드가 한 장으로 잡힌 값) — 4점 값을 쓴다.
    - ⚠️ **표지 한 줄 소개는 없앴다**(2026-09-04 사용자 요청). 실서버 작가 81명 중 채운 사람이 **0명**이라
      자리만 차지하고 구성을 흔들었다(`coverBandTop` 하단 28.7% 공백의 원인이 이거였다).
      `PdfDesign.coverTagline`/`coverTaglineText`·`COVER_SHOWS_TAGLINE` 전부 제거. `Portfolio.tagline` 자체는
      공개 홈페이지가 쓰므로 **남겨 둔다** — 표지에만 안 싣는다.
    - ⚠️⚠️ **여러 점을 놓을 땐 세로 기준선(칼럼)에 맞출 것.** 정렬 격자를 처음 넣었을 때 한 행의 작품을
      폭만큼만 붙여 놓고 **행 전체를 가운데 정렬**했는데, 행마다 비율 합이 달라 이음매와 좌우 끝이
      장마다 다른 x 에 와서 **삐뚤빼뚤해 보였다**(사용자 지적). 지금은 행을 **같은 폭의 칸**으로 나누고
      작품을 칸 안에서 가운데 둔다. 칸 폭은 `cols` 가 아니라 **그 행의 점수**로 나눈다 —
      2점만 남은 4점 격자에서 남은 칸을 비워 두면 파노라마가 칸 하나에 갇혀 반쪽이 된다(실측 396 ↔ 836).
      좌우 대칭이라 중앙선은 그대로 맞고, 높이는 여전히 기준 H 를 넘지 않아 마지막 한 점이 혼자 커지지 않는다.
    - ⚠️ **배치 목업의 작품 수는 그 배치 정원의 배수로 줄 것**(`worksMocks`). 6장 고정으로 뒀더니
      균형 분할(6@4 → [3,3]) 때문에 '4점씩' 목업이 **3점으로 그려져 픽토그램이 거짓말을 했다**(사용자 지적).
    - 결과(마은영 27점): 37쪽 → **18쪽** · 채움 67~85% → **99.9%** · 크기편차 0.14 → **0.74** ·
      같은 구성 최대 연속 27 → **2** · 거의 빈 장 5 → **0** · 채점 **94.4/100** · 잘림 0 유지.
      합성 검증 40조합 평균 **92.0점**. 회귀는 `portfolioArtDirection.test.ts`(28개)+`score.mjs`.

46. **토글은 '확인하고 만들지' 말 것 — 연타에 에러가 난다** (2026-09-04, 하니스 `e2e/tests/39-load-community-story.spec.ts`)
    좋아요·이웃 추가가 `findUnique` 로 있나 본 뒤 `create`/`delete` 했는데, 그 확인이 **트랜잭션 밖**이라
    동시에 두 번 누르면 둘 다 "없음"을 보고 들어가 unique 위반으로 떨어졌다.
    실측 동시 10회에 **200 1건 + 400 9건**(`{"error":"데이터 처리 중 오류가 발생했습니다."}`).
    - 하트는 **모바일에서 더블탭이 흔하다** — 재현이 어려운 게 아니라 자주 났다.
      커뮤니티는 낙관적 갱신이 조용히 롤백돼 "눌렀는데 안 눌림"이 되고,
      **ArtStory 는 에러 토스트까지** 떴다("잠시 후 다시 시도해주세요").
    - 고치는 방법은 둘을 **함께** 지키는 것이다:
      ① `deleteMany` / `createMany({skipDuplicates:true})` — 없는 걸 지우거나 있는 걸 만들어도 **안 던진다**
      ② 카운터를 `1` 이 아니라 **실제로 바뀐 행 수(`.count`)만큼** 움직인다
         → 아무것도 안 바뀐 요청은 카운터도 안 건드리므로 드리프트가 **원천 차단**된다
    - ⚠️ `explore.ts` 작품 좋아요에는 **이 해법이 먼저 들어가 있었다**(P2002/P2025 를 잡고 `count()` 로 다시 셈).
      커뮤니티·스토리가 나중에 만들어지며 그 패턴을 안 가져온 것이다 — **새 토글을 만들 땐 먼저 여기를 볼 것.**
      다만 거긴 매번 세도 되지만, 커뮤니티·스토리는 **인기순 정렬 근거**로 `likeCount` 컬럼을 들고 있어 셀 수 없다.
    - ⚠️ 이웃 추가(`follow.ts`)는 알림이 두 번 갈 수 있었다 — `Notification.refKey` 는 **유니크가 아니라**
      (`@@index([userId, refKey])` 뿐) DB 가 안 막아 준다. `createMany` 의 `count > 0` 을 "이번에 새로 맺어졌는가"로 쓴다.
    - ⚠️⚠️ **회귀 임계를 '5xx 만 아니면 통과' 로 두지 말 것.** 실제로 나던 건 **400** 이었다 —
      500 만 보면 사용자가 겪던 증상을 통째로 놓친다. 지금은 `!== 200` 이면 실패다.

### 커뮤니티 (1단계, 2026-08-28) — 홈 개편 + 글로벌 게시판
- **홈 구성**: 배너(HeroSlider) → ArtWorks → **[좌 인기글(커뮤니티) / 우 GOTM 레일]**.
    - 배너는 **화면 전체 폭의 색 띠**(슬라이드 dominant color) 위에 컨텐츠를 `max-w-7xl` 가운데로. 그라데이션·글로우 제거 — "좌우는 배경색이 자동 확장".
    - GOTM 은 우측 좁은 컬럼이라 `variant="rail"`(세로 리스트). ⚠️ 4열 grid 를 좁은 컬럼에 그대로 두면 뷰포트 media query 라 뭉개진다(container query 미사용) — rail 변형 필수.
- **커뮤니티 게시판**(블라인드식 글로벌): `Post`/`PostComment`/`PostLike`. `routes/community.ts`, 화면 `CommunityPage`(목록·글쓰기)·`CommunityPostPage`(상세·댓글·좋아요).
    - **글마다 실명(닉네임+역할)/익명 선택**(`anonymous`). ⚠️ **익명이면 서버가 신원을 통째로 가려 내려준다**(id·닉네임·아바타·역할 null) — 역추적 방지. 본인에게만 '익명(나)'. 프론트에서 가리지 말고 서버 응답을 믿을 것(`serializeAuthor`).
    - `likeCount`/`commentCount` 는 비정규화 카운트 — 좋아요·댓글 CUD 시 `$transaction` 으로 함께 증감(랭킹·목록의 근거). 인기글 = 좋아요순.
    - 읽기 공개(`optionalAuth`), 쓰기 로그인, 삭제는 작성자 또는 Admin(모더레이션). cascade 로 글 삭제 시 댓글·좋아요 함께 삭제.
    - 회귀: `backend/__tests__/community.test.ts`(17), `e2e/36-community.spec.ts`(5).
- **2·3단계 예정**(미구현): 이웃(단방향 팔로우+알림+공개/비공개) · 스토리(별도 [소식] 피드) · 방명록. snssample.png 참고.

## TanStack Query Key Map

| 쿼리 키 | 사용 위치 | invalidate 하는 곳 |
|---------|----------|-------------------|
| `['galleries', ...]` | GalleriesPage | 찜 토글 |
| `['gallery', id]` | GalleryDetailPage | 찜, 리뷰 CUD, 상세수정, 홍보사진 |
| `['exhibitions', ...]` | ExhibitionsPage | 찜, 지원 |
| `['exhibition', id]` | ExhibitionDetailPage | 찜, 지원, 삭제 |
| `['shows', ...]` | ShowsPage | 찜 토글 |
| `['show', id]` | ShowDetailPage | 찜, 소개수정 |
| `['gallery-of-month']` | GalleryOfMonth | 리뷰 CUD, Admin GotM 관리 |
| `['hero-slides']` | HeroSlider, Admin | Admin Hero CRUD |
| `['benefits']` | BenefitsPage, Admin | Admin Benefit CRUD |
| `['favorites']` | MyPage 찜 목록(MyPicks), 찜 토글 전체 | 모든 찜 토글 |
| `['chats']` | 대화 목록 | 메시지 전송, 초대 수락 |
| `['chat', id]` | 대화 하나 | 메시지 전송 (8초 폴링) |
| `['chat-unread']` | Navbar 대화 배지 | 30초 폴링, 메시지 전송 |
| `['mutuals']` | 메시지창 [이웃에게 메시지] 피커 | (피커 열 때만 fetch) |
| `['received-invites']` | 내 전시 '초대받은 전시' 탭 | 초대 수락/거절 |
| `['my-likes']` | MyPage 찜 목록의 '작품' | 좋아요 토글 |
| `['portfolio']` | MyPage Artist | 포트폴리오 수정 |
| `['my-applications']` | MyPage Artist | 지원 |
| `['my-galleries']` | MyPage Gallery | 갤러리 등록 |
| `['my-exhibitions']` | MyPage Gallery | 공모 등록/삭제 |
| `['my-shows']` | MyPage Gallery | 전시 등록/삭제 |
| `['approvals']` | MyPage Admin | 승인/거절 |
| `['notifications']` | Navbar 알림 dropdown | 읽음/전체읽음 |
| `['unread-count']` | Navbar 벨 뱃지 | 30초 polling, 읽음/전체읽음 |
| `['inquiries']` | SupportPage | 문의 작성/답변 |
| `['community', sort, scope]` | CommunityPage 목록(scope=all/mine/commented) | 글쓰기·좋아요·삭제 |
| `['community','post', id]` | CommunityPostPage 상세 | 좋아요·댓글 CUD |
| `['community-popular']` | 홈 인기글 위젯 | 글쓰기·좋아요 |
| `['todo-boards']` | MyPage Admin 할 일 보드 | 보드/항목 CUD |
| `['todo-board', id]` | 할 일 보드 상세 | 항목 CUD·체크·순서·댓글 |
| `['todo-members']` | 담당자 선택 | (5분 staleTime, invalidate 없음) |
| `['todo-comments', itemId]` | 항목 모달 | 댓글 작성/삭제 |
| `['community', sort]` | CommunityPage | 글 작성/삭제 |
| `['community-popular']` | 홈 PopularPosts | 글 작성 |
| `['follow', userId]` | FollowButton | 이웃 추가/취소 (낙관적) |
| `['story-feed']` | FeedPage(ArtStory) | 스토리 작성/삭제, 좋아요, 댓글, 이웃 추가 |
| `['story-comments', storyId]` | 스토리 카드 댓글 | 댓글 작성/삭제 |
| `['story-likers', storyId]` | 스토리 좋아요 누른 사람 | 좋아요 토글 |
| `['guestbook', userId]` | 작가 홈페이지 방명록 | 방명록 작성/답글/삭제 |
| `['ads']` | AdSlot(사이드바 광고) | (5분 staleTime) Admin 광고 CUD |
| `['ads-all']` | Admin 광고 관리 | 광고 CUD |

## Deployment (Render.com)

- **URL**: https://artlink-stmq.onrender.com
- **브랜치**: `deploy/render` (main에서 작업 → merge → push)
- **빌드 순서**: frontend build → backend build → prisma migrate deploy → seed → npm start
- ⚠️ **Node 버전 고정 필수** — `backend/package.json`의 `engines.node`는 **`24.x`(LTS)**. 예전엔 `">=20"`이라 Render가 매번 **최신 Node**를 골랐고, 2026-08-03 새로 나온 **26.6.0 설치 실패**로 배포가 죽었다(`Unable to install Node.js version 26.6.0`). 범위 지정 금지 — 새 Node가 나올 때마다 무작위로 배포가 깨진다. `.nvmrc`(24)도 함께 둔다.
- ⚠️ **배포 전 검증은 `npm run build`로** — `tsc --noEmit`은 테스트 파일을 검사하지 않아 통과하지만, 실제 빌드는 `tsc -b && vite build`라 테스트까지 검사한다. 실제로 테스트의 타입 오류로 배포가 실패한 적 있다(2026-08-03).
- **모놀리스 배포**: Backend Express가 Frontend `dist/`도 서빙
- **이미지 업로드**: Cloudinary 환경변수 유무로 자동 전환 (있으면 Cloudinary, 없으면 디스크)
- **PWA 캐시**: workbox `skipWaiting` + `clientsClaim`, `controllerchange` → 자동 reload
- **요금제(Starter)**: 2026-07 무료→Starter 전환. **스핀다운/콜드스타트 없음**, PostgreSQL **90일 자동삭제 없음**(영구 유지). 백업 보존은 대시보드에서 확인 권장(별도 pg_dump→R2 오프사이트 백업 미구성)
- **재배포**: `git checkout deploy/render && git merge main && git push`
- 🔒 **배포 전 데이터 유출 점검 필수** — `bash scripts/predeploy-check.sh`
  - `.githooks/pre-push`가 **push 때 자동 실행**한다. 최초 1회만 `git config core.hooksPath .githooks`
  - 막는 것: DB 덤프(`*.dump/*.sql/*.csv` — 마이그레이션 제외) · `.env` 계열 · `backend/uploads` 원본 ·
    파일 내용의 실서버 접속정보(Render Postgres 호스트, 비밀번호가 박힌 DB URL) 및 GitHub 토큰 · 2MB 초과 신규 파일
  - **왜**: 실서버 DB를 로컬로 복제해 확인하는 일이 잦다. 그때 생기는 덤프·`.env` 백업이 커밋되면
    **실제 가입자 개인정보가 GitHub에 공개**된다. 한번 올라가면 커밋을 지워도 퍼진 것으로 봐야 하므로
    올라가기 전에 막는 게 유일한 방어다. 실제로 `.env.local-backup`이 추적 대상이라 커밋될 뻔했다(2026-08-11)
  - 우회는 `--no-verify`지만, 걸린 이유를 먼저 확인할 것

## 개발 원칙

- **아키텍처 문서 유지**: 신규 작성 및 변경 시 항상 `architecture.md`에 반영
- **사용자 가이드**: 기능 구현 시 코드 블록 위치를 명시한 개발자 가이드를 주석이나 문서로 남길 것 (큰 틀에서)
- **데이터 무결성**: 등록/수정/삭제 시 서버 상태와 DB 동기화 보장 및 예외 처리 철저
- **검증**: 단일 기능 검증 + 복합 시나리오 ([등록→승인→검색→지원]) 전체 흐름 테스트
- **상업화 고려**: 인증(OAuth), 환경 변수 교체가 용이한 추상화 구조 유지

## 미완료 항목 (우선순위 순)

1. **React-hook-form + Zod** — 설치됨(v7.71, v4.3)이나 미사용. 갤러리/공모 등록 폼에 적용 필요
2. **수정 요청 UI** — 백엔드 API 완성, 프론트엔드 MyPage Gallery 섹션에 "수정 요청" 버튼+폼 미구현
3. **ESLint + Prettier** — eslint.config.js 존재하나 팀 규칙 미설정
4. **MyPage 분리** — `MyPage.tsx` ~3000줄 단일 파일. 섹션별 컴포넌트 분리 고려
5. 나머지 페이지 `DESIGN.md` 기반 리디자인 (상세/마이페이지/혜택/고객센터)

> 2026-07 완료: 코드 스플리팅(React.lazy), 목록필터 URL 동기화, 알림 TTL 정리(읽은 90일+), 업로드 orphan 파일 정리(`lib/storage.ts`)

## 참고 문서

- `HANDOFF.md` — **인계/현재상태 우선**. 0장(계정 인계)에 최근 변경 이력·운영 정보·다음 작업 정리. 이하 장은 API 전체·인증 플로우·버그 이력 등 상세 레퍼런스
- `architecture.md` — 상세 아키텍처 (데이터 모델, API 상세, 컴포넌트 가이드, 로깅/안정성)
- `REQUIREMENTS_CHECKLIST.md` — 요구사항 체크리스트
- `docs/artlook-asset-provenance.md` — **ArtLook 벽·액자 자산 출처**. 전부 자체 제작(GPT 생성)이다.
  ⚠️ 예전엔 `frontend/public/artlook/*/ATTRIBUTION.txt` 였는데 그 자리는 `artlink.cc/...` 로
  **공개 서빙된다** — 내부 메모라 2026-09-02 에 옮겼다. 자산을 추가하면 여기에 적을 것.
  ⚠️ **출처를 추측으로 적지 말 것** — zip 파일 이름만 보고 경쟁사 자산이라 적어 둔 옛 기록 때문에
  2026-09-02 감사가 멀쩡한 자산의 배포를 막을 뻔했다. 모르면 '미확인'으로 두고 만든 사람에게 물을 것.

---

## 유저 시나리오 및 UI 로직 상세

### 1. 초기 진입 및 공통 UI
- **Navigation Bar**: 전 페이지 노출. 좌측 [ArtLink 로고](클릭 시 홈 이동), 가운데 [홈, 갤러리, 전시, 모집공고, 고객센터], 우측은 로그인 상태에 따라 갈린다.
    - 가운데 메뉴에는 **비로그인도 볼 수 있고, 다른 진입점이 없는 페이지만** 둔다.
      로그인해야 쓸 수 있는 마이페이지를 여기 두면 비로그인 사용자가 눌렀다가 로그인 화면으로 튕긴다.
    - 로그인 시 우측: [쪽지][알림] · `이름 (역할)`. 비로그인 시 우측: [로그인] 하나뿐.
      **[로그아웃]은 우측 세로 사이드바 맨 아래**로 옮겼다(2026-08-28) — 제일 자주 보는 자리에 '나가기'가 있을 이유가 없다.
      그래서 우측 그룹의 마지막이 이름이고, **이름 오른쪽 끝이 사이드바 오른쪽 끝과 맞는다**(실측 1584=1584).
      ⚠️ lg 미만에는 사이드바가 없다 — 그쪽 로그아웃은 Navbar 햄버거 메뉴 안에 그대로 있다.
      **[마이페이지] 버튼은 없앴다**(2026-08-27) — 우측 세로 사이드바가 각 탭으로 바로 보내므로 중복이었다.
    - 🔑 **로고 왼쪽 끝은 본문 제목과 정확히 맞아야 한다.** 두 가지를 지킬 것:
      ① 네비바도 본문과 **같은 컨테이너** `max-w-7xl mx-auto px-6 md:px-12` (예전엔 `px-4 sm:px-6 lg:px-8` 이라 달랐다)
      ② 로그인 시 본문이 224px 좁아지므로 네비바도 **같은 조건으로 사이드바 자리를 비운다** — 안 비우면 1600px에서 48~144px 어긋난다.
      페이지 쪽도 `px-*` 바깥 + `max-w-*` 안쪽으로 이중으로 감싸지 말 것(HomePage 가 그래서 48px 밀렸다).
    - **로그아웃 버튼은 Navbar에만 둔다** — 예전엔 마이페이지 본문 우상단에도 있어 한 화면에 두 개가 보였다.
    - **모바일(lg↓)은 가운데 5메뉴를 하단 고정 탭바로 내렸다(2026-08-29, `components/layout/BottomTabBar.tsx`, catch 앱 방식).**
      정의는 **`lib/navLinks.ts` 하나** — 데스크톱 상단 중앙과 하단 탭바가 공유(아이콘 포함). `Layout` 이 본문에 `pb`(바 높이+safe-area)를 줘 가림 방지.
      그래서 모바일 [메뉴](햄버거)에는 이제 **마이페이지 탭 + 로그아웃만** 들어가고, 햄버거 자체가 **로그인 시에만** 뜬다(비로그인은 하단바+[로그인]으로 충분).
- **홈 구성 (2026-08-27 개편)**: `ArtWorks` → `Hero 슬라이더` → `Gallery of the Month`. 이 순서다.
    - **최상단은 작품이다.** 예전엔 히어로 배너가 맨 위, 작품(구 Favorites)이 맨 아래였다.
    - 히어로와 GOTM 사이에 있던 **퀵 내비게이션(QuickActionCards)은 Navbar와 중복이라 삭제**했다(파일도 제거).
- **ArtWorks 섹션** (`components/home/ArtWorks.tsx`, 구 `ExploreHighlight` = 구 'Favorites'):
    - 제목은 **ArtLink 로고와 같은 색 규칙** — `Art`(gray-900) + `Works`(#dc3545). 로고와 짝이 맞아야 브랜드 이름처럼 읽힌다. **부제는 두지 않는다.**
    - 우측 상단에 [⟳ 새로고침]과 [모두 모아보기 →]. 제목보다 작게 두되 **색은 gray-500 아래로 내리지 말 것** —
      gray-300/400 으로 뒀더니 "안 보인다"는 지적이 나왔다(흰 배경에서 gray-300 은 대비 1.5:1 수준으로 사실상 배경이다).
      현재 대비 4.84:1(AA 통과). 옅게 만들고 싶으면 크기로 조절할 것.
    - **첫 진입도 랜덤이다**(2026-08-27). 화면이 들어올 때마다 새 시드를 만들어 `?seed=N` 으로 부른다.
      좋아요순으로 고정하면 홈에 늘 같은 얼굴이 걸려 좋아요가 적은 작가는 영영 안 나온다 — 노출을 골고루 돌리는 쪽을 택했다.
      **대신 "좋아요가 홈 노출을 정한다"는 참여 동기는 사라졌다**(하트 배지는 그대로, 좋아요순은 둘러보기의 [좋아요순] 탭).
      서버는 seed 없이 부르면 **여전히 좋아요순**이고 그 경로도 테스트에 남아 있다 — 되돌리려면 프론트에서 시드만 빼면 된다.
    - [새로고침]은 둘러보기의 그것과 **같은 동작**(랜덤 재정렬 + 같은 작가 연속 방지). 시드만 갈아끼운다.
      백엔드는 seed가 있으면 좋아요 집계 쿼리 2개를 건너뛴다 — 연타되는 버튼이라.
    - 재정렬 중에도 **이전 작품을 그대로 둔다**(`placeholderData: prev => prev`). 안 그러면 `images.length === 0 → null` 때문에 섹션이 통째로 사라졌다 나타나 홈이 위로 튄다.
- **둘러보기(`/explore`)는 Navbar 메뉴에서 뺐다**(2026-08-27) — 진입점은 ArtWorks 섹션의 [모두 모아보기] 하나. 라우트·페이지는 그대로다.
- **Hero Section (슬라이더)**: 홈 중간(ArtWorks 아래).
    - Admin이 [사진, 제목, 링크 URL] 등록/수정/삭제 가능.
    - 3초 자동 슬라이드. **사용자가 수동 조작(슬라이드) 시 해당 시점부터 타이머 리셋 후 3초 뒤 다시 자동 시작.**
    - [바로가기] 버튼 우측 하단 배치: 외부 URL은 브라우저 새 창, 내부 URL은 라우팅 이동.
- ⏸ **혜택(`/benefits`)은 당분간 비활성화**(2026-08-27) — Navbar 메뉴에서 빼고 라우트는 **홈으로 리다이렉트**(404 아님. 기존 링크·북마크·검색결과가 죽지 않게).
    `BenefitsPage.tsx`·백엔드 `/api/benefits`·Admin 마이페이지 '혜택 관리' 탭은 **그대로 살아 있다**(데이터도 유지). 다시 켤 땐 `App.tsx`의 주석 처리된 import·라우트, `Navbar.tsx` navLinks, `e2e/tests/13-admin-content.spec.ts`의 `test.skip` 셋을 되돌리면 된다.

### 2. 마이페이지 및 권한 (My Page)
- **메뉴 배치(2026-08-27, artspoon.io 참고 — 그쪽은 좌측, 우리는 우측)**:
    - **lg(1024px)↑ — 전 페이지 우측 세로 사이드바** (`components/layout/MyPageSideMenu.tsx`, `Layout` 이 렌더).
      홈·갤러리 목록 어디서든 마이페이지 각 탭으로 바로 들어간다. **로그인했을 때만** 뜨고,
      **강조는 `/mypage` 에 있을 때만** — 홈에서까지 '프로필'이 눌린 것처럼 보이면 현재 위치를 잘못 알려준다.
      `Layout` 의 `<main>` 에 **`min-w-0` 필수**(27번) — 없으면 안쪽 표가 폭을 밀어 사이드바가 화면 밖으로 나간다.
      **MyPage 안에는 사이드바를 두지 말 것** — 전역 것과 겹쳐 마이페이지에서만 메뉴가 두 개로 보인다.
    - **lg↓ — Navbar 우측 상단 [메뉴] 버튼 안**에 같은 목록이 'MY PAGE' 구분선 아래로 들어간다.
      **버튼을 따로 만들지 않는다** — 우측 상단에 햄버거가 둘이면 뭐가 뭔지 알 수 없다.
      마이페이지 본문의 **가로 스크롤 탭바는 lg↓ 에서 그대로 유지**한다(그 화면 안에서 탭을 바꾸는 데는 이게 제일 빠르다).
    - **작가 메뉴(2026-08-27 개편)**: `프로필 · 홈페이지 · 포트폴리오 · 받은 초대 · 찜 목록 · 좋아요한 작품 · 내 리뷰 · 내 전시`
        - **[홈페이지]는 마이페이지 탭이 아니라 공개 작가 페이지(`/portfolio/:userId`)로 나가는 링크**(`linkTo`).
          작가에게 이 페이지는 '관리 화면'이 아니라 남에게 보여줄 홈페이지다 — 고치기 전에 남 눈에 어떻게 보이는지 반드시 한 번 보게 한다.
        - **편집은 그 공개 페이지의 [수정] 버튼(주인 본인만)** → `/mypage?tab=homepage-edit`. 메뉴에는 없는 숨은 탭이다.
          이 탭은 **들어오자마자 편집 모드**다(읽기 화면을 거쳐 [수정]을 또 누르게 하지 않는다).
          ⚠️ 자동 진입은 **딱 한 번만**(`autoEditedRef`) — 저장하면 invalidate→재조회가 일어나는데 매번 걸리면
          저장 결과를 볼 새도 없이 편집 모드로 튕기고 [취소]도 무력화된다.
          주인 판정은 **로그인 id 와 주소의 userId 비교**뿐 — 역할(ARTIST)만 보면 남의 페이지에서도 [수정]이 뜬다.
        - **[포트폴리오] 탭 = PDF 포맷 4종**(`PortfolioFormatSection`). 예전엔 편집 화면 **맨 아래**에 붙어 있어
          작품 30장을 지나야 나왔고 있는 줄도 모르는 기능이었다.
        - `?tab=homepage` 로 들어와도 열 화면이 없다 → `resolveTab` 이 **바깥 링크 id 는 절대 돌려주지 않는다**(프로필로 폴백).
    - 🔑 **메뉴 정의는 `frontend/src/lib/myPageMenu.ts` 하나뿐** — 위 세 곳이 같이 쓴다.
      **항목을 추가하면 `MyPage.tsx` 의 `currentTab === '...'` 분기도 함께 추가할 것.**
      빠뜨리면 역할 폴백에도 안 걸려(id 는 유효하니까) **에러 없이 빈 화면**이 된다.
      회귀는 `frontend/src/__tests__/myPageMenu.test.ts` 가 MyPage.tsx 소스를 직접 대조해 양방향으로 잡는다.
- **인증**: 비로그인 시 로그인 창 노출. 로그아웃 시 로그인 페이지 이동. **프로필 사진 변경 기능** 포함.
- **Artist 유저**:
    - **포트폴리오**: [작가 약력, 작가노트, 한 줄 소개, 경력(학력/개인전/단체전/아트페어/수상), 작품 사진(최대 30장)] 관리.
      - **작품 정보**: 사진을 누르면 [작품명, 시리즈, 크기(가로×세로), 재료, 제작연도, 판매상태, 작품 설명] 입력. 실제 작가 포트폴리오는 예외 없이 작품마다 캡션을 붙이므로 이 정보가 없으면 포맷 PDF에서 캡션이 통째로 빠진다 — 미입력 작품엔 '정보 없음' 배지 표시.
      - **시리즈**: 작품에 붙인 시리즈명으로 자동으로 묶이고, 시리즈마다 소개 글을 넣으면 PDF에 소개 페이지가 생긴다.
      - **화면 이름은 ArtLink 로고 규칙을 따른다**(앞 검정 + 뒤 빨강 #dc3545) — Art**Link** / Art**Works**(홈 섹션·둘러보기) /
        Home**Page**(공개 작가 페이지) / Port**Folio**(포트폴리오 탭) / Art**Look**(액자 걸기) / My**Picks**(찜 목록) / Art**Talk**(대화).
        **화면 좌측 상단**에 둔다 — 사이드바 메뉴 라벨은 평범한 한글 그대로다(ArtLook 만 예외: 기능 이름이 곧 브랜드라 메뉴에도 색을 준다).
        메뉴 항목에 색을 넣을 땐 `MyPageTab.brand: ['Art','Look']`, 옆 설명은 `note` (그냥 기능 이름에 색을 넣으면 로고 규칙이 헐거워진다).
      - **둘러보기(`/explore`)는 홈 ArtWorks 의 [모두 모아보기]가 보내는 곳**이라 **같은 이름·같은 컨트롤**을 쓴다.
        우측 상단 [⟳ 작품 새로고침] 아래 [♡ 좋아요순] 두 글자 버튼. 예전의 알약 [랜덤]/[좋아요순] 은 없앴다 —
        랜덤이 기본이라 버튼일 이유가 없었다. 기간 필터는 좋아요순일 때만 나온다.
      - **ArtLook(액자 걸기)은 마이페이지 메뉴 탭**이다. 예전엔 홈페이지 편집 화면 안 버튼이라
        '수정'에 들어가야만 보였다 — 편집과 무관한 기능인데도. 이름이 ArtLink 와 헷갈려 옆에 '액자 걸기'를 작게 적는다.
        메뉴 글씨체도 로고와 맞춘다(`font-bold tracking-tight font-serif`), 아이콘은 액자(`Frame`).
      - **ArtLook 은 새 탭이 아니라 그 탭 안 `iframe`** 이다(2026-08-28). 마이페이지 안에서 하는 일이라 왕복할 이유가 없다.
        ⚠️ 작품 목록은 **iframe 을 그리기 전에** `stageArtLookWorks()` 로 localStorage 에 올려야 한다 —
        ArtLook 은 뜰 때 **한 번만** 읽으므로 순서가 뒤바뀌면 빈 화면이 된다. (같은 출처라 iframe 안에서도 읽힌다)
        ⚠️ 임베드는 `?embed=1` 로 페이지 머리말을 감춘다 — 바깥에 이미 제목이 있어 겹친다.
        새 탭(운영페이지 정산 > 판매작 홍보)에서는 머리말이 그대로 나온다.
      - **작가 알림은 그 전시를 열어둔 채로 보낸다** — `artistExhibitionLink(id)` → `?tab=applications&ex=<id>`.
        `ApplicationsSection` 이 그 값을 읽어 **탭까지 바꾸고** 해당 카드를 펼친다(다른 탭이면 카드가 아예 안 보인다).
        딱 한 번만 실행할 것(`deepLinkDone`) — refetch 마다 걸리면 사용자가 접어도 다시 열린다.
        마이페이지의 `My Page` 제목은 없앴다 — 우측 사이드바가 현재 위치를 알려주고 각 탭이 제 이름을 갖는다.
      - **공개 페이지 이름 아래는 [한 줄 소개] → [인스타]** 순. 한 줄 소개가 없으면 **아무것도 쓰지 않는다**
        (예전엔 '아티스트 포트폴리오' 라는 누구에게나 해당되는 문구가 자리만 차지했다).
      - **포맷 카드에 '포맷 A' 같은 이름과 설명을 두지 않는다** — 고르는 단서는 표지 그림과 판형 배지다.
      - **정보가 빠진 작품 안내는 빨간 글씨 한 줄**(노란 상자 아님 — '주의'로 안 읽히고 자리만 컸다):
        "작품 N점에 작품명·재료·크기·연도가 없습니다. 홈페이지에서 정보를 채워 퀄리티를 높여보세요."
      - **포맷 PDF**: 4종(16:9 / A4가로 / A4가로 / A4세로) 중 골라 미리보기 후 저장. 미리보기는 PDF와 **같은 HTML**을 축소해 보여준다(`components/shared/PortfolioFormatPicker.tsx`, `lib/portfolioFormats.ts`).
        마이페이지 **[포트폴리오] 탭**에 있다(내용은 [홈페이지]에서 고친다).
      - **가이드형 디자인 — 제작 화면(2026-08-29)**. `PortfolioFormatPicker` 는 4샘플 그리드가 아니라 **좌측 sticky 설정(탭 3개) / 우측 단일 라이브 미리보기**.
        ⚠️ **설정은 탭 3개**(표지 / 색·글꼴 / 작품·본문)로 묶고 탭 안에서만 스크롤 — 세로로 다 쌓았더니 "설정란이 존나 길어" 못 쓴다는 지적. 저장·전체화면 버튼은 탭 밖 하단 고정.
        ⚠️ **라벨은 디자이너 용어 금지, 눈에 보이는 대로** — "밴드 상단"→"사진 위·이름 아래", "타이포"→"사진 없이", "뮤지엄 라벨"→"작품+설명" 등(작가가 뭘 얻을지 바로 알게).
        **포맷 A/B/C/D 프리셋 없음. 표지는 디자인된 레이아웃 22종 선택**(자유 축 조합은 폐기 — 아래 ⚠️표지). 컨트롤:
        **표지 레이아웃 15종(그룹은 눈에 보이는 대로: 사진 없이 3·대표작 1점 6·여러 작품 2·색 배경 3·심플 1) + 글꼴 6 · 색[배경 8·글자 7·강조 7] · 판형(세로/가로/와이드) · 작품 페이지 레이아웃(1점 크게/작품+설명/꽉채우기/2점씩/4점씩/6점목록) · 설명 · 작품 정보 위치 · 본문 정렬**.
        ⚠️ **표지 글 요소(머리말·한 줄 소개·연도·이름 강조색)와 사진은 세팅 메뉴에서 뺐다** — 기본 전부 표시하고 **미리보기 표지를 클릭하면 아래 [표지 편집] 인라인 패널**이 열려 끄거나 바꾼다(미리캔버스식). **사진은 슬롯별**: `coverImageIds: number[]`(표지 칸에 넣을 작품 id를 순서대로, **`0`=빈 칸**). 여러작품 표지(4점 격자 등)는 칸을 골라(활성) 아래 작품 띠에서 채우고, 칸의 × 로 비운다. **미편집(빈 배열)이면** 포트폴리오 순서(자동)+부족 시 사진 있는 표지로 폴백; **편집하면(배열 있음)** 그 레이아웃을 그대로 유지하고 **빈 칸은 폴백 없이 빈 자리로 둔다(전부 비워도 격자 유지)** — 빈 셀은 `gridCell(url='')`이 소프트 패널만 그린다. 옛 단일 `coverImageId` 는 `[id]` 로 마이그레이션. 영문 머리말 문구=`coverEyebrowText`(override, 비우면 기본 'ARTWORK PORTFOLIO'; `EYEBROW` 상수를 직접 쓰지 말고 `v.eyebrow`), 한 줄 소개 문구=`coverTaglineText`(표지 전용 override, 포트폴리오 tagline 안 건드림 — 비우면 표지 소개 줄 없음). `CoverInlineEditor`(`PortfolioFormatPicker`), 표지 페이지 아래에만 렌더(미리보기 표지 클릭으로 열림).
        ⚠️ **작품·본문 탭은 레이아웃별로 적용 여부가 다르다**(`bodyApplicability`): 작품 설명은 **full(꽉채우기)·index(6점목록)엔 안 나온다**(회색 비활성). 설명 '전체'(전문)는 **한 장에 작품 1점인 hero·label에서만** — 작품 페이지엔 요약 2줄, 전문은 뒤 글 페이지(「〇〇 이야기」, `prosePages`)로 잇는다(넘침 없음). 격자(duo/grid)는 여러 점이라 '전체' 칩을 안 보이고 2줄 요약까지(`desc==='full'`도 엔진이 short 취급). 작품 정보 위치(worksCaption)는 **duo/grid 에서만** 유효(hero=가운데 고정·label=라벨블록·full=하단좌측·index=제목만). 레이아웃 바꿀 때 못 쓰는 값은 정리(full→short).
        - **편집 가능한 PPTX 내보내기**(2026-08-30, `renderPagesToPptx`/`downloadPortfolioPptx`, pptxgenjs 동적 import). 페이지를 이미지로 붙이지 않는다 — **렌더된 DOM을 걸어**(`serializePage`) 각 요소를 네이티브 개체로: 텍스트→`addText`(runs, 위치=getBoundingClientRect·스타일=getComputedStyle, px→pt=`px*k*72`), `<img>`→`addImage`(canvas `toDataURL` 임베드, 1500px 캡), 배경·테두리→`addShape('rect')`. 슬라이드=페이지, 판형 mm→inch. **텍스트 잎 판정**(`isTextLeaf`: 자식이 전부 인라인)으로 컨테이너 배경과 텍스트 이중출력 방지, 색 다른 `<span>`(상태 배지)은 run 으로 분리. ⚠️ **글꼴은 이름 참조** — 한글 웹폰트는 보는 PC에 없으면 대체되므로 `faceOf`가 널리 깔린 **맑은 고딕/바탕**으로 매핑(색·위치·사진·정렬은 그대로). PDF와 같은 이미지 프리페치(동일출처 blob→canvas taint 없음) 재사용. 검증: 실제 생성 후 LibreOffice 렌더가 HTML과 일치 + slide XML 개체 카운트. ⚠️ **`hexOf()` 는 `rgba?()` 만 파싱한다** — 크롬이 `color(srgb …)` 로 내리는 색(= `color-mix` 결과)은 null 이 되어 도형이 **에러 없이 조용히** 빠진다.
        - **⚠️ 그림 크기 슬라이더는 음영(패널)도 함께 줄인다**(`panelBox`/`fillImg`, 2026-08-30) — 이미지만 줄이면 회색 패널이 남아 어색했다. 패널을 영역의 `coverImgScale`%로, 이미지는 패널 100%(비율 유지). heroBox/gridCell/squareHero/mosaic 큰칸 적용. `cImg`는 패널 없는 bandBottom 전용.
        - **표지·작품 선택 UI = 미니 목업 그리드**(2026-08-30): 텍스트 칩 대신 실제 표지/작품 페이지를 회색 자리표시 이미지로 축소 렌더(`CoverMock`, `coverMocks`/`worksMocks` useMemo). 표지에 **그림 크기(`coverImageScale` 0.6~1.0)·글자 크기(`coverTextScale` 0.8~1.25) 슬라이더**([표지 편집] 패널). 그림 스케일은 표지 전용 모듈변수 `coverImgScale`(heroBox/gridCell/cImg만 참조, body 는 1)로, 글자 스케일은 `ft(v,base,…)`가 `fitTitle` base·min 을 배율(여전히 폭·줄수로 바운드, 안전). **이름 겹침(overlap) 삭제 → 21종**(2026-08-30), **6종 더 삭제 → 15종**(2026-09-04 사용자 요청:
          큰 이름(좌측)·이름 아래쪽·큰 이름+사진·3점 가로·이름+작품 띠·구석 여백).
          ⚠️ 표지를 없앨 땐 `normalizePdfDesign` 의 `RETIRED` 표에 **같은 그룹에서 가장 가까운 표지**를 적을 것 —
          기본값(bandTop)으로 떨어뜨리면 '사진 없는 타이포 표지'를 고른 사람이 갑자기 사진 표지를 받는다.
          editorialLeft·baseline→stacked / poster·overlap→fullTint / triptych·filmstrip→grid2x2 / corner→ruleFrame.
          ⚠️ **삭제한 표지를 `DESIGN_DIRECTIONS` 가 참조하고 있지 않은지 확인할 것** — 자동 편집이 그걸 고르면
          타입은 통과하는데 렌더가 기본값으로 폴백한다(에러 없이 조용히).
        저장은 `Portfolio.designConfig`(JSON: `{bg,ink,accent,font,page,worksLayout,desc,worksCaption,proseAlign,coverLayout,coverEyebrow,coverEyebrowText,coverTagline,coverYear,coverNameAccent,coverImageIds,coverTaglineText,coverImageScale,coverTextScale}`). `normalizePdfDesign` 기본 = 화이트/블랙/레드·명조·세로·**작품 hero(쪽당 1점)**·설명none·글배치below·본문왼쪽·표지 bandTop·글요소 전부표시. 옛 `palette`·옛 글꼴(serif→myeongjo/sans→gothic)·**옛 `density`(1/2/4)→`worksLayout`(hero/duo/grid)**·옛 이미지없음→`coverLayout:'serifCenter'`·옛 6키(editorial→bandTop 등) 자동 마이그레이션. 옛 표지 축·`cover:'studio'` 키는 **조용히 무시**.
        - **⚠️ 본문도 디자인됐다(2026-08-29, 표지만 22종 만들고 본문 방치한 걸 사용자가 지적 → 격상)**. 실서버 40명 = 작품 중앙 8점·회화, 그래서 **작품 페이지가 PDF의 핵심**. `worksLayout`(WORKS_PER_PAGE hero1/label1/full1/duo2/grid4/index6): **hero**(대형 단독) · **label**(뮤지엄 라벨=작품+옆 캡션블록[재료·크기·연도·상태·설명], 긴 설명은 뒤 글페이지로 spill) · **full**(전면, 소프트패널) · **duo/grid/index**(격자, `gridWorksPage`). 작가노트는 **`statementPages`**: 짧으면 세로 중앙+대형 인용(옛날엔 상단에 붙어 90% 텅 빔), 길면 `prosePages` 폴백. 회귀 **`scratchpad/pf/audit.mjs`**(worksLayout×설명×판형×작가 216조합 2,688장, 넘침 0).
        - **색은 `lib/portfolioColors.ts`** — 배경/글자/강조를 **따로** 고르고 sub·line 은 자동 도출(mix). **배경색별 추천 글자색**은 WCAG 대비(≥4.5), **추천 강조색**은 ≥3.0(`mono`=글자색은 항상 추천)으로 자동 판정(화면에 초록 점). 배경 바꾸면 대비 부족한 값을 추천값으로 자동 교체(`setBg`). ⚠️ **표지·본문 전부 `theme.bg/ink/sub/accent/line` 만 쓸 것**(하드코딩 금지) — 실제로 CV/약력을 `#4a4a4a`/`#3a3a3a`, 작품이야기를 `#444` 로 박아 **다크 배경에서 작가이력이 안 보였다**(2026-08-29 수정). 렌더 host 배경도 `theme.bg`(옛 `#fff` 는 다크에서 흰 플래시).
        - **⚠️ 표지 = 디자인된 레이아웃 22종(2026-08-29, 진화: 8프리셋→자유축조합→6레이아웃→22레이아웃)**. **왜 6→22**: 실서버 40명 조사 결과 **한줄소개 0명·작가노트 대부분 없음·작품 중앙 8점**(한국화가들) → 표지는 **이름+(선택)작품+연도만으로 아름다워야** 하고, 6종은 "개짜쳐"(사용자). fliphtml5/캔바 같은 템플릿 갤러리로 일반화. `COVER_LAYOUTS`(22, `group`·`minImages`·`render`). 그룹: **타이포**(serifCenter/editorialLeft/stacked/baseline/nameplate) · **이미지**(bandTop/bandBottom/matted/fullTint/squareHero/side/poster/overlap) · **여러 작품**(grid2x2/triptych/mosaic/filmstrip — 실제 여러 그림을 표지에 보여줌) · **색**(accentField/colorBand/split) · **미니멀**(corner/ruleFrame). 색·글꼴은 theme 토큰(자유). **회화 크롭 금지** → 이미지·그리드셀 전부 `img()`(object-fit:contain). **안전**: 표지는 고정 page(overflow:hidden) 안 absolute라 페이지를 못 넘긴다. 이름 `fitTitle`(슬롯 폭·줄수 자동축소 min32), 한 줄 소개 `-webkit-line-clamp`. `coverHtml`이 **이미지 부족 시 폴백**(minImages 미달 → 1장↑ bandTop, 0장 serifCenter). 회귀 **`scratchpad/pf/audit.mjs covers`**(21레이아웃 × 작가 3 × 판형 3 = 189조합, 요소 페이지밖 검사, 넘침 0).
        - **엔진 이음새**: `applyDesign(theme, design)` 가 색·판형(`page`)·밀도(`worksPerPage`)·글꼴(`display`/`bodyFont`/`titleSerif`)·본문정렬(`proseAlign`)을 갈아끼운 **파생 테마**를 만든다. ⚠️ **본문(여백 PAD·러닝요소)은 표지와 무관하게 항상 `archive` base** — `buildPortfolioPages` 가 `applyDesign(themeById('archive'), design)` 로 고정. 글꼴 프리셋(`FONT_PRESETS` 6)은 제목+본문 한 쌍, Google Fonts `<link>`(`PORTFOLIO_FONT_HREF`)를 피커 마운트 때 지연 로드.
        - 데모 리뷰용 리치 포트폴리오(작동 사진 + 캡션·약력·경력·시리즈)는 `backend/scripts/seed-demo-portfolio.ts [userId]`(로컬 전용).
        - **작품 배치는 밀도 기반 일반 그리드**(`gridWorksPage`) 하나로 통일 — 판형 방향(가로/세로)×밀도(1/2/4)로 cols×rows 결정. 옛 포맷별 분기(studio 3업/story 내러티브)는 미사용(참고용 dead code).
        - ⚠️ **높이 안전이 전부다**: 페이지는 `height:avail` 고정, 캡션 높이는 **제목·재료 줄바꿈까지 추정**(한글 글자폭 넓음)해야 한다 — 상수로 뒀더니 좁은 칸(밀도4)+긴 제목에서 **300장이 조용히 잘렸다**. `gridWorksPage` 의 `estCaptionH`(제목·메타 줄 추정 + SAFETY 24)로 잡는다. 설명='전체'는 밀도1에서만(스토리식 좌작품/우설명 + 뒤 글페이지로 넘김, 안 자름); 밀도>1 의 '전체'는 2줄 요약.
        - ⚠️ **검증은 실측으로 — 하니스는 `scratchpad/pf/`** (사용법은 그 안 README, 2026-08-31 재구축).
          `node audit.mjs all`(넘침·설명줄수·대비 전수 2,688장) · `probe3.mjs`(작품 지면점유) ·
          `blast.mjs`(PDF 가 깨지는 조합) · `fidelity.mjs`(미리보기 vs html2canvas) ·
          `golden*.py`(실제 작가 포트폴리오 5종 실측). **웹폰트 로드 + `document.fonts.ready` 대기 필수** —
          안 그러면 폴백글꼴로 측정돼 무의미하다.
          ⚠️ **하니스를 지우지 말 것.** 여기 적혀 있던 옛 하니스 6개(`pdf-overflow-audit.mjs`·
          `cover-layout-audit.mjs`·`works-caption-audit.mjs`·`covers-22-engine.mjs`·
          `works-layouts-engine.mjs`·`pptx-test.mjs`)가 **전부 사라져 있었고**, 그래서 아래 P0 사고를
          아무도 못 잡았다. 문서에 적힌 "46,752장 → 0" 은 재현 불가능한 숫자였다.
        - ⚠️ **표지에서 사진 높이를 못박고 글을 그 아래 붙이지 말 것** (`coverBandTop`, 2026-08-31).
          사진이 `h*0.5` 고정 + 글이 `top:100+imgH+56` 이라 A4 세로에서 하단 **28.7%**(414px)가 빈 채로
          남았다(위는 7.1%). 한 줄 소개가 그 자리를 메우게 돼 있었는데 — **실서버 작가 81명 중
          한 줄 소개를 채운 사람은 0명**이다(작가노트 2명 / 약력 54명). 즉 예외가 아니라 **전원**이 그 표지를 받았고,
          `bandTop` 은 기본값이라 대부분이 그걸 본다. 지금은 위아래를 다 잡은 flex 기둥 —
          글이 바닥에 앉고 사진이 남는 높이를 전부 가져간다.
          ⚠️ **21종을 전수 측정해서 고칠 것을 고를 것**(`scratchpad/pf/covervoid.mjs`). 비대칭인 건 이것 하나였다 —
          `serifCenter`(41/41)·`nameplate`(43/43)·`accentField`(42/42)는 **위아래가 같이 비는 가운데 정렬**이라
          하단 공백만 보고 고치면 멀쩡한 구성을 망친다. 판정은 **하단 공백이 아니라 위아래 비대칭**으로.
        - ⚠️ **뮤지엄 라벨(`label`)은 라벨에 적을 게 있을 때만 뮤지엄 라벨이다** (2026-08-31).
          이 배치는 지면의 44% 를 캡션 칸으로 비우는데 실서버 작품의 97% 는 적을 게 아무것도 없어,
          페이지 절반이 빈 흰 판이 됐다. `worksPages` 가 캡션·설명이 모두 비면 조용히 `hero` 로 그린다.
          고른 사람에게 배신이 아니다 — "라벨에 넣을 정보가 없으니 작품을 크게" 가 그 의도에 더 맞다.
        - ⚠️⚠️ **`artworkTitle()` 을 캡션에 그대로 쓰지 말 것 — '무제' 로 도배된다** (2026-08-31 실데이터 확인).
          그 함수는 빈 제목에 '무제' 를 돌려준다(정렬이 흔들리지 않게). 그런데 실서버 작품 372점 중
          제목이 있는 건 **10점(2.7%)**, **361점(97%)은 제목·재료·크기·연도가 전부 비어 있다**.
          그래서 26점짜리 포트폴리오가 **26쪽 내내 그림 아래 '무제' 한 단어만** 달고 나왔다 —
          정보가 아니라 "작가가 대충 만들었다"는 인상만 준다. 공개 홈페이지(`HomepageView`)는
          이미 `hasTitle()` 로 걸러 왔는데 **PDF 엔진만 안 걸렀다.**
          지금은 `captionParts()` 하나가 "무엇을 그리는가"를 정하고 **렌더와 높이 추정이 그걸 같이 본다** —
          캡션이 통째로 비면 자리를 아예 예약하지 않아 작품이 지면을 더 쓴다(실데이터 hero 골든의 93~117%).
          ⚠️ `captionHtml` 만 고치면 안 된다 — `labelWorksPage`·`fullWorksPage` 가 **따로** `artworkTitle()` 을
          쓰고 있었다(회귀 테스트가 잡았다). 새 레이아웃을 만들 때도 `captionParts` 를 통과시킬 것.
        - ⚠️ **작품 이미지 상자에 `height` 를 못박지 말 것 — `max-height` 여야 한다** (`heroWorksPage`).
          정사각·가로 작품은 폭에서 먼저 걸리므로(세로 지면에서 흔하다) 고정 높이 상자 안에서 위아래로 뜨고,
          그만큼 캡션이 작품에서 멀어진다 — 실측 **145~185px**. 미술관 캡션은 작품 바로 아래 붙는다.
          상자를 내용 높이로 두면 바깥 flex 가 작품+캡션을 **한 덩어리로** 가운데 정렬한다.
          넘침은 `max-height` 가 그대로 막으므로 안전하다.
          ⚠️ 다만 **격자(`gridWorksPage`)의 `height:imgH` 는 유지할 것** — 거긴 칸마다 같은 크기 패널을
          만들어 좌우를 맞추는 게 목적이다(비율이 제각각인 작품을 나란히 둘 때 필요).
        - ⚠️⚠️ **페이지 HTML 에 `color-mix()` 를 쓰지 말 것 — PDF 저장이 통째로 실패한다** (2026-08-31).
          크롬은 `color-mix()` 를 `color(srgb …)` 로 계산해 내리는데 **html2canvas 1.4.1 이 그걸
          파싱하다 던진다**(`unsupported color function "color"`). `renderPagesToPdf` 에 방어가 없어
          그대로 reject 되고 화면엔 "PDF 생성에 실패했습니다" 한 줄만 뜬다. PPTX 는 더 나쁘다 —
          `hexOf()` 가 `rgba?()` 만 받아 null 을 돌려주고 배경 도형이 **에러 없이 조용히** 빠진다.
          실측: `softPanel()` 하나 때문에 **표지 21종 중 13종 · 작품 6종 중 4종**이 걸려
          **조합 126가지 중 16가지(13%)에서만 PDF 가 나왔다**(기본값 bandTop+hero 포함).
          색은 `portfolioColors.ts` 의 **`mixHex()` 로 미리 섞어 hex 로** 넣을 것.
          회귀는 `portfolioFormats.test.ts` 가 전 조합 페이지 HTML 에서 `color-mix`·`lab/lch/oklab/oklch/color()`
          를 금지해 잡는다. **화면(크롬)에서는 멀쩡해 보이므로 눈으로는 절대 못 찾는다.**
        - ⚠️ **여백(`PAD`)·행간·캡션 상수는 판형에 비례시킬 것** (2026-08-31). 예전엔 A4 세로 기준
          픽셀 상수 하나를 전 판형에 썼는데, `applyDesign` 은 판형만 갈아끼우므로 그 값이 크기가
          다른 지면에 그대로 갔다 — 상하 224px 은 A4 세로(1414)에서 15.8% 지만 **와이드(900)에서는 24.9%**,
          같은 설정인데 지면의 4분의 1이 여백이 되고 그만큼 작품이 작아졌다(hero 45.8% → 17.6%).
          비율의 분모는 **각 테마의 원래 판형**이라 그 판형에서는 기존 값이 그대로 재현된다.
          ⚠️ 러닝 머리말 자리(`runTop`)도 **같이** 내릴 것 — 여백만 비례시키면 짧은 판형에서 머리말이
          본문 시작선을 넘어 작품과 겹친다. 머리말 덩어리는 글꼴이 고정이라 안 줄어드니 위 여백에 바닥을 둔다.
        - ⚠️ **캡션 높이 상수는 실측값이어야 한다** (`CAP_TOP`/`CAP_TITLE_LINE`/`CAP_META_LINE`, 글꼴 6종 실측).
          옛 상수(제목 28·보조 23)는 실제(21~26·15~17+5)보다 커서 **그 차이만큼 작품이 작아졌다** —
          격자에서 캡션 예약이 한 행의 40% 를 먹는데 실제 캡션은 그 3분의 2였다.
          `shortDescHtml` 의 글자폭은 `13*0.62`(=8.06px)로 잡혀 있었지만 **실측은 12.1px** 이라
          55% 과대평가였고, "2줄" 컷이 실제로는 3줄을 통과시켰다(예약 42px에 60px). 넘침이 안 났던 건
          `SAFETY`(24px) 쿠션이 그 초과분을 먹고 있었기 때문 — **글꼴 편차용 쿠션이 산수 오류에 이미
          소진돼 있었다.** 글 전체가 쓰는 `CHAR_W_RATIO` 로 통일했다. ⚠️ SAFETY 자체는 줄이지 말 것(별개).
        - ⚠️ **파생 색(`sub`/`line`)도 대비를 재야 한다** (`portfolioColors.ts`). `ink`·`accent` 만 검사하고
          거기서 나오는 색은 아무도 안 쟀는데, **작품 캡션(재료·크기·연도)·작품 설명·CV 영문 라벨·연락처**가
          전부 `sub` 다. 옛 상수 `mix(ink,bg,0.45)` 는 흰 배경·검정 글자에서도 **3.90:1**(AA 미달)이었고,
          화면이 '추천'(초록 점)으로 표시하는 29조합 중 **23개(79%)가 미달**, 최저 2.76:1 이었다.
          이제 `towardBg()` 가 **대비를 만족하는 가장 물린 값**을 찾는다(상수 아님).
          `line` 은 헤어라인이라 인쇄 관례(1.2~1.3:1)를 따르되 **하한**을 둔다 — 어두운 배경에서 1.03:1 이라
          사실상 안 보였는데, 그 선이 곧 디자인인 표지가 셋이다(얇은 테두리·명패·가운데 액자).
          강조색 추천 기준도 3.0 → **4.5** 로 올렸다 — 강조색은 10~13px 글자로도 쓰이는데
          3.0 기준 추천 38조합 중 **19개(50%)가 12px 기준 미달**이었다. 초록 점은 약속이므로 가장 가혹한 쓰임에 맞춘다.
        - ⚠️ **표지는 색을 하드코딩하지 말 것** — `theme.bg/ink/sub/accent/line`(팔레트)만 쓴다(`renderCover`/`coverTextBlockHtml` 준수). 밴드 위 글자는 `theme.bg` 로 뒤집는다.
        - ⚠️ **미리보기 스케일은 적용된 판형(design.page)으로** — `PortfolioFormatPicker` 가 `PAGE_DIMS[design.page]` 를 `MakerPreview`/`PreviewModal` 에 넘긴다. 노브(표지축·글꼴·색·판형·밀도·설명·작품글배치·본문정렬) 서로 독립.
        - ⚠️ **PUT /portfolio 는 designConfig 를 '보냈을 때만' 갱신**(백엔드) — 홈페이지 내용 저장(전체 교체)이 이 설정을 안 지운다. 디자인 저장(`designMutation`)만 실어 보내고 포맷 저장은 안 보내 보존. 회귀: `portfolioFormats.test.ts`·`portfolio-artwork.test.ts`.
      - **편집 화면은 좌우 2단**(lg↑): 왼쪽 입력 / 오른쪽 **홈페이지 모양 실시간 미리보기**. lg↓ 에서는 미리보기가 입력란 아래로.
        ⚠️ **작품 사진 관리도 왼쪽 열 안**에 넣는다(업로드 격자는 2열). 바깥에 두면 거기까지 스크롤한 순간
        오른쪽 미리보기가 사라진다 — sticky 는 부모(왼쪽 열) 높이 안에서만 붙기 때문이다.
        ⚠️ 2단 컨테이너에 **`items-start` 를 주지 말 것** — 오른쪽 열이 제 내용 높이로 줄어 sticky 가 움직일 여지가 0이 된다
        (실측: y=800 에서 미리보기가 화면 밖으로). 기본값 stretch 여야 왼쪽 열 높이만큼 늘어난다.
        미리보기는 편집 영역 끝(2단 구간)까지 따라오고, 그 아래 저장바+푸터 구간에서는 자연히 올라간다.
        미리보기는 공개 페이지와 **같은 컴포넌트**(`components/shared/HomepageView.tsx`)를 쓴다 — 따로 만들면 어긋나고,
        실제와 다른 미리보기는 볼 이유가 없다. 저장 전 입력값을 그대로 반영한다.
      - ⚡ **작품 격자는 타이핑할 때마다 다시 그리지 않는다** — 30장이면 한 글자마다 30개 figure/img 가 재조정돼 입력이 밀린다.
        `HomepageView` 가 **내용 지문(artSignature)** 으로 memo 한다(참조가 아니라 내용으로 판단하므로 부모가 객체를 매번 새로 만들어도 안전).
        지문에 캡션·시리즈를 포함해야 작품 정보를 고쳤을 때는 다시 그려진다. 라이트박스 콜백(`onOpenImage`)은 **`useCallback` 필수** — 매 렌더 새로 만들면 memo 가 깨진다.
      - **편집 화면의 저장은 하단 고정 저장바**(`sticky bottom-0`). 폼이 길어(약력·작가노트·한 줄 소개·시리즈·경력 5칸·파일)
        [저장]이 화면 밖으로 나가 있었다. ⚠️ 저장바는 **폼 안이 아니라 섹션 맨 끝**에 둘 것 —
        폼 안에 두면 sticky 가 폼 높이에서 끝나 '작품 사진 관리'로 내리는 순간 **저장 안 된 변경을 안은 채 사라진다**(실측 y=-299).
      - **경력 항목 이름(개인전·단체전…)은 그 아래 줄들보다 커야 한다** — 예전엔 이름이 12px 회색, 내용이 14px 이라
        이름이 더 작았다. 한 열에 두 덩어리가 들어가면 어디서 개인전이 끝나고 수상이 시작되는지 안 읽힌다.
        지금은 이름 14px 진하게+밑줄 / 내용 13px 회색.
      - **경력 배치는 `lib/careerColumns.ts` 의 `splitIntoColumns`** — 공개 페이지·마이페이지 읽기 뷰 공용.
        grid 는 한 **행**의 높이가 제일 긴 칸에 맞춰져(단체전 20줄 vs 개인전 3줄) '수상 및 선정'이 개인전에서 한참 떨어졌다.
        `items-start` 로도 행 높이는 안 줄어든다. **항목 줄 수를 무게로 주고 그때그때 제일 짧은 열에 담는다** —
        단순 라운드로빈은 2열에서 무너진다(수상이 20줄 단체전 아래로 밀려 1180px). 열 수는 `useCareerColumns`(sm 640/lg 1024, Tailwind 와 같은 경계).
    - **찜 목록**: 필터 `전체 / 갤러리 / 공모 / 전시 / 작품`. **찜 취소 시 목록에서 즉시 제거.**
      **좋아요한 작품은 별도 탭이 아니라 여기 '작품' 필터**다(2026-08-27) — 둘 다 '모아둔 것'이라 탭을 나눌 이유가 없었다.
      '전체'에서는 찜한 항목 아래에 좋아요한 작품을 이어 붙인다(카드 모양이 달라 격자는 따로 둔다).
    - ⏹ **'내 리뷰' 탭은 삭제**(2026-08-27). 리뷰 **수정·삭제는 갤러리 상세에서 작성자 본인에게 그대로 열려 있다** —
      없어진 것은 '내 리뷰를 한곳에 모아 보는 목록' 뿐이다. 되살리려면 git 에서 `MyReviewsSection` 을 가져오면 된다.
    - **[내 전시]에서 전시 운영까지 끝낸다**(2026-08-27) — 수락된 전시 카드를 펼치면
      **운영 공지 · 제출 자료 · 정산 확인** 세 블록이 카드 안에 들어온다(`components/operation/ArtistOperationPanel.tsx`).
      작가가 운영페이지에서 하던 일이 이 셋뿐이라 왕복할 이유가 없었다.
        - 세 블록은 운영페이지가 export 하는 **같은 컴포넌트**다. 복제하면 갤러리의 '대신 입력'(proxyFor)과 갈라진다.
        - **지금 할 일만 자동으로 펼친다**(자료 미제출 → 제출 자료 / 정산 요청 대기 → 정산). 전부 펼치면 카드가 수천 px 이 된다.
        - **할 일이 맨 위**다. 지원서(수십 줄)를 먼저 두면 정작 해야 할 게 아래로 밀려서, 지원서는 접어 둔다.
        - 작가가 `/exhibitions/:id/operation/new` 로 오면 **마이페이지로 되돌려 보낸다**(라우트는 살려둘 것 — 아래 참고).
    - **일정 줄은 항상 그린다** — 남은 일정이 없다고 줄째로 빼면 카드 높이가 제각각이 된다
      (실측 130px vs 188px, 목록에서 한 카드만 작아 보였다). 없으면 전시 기간을 대신 적는다.
    - 카드는 **갤러리 [내 공모 운영]과 같은 얼개**다 — 좌 220px 이미지(D-day) / 우 배지·제목·버튼, 그 아래 일정,
      펼치면 작업 영역. 역할만 다르지 하는 일이 같으니 화면도 같아야 배우는 게 한 번으로 끝난다.
    - 🧪 **단계별 확인용 테스트 데이터**: `backend/scripts/seed-artist-stages.ts`
      (심사중·거절·수락/자료미제출·자료제출완료·확정·전시종료·정산요청·정산완료 8단계).
      제목이 전부 `[TEST]` 로 시작하고 `--clean` 으로 남김없이 지운다. **로컬 전용**(NODE_ENV=production 이면 중단).
      ⚠️ 만든 공모는 `status='APPROVED'` 라 **공개 모집공고 목록에도 뜬다** — 확인이 끝나면 반드시 --clean 할 것.
    - 🔑 **알림 링크는 받는 사람에 따라 다르다** (`backend/src/lib/notifyLinks.ts`).
      작가에게 가는 것은 `ARTIST_EXHIBITION_LINK`(`/mypage?tab=applications`), 갤러리·Admin 은 `operationLink(id)`.
      ⚠️ 실수하기 쉽다 — 정산 재촉(`SETTLEMENT_REMINDER`)과 이의 제기(`SETTLEMENT_ISSUE`)는 **오너에게** 간다.
      작가 링크로 바꾸면 오너가 작가 전용 탭으로 튕겨 아무것도 못 한다(실제로 그렇게 바꿨다가 테스트에 걸렸다).
      회귀는 `backend/src/__tests__/notify-links.test.ts` 가 소스를 대조해 잡는다.
      ⚠️ **운영페이지 라우트를 지우지 말 것** — 이 변경 전에 발송된 알림이 옛 주소를 들고 있다.
    - **지원 내역 관리**: 지원한 공모 목록에서 상태 배지 표시 (접수/검토중/수락/거절).
      - **상태 필터**: 전체/접수/검토중/수락/거절 탭으로 필터링 + 각 상태별 카운트 표시.
      - **지원 답변 확인**: 카드 클릭 시 확장하여 내가 입력한 커스텀 답변(텍스트, 선택, 다중선택, 파일) 확인 가능.
      - **공모 상세 이동**: 확장 영역에서 해당 공모 상세 페이지로 바로 이동.
- **Gallery 유저**:
    - **등록 진입점은 전용 페이지**(2026-08-29) — 목록(갤러리/전시/모집공고)·마이페이지의 [등록] 버튼은 **`/galleries/new`·`/exhibitions/new`·`/shows/new`** 로 이동한다(커뮤니티 글쓰기처럼 화면 전환, 새 탭 아님). 이 페이지들은 `MyPage.tsx` 의 `MyGalleriesSection`/`MyExhibitionsSection`/`MyShowsSection` 을 **`createOnly` 모드**(폼만·목록/헤더 숨김)로 렌더하는 export 래퍼(`GalleryRegisterPage` 등, GALLERY 전용). 제출 성공 시 `/mypage?tab=my-...` 로, 취소/뒤로가기는 이전 화면으로. 버튼 스타일은 옅은 빨강 아웃라인 알약(커뮤니티 [글쓰기]와 통일).
    - **갤러리 등록**: [이름, 주소, 소개, 대표자명, 전화번호, 대표 이미지, 지역 태그] 승인 요청.
    - **공모 등록**: [대상 갤러리 선택, 제목, 구분(개인전/아트페어), 공모시작일, 공모마감일, 전시시작일, 전시종료일, 모집인원, 지역, 소개] 승인 요청. **4개 날짜 필드 모두 필수.**
      - **검증 강화**: 미입력 필수 항목을 구체적으로 표시 (줄바꿈 toast + 빨간 테두리/라벨 하이라이트). 입력 시 즉시 에러 해제.
    - **공모 추가정보 (커스텀 필드)**: 공모 등록 시 지원자에게 추가 요청할 정보 설정 가능.
      - 필드 타입: **텍스트**(text, 글자수 제한 설정 가능 / maxLength > 200이면 textarea로 자동 렌더링), **선택형**(select, maxSelect로 단일/복수 제어: 1=단일선택 라디오버튼, 2+=최대N개 체크박스, 0=무제한 체크박스), **파일 업로드**(file)
      - 각 필드에 라벨, 필수 여부, 옵션(선택형 시) 개별 추가/삭제 가능
      - 텍스트 필드: `maxLength` 속성으로 글자수 제한 (0 = 무제한). 지원 모달에서 실시간 글자수 카운트 표시.
      - 선택형 필드: `maxSelect` 속성으로 동작 제어 (1=단일선택 라디오버튼, 2+=최대N개 체크박스, 0=무제한 체크박스). 초과 시 체크박스 비활성화 + 카운트 표시.
      - **maxSelect vs 옵션 수 검증**: 최대 선택 수가 옵션 수보다 많으면 등록/수정 시 경고 후 차단.
      - 게시(승인) 후에도 Gallery 오너가 커스텀 필드 수정 가능 — **공유 모달** `components/shared/CustomQuestionsEditor.tsx`(default export `CustomQuestionsEditModal`)를 **MyPage 내 공모 카드의 [지원자 관리] 패널 안 "추가 질문 수정" 버튼**에서 호출 → `PATCH /exhibitions/:id/custom-fields`. (카드 상단 버튼이었는데 2026-08-28 패널 안으로 옮겼다 — "질문은 등록 때 받는데 왜 있냐"는 지적에, 기능은 유지하되 눈에 덜 띄게. 공고 상세 페이지에는 진입점 없음 — 마이페이지 전용). 단일선택(`type:'select'`,maxSelect 1)↔다중선택(`type:'multiselect'`)은 "중복 선택 허용" 토글로 전환, `maxSelect`도 편집. 등록 폼과 동일한 `CustomQuestionBuilder`/`sanitizeCustomFields`를 공유. 기존 지원자 답변은 유지됨.
      - **백엔드 Zod 스키마**: `customFieldSchema`에 `maxLength`, `maxSelect` optional 필드 포함 → DB 저장/조회 시 유실 방지.
    - **지원자 관리**: 공모별 지원자 목록 조회, 커스텀 답변 확인, 상태 변경 (접수 → 검토중 → 수락/거절).
      - **엑셀(CSV) 다운로드**: 지원자 목록을 CSV로 내보내기 (이름, 이메일, 지원일, 상태, 커스텀 답변 포함). BOM 포함 UTF-8 인코딩.
      - API: `GET /exhibitions/:id/applications`, `PATCH /exhibitions/:id/applications/:appId`
    - **상태 확인**: 승인 대기 / 승인 거절(**거절 사유 확인 가능**) / 승인 완료 상태 제공.
    - **수정 정책**: 승인 후 상세 내용만 수정 가능. 초기 정보 수정은 Admin에게 수정 요청 기능 사용.
- **Admin 유저**:
    - **승인 큐**: 갤러리/공모 등록 및 수정 요청 승인/거절(**거절 시 사유 작성 필수**).
    - **운영 관리**: Hero Section, 혜택 목록 관리 시 **[등록 전 미리보기]** 기능 제공.
    - **이달의 갤러리**: 갤러리 검색 선정 및 **등록 기한 만료 시 자동 제거** 로직 구현.
    - **할 일 보드**: 마이페이지 '할 일 보드' 탭 (`components/admin/KanbanSection.tsx`). Admin 계정끼리 회의 내용·할 일 정리.
      - **화면은 한 장뿐** — 보드를 클릭해 들어가는 단계가 없다. 보드 목록에 각 보드의 할 일이 그대로 펼쳐지고 거기서 다 한다. 머리말을 눌러 접을 수 있고 접은 상태는 localStorage 에 남는다.
      - 그래서 `GET /kanban/boards` 가 **항목·세부항목까지 통째로** 준다(보드별로 또 부르면 N+1). 쿼리 키는 `['todo-boards']` 하나.
      - **보드 여러 개**(회의별·주제별), 각 보드는 **체크박스 목록 하나**. 항목에 담당자(Admin만)·마감일·본문·댓글·세부항목.
      - **세부항목은 목록 화면에 그대로 펼쳐진다** — 모달을 열지 않고 거기서 바로 추가·체크·이름수정·삭제. 상위 줄엔 `2/3` 배지.
      - **세부를 다 체크해도 상위 항목은 자동 완료 안 됨** — 확인·보고가 남을 수 있고, 자동으로 목록에서 사라지면 되돌리기 번거롭다.
      - 세부항목은 **체크해도 자리가 안 바뀐다**(상위와 다름). 서너 줄에서 줄이 튀면 다음 걸 누르기 힘들다.
      - **완료는 `doneAt` 하나로만 판정** — 상태 필드를 따로 만들지 말 것. 두 곳에 두면 어긋난다.
      - **체크해도 `position` 은 안 바꾼다** — 완료 항목은 정렬로만 뒤로 간다. 실제로 줄을 옮기면 **체크를 푸는 순간 원래 자리로 못 돌아온다**. [완료한 항목 숨기기] 토글 제공.
      - **순서 변경은 전체 순서 id 배열**을 통째로 보낸다(`PATCH /boards/:id/reorder`). index 로 주고받으면 화면(완료가 뒤로 밀림)과 실제 position 이 어긋난다.
      - **이동 두 경로**: 드래그앤드롭(데스크톱) + 줄의 `↑ ↓` 버튼(모바일). HTML5 DnD 는 터치에서 안 되므로 버튼을 지우면 폰에서 순서를 못 바꾼다. 목록 컨테이너의 `onDragOver` 에서 목표 위치를 덮어쓰지 말 것(여백에서도 떠서 맨 아래로 튄다).
      - 드래그는 **같은 보드 안에서만** — 보드를 건너뛰면 항목의 소속이 흔들린다.
      - ⚠️ `routes/kanban.ts` 는 **라우터 전체가** `authenticate, authorize('ADMIN')` 뒤에 있다. 새 엔드포인트를 추가하면 `__tests__/kanban.test.ts` 의 접근 제어 전수 목록에도 넣을 것 — 빠지면 내부 회의 내용이 그대로 샌다.
      - 이름이 `kanban` 인 것은 처음에 3열 칸반으로 만들었던 흔적(2026-08-21 당일 변경). **열 개념은 없다.**
    - **개발자 도구**: 마이페이지 '개발자 도구' 탭의 **[수락 상태 되돌리기 허용]** 전역 토글(`AppSetting` key-value, `GET/PUT /api/admin/dev-settings`). ON이면 **전체 갤러리**가 수락한 지원을 거절로 되돌릴 수 있음 — 되돌리면 해당 작가의 운영페이지 제출물·판매·정산 데이터 삭제 + 정원 슬롯 복구 (정산 완료 공모는 불가). 갤러리 UI는 `GET /api/settings/flags`로 토글 상태 확인.

### 갤러리 스카우팅 — 하트로 모으고 초대 (2026-08-28 개편)
- **갤러리의 비공개 스크랩(북마크)을 없앴다.** 갤러리도 이제 **하트(좋아요)** 로 작품을 모은다.
    - `ArtworkDetailModal` 에서 갤러리 전용 북마크 버튼 제거(`scrapMutation`·`isScrapped` 표시 삭제). 백엔드 `/explore/scraps`·`/explore/:id/scrap` 라우트와 `explore-engagement.test.ts` 는 **그대로 살려 둔다**(화면만 뗐다).
    - ⚠️ **작품 모달에서 '바로 공모 초대'하던 버튼도 없앴다(2026-08-29).** 남은 액션은 [이웃(FollowButton iconOnly)]·[메시지]·[하트]. 초대는 **공모 쪽에서만** 한다.
    - ⚠️ **트레이드오프**: 스크랩은 비공개였지만 하트는 **작가에게 보인다**(포트폴리오의 '좋아요한 사람' 명단). "관심을 숨길 이유가 없다"는 판단이라 의도된 것. 회귀는 `e2e/26-invite-engagement.spec.ts` D.
- **[관심 작품] 탭(`tab=scraps`, 라벨 유지)** = 하트 보드. `ArtworkScrapsSection` 이 `/explore/my-likes` 를 읽는다(작가 [찜 목록]의 '좋아요한 작품'과 같은 소스). 개수·'저장 사실 비공개' 문구 대신 **"저장한 작품의 작가에게는 전시 초대를 보낼 수 있습니다."** 안내 + 카드마다 [전시 초대] 버튼.
- **⚠️ 초대 관문(2026-08-29, `lib/inviteEligibility.ts`)**: 갤러리는 **좋아요(하트)했거나 서로 이웃**인 작가만 초대할 수 있다.
  아무나 검색해 부르지 못하게 하는 **서버 단일 관문**(`POST /exhibitions/:id/invite`, 아니면 **403**). 두 예외: ①Admin ②Admin 주최 공모(`hostType='ADMIN'`)의 위임 갤러리(관리자 큐레이션이라 자유 초대). 회귀는 `explore-engagement.test.ts`.
- **초대 두 방향** — 둘 다 `POST /exhibitions/:id/invite {artistId}`(위 관문 적용):
    - **작가 고정 + 공모 선택** = `InviteModal`(export, MyPage 받은 초대 경로에서 사용). 작품 모달의 '바로 초대' 버튼은 뗐다.
    - **공모 고정 + 작가 선택** = `ExhibitionInviteModal`(MyPage) — 내 공모 [지원자 관리] 패널 안 [작가 초대]. 대상은 **하트를 저장한 작가**(`/explore/my-likes` 를 작가 단위로 dedupe). 모집 마감·정원·중복은 서버가 막고 모달이 사유를 띄운다.
- **⚠️ `like` 는 토글이라** 테스트에서 같은 이미지를 두 번 좋아요하면 취소된다. E2E 는 `seedGalleryLike`(`e2e/lib/helpers.ts`)로 **매번 새 작품**에 하트를 눌러 my-likes 를 확실히 채운다.
- **내 공모 [상세 운영]은 페이지 이동이 아니라 카드 안 인라인 아코디언**(2026-08-28). `OperationPage` 의 본문을 `export function OperationBody({ id, embedded })` 로 분리하고, 기본 export 는 그걸 감싸는 얇은 래퍼다. 카드에서 `<OperationBody id embedded />` 로 임베드(머리말·최대폭 제거). ⚠️ **`/operation/new` 전용 라우트는 유지 필수** — 옛 알림 10곳이 그 주소를 가리킨다(`/operation` 는 별개의 옛 classic 페이지). 회귀 `e2e/35-gallery-exhibition-ops.spec.ts`.

### 3. 서비스 화면별 세부 동작
- **갤러리 찾기**:
    - 그리드: 반응형 masonry 레이아웃 (모바일 1열, 태블릿 2열, 데스크톱 3열). 세로형 카드: 상단 이미지(4:3), 하단 정보(이름, 주소, 전화번호, 한줄소개, 찜하기, 별점).
    - 필터: 지역(서울, 경기 북/남, 대전, 부산), 별점(3/4점+). **현재 적용된 필터 목록을 화면에 표시.**
    - 정렬: 별점순 나열 기능.
- **갤러리 상세**:
    - 상단 스와이프 사진, 우상단 하트(찜), 상세 소개(주인일 때만 수정 버튼 노출), 진행 중 공고(D-day).
    - 전시 종료 후 **홍보용 사진 및 후기 글 등록** 기능 (Gallery 유저 전용).
    - **리뷰**: Artist 전용. [별점, 사진(옵션), 텍스트]. 익명 시 `익명의 예술가 N` 표기. Admin은 삭제 버튼 노출.
- **모집 공고**:
    - D-day가 남은 공고만 노출. 갤러리 별점 및 지역 필터 제공.
    - **지원하기**: 클릭 시 갤러리 오너에게 인앱 알림(NEW_APPLICANT) 발송 (Artist 전용). ※ 포트폴리오 이메일 자동전송 기능은 제거됨(2026-07, mailer 삭제).
    - **커스텀 필드 지원**: 공모에 추가정보 항목이 있으면 지원 시 모달에서 입력.
      - 텍스트: 글자수 제한 시 실시간 카운트 표시, maxLength > 200이면 textarea로 자동 전환.
      - 선택형(select): maxSelect=1이면 **라디오 버튼**, 2+/0이면 **체크박스**. 최대 선택 수 도달 시 나머지 비활성화 + 카운트 표시.
      - **검증 강화**: 제출 시 모든 필수/글자수/선택수 오류를 한번에 수집하여 줄바꿈 toast로 표시. 오류 필드에 빨간 테두리(ring) + 라벨 색상 강조.
      - **커스텀 답변 표시**: 긴 텍스트 overflow 방지 (`whitespace-pre-wrap break-all`), 배경 박스(`bg-gray-50 rounded px-2 py-1`)로 가독성 향상.
    - **지원 상태**: Artist는 마이페이지에서 본인의 지원 상태(접수/검토중/수락/거절) 확인 가능.
