#!/usr/bin/env bash
# push 직전 자동 점검 — DB 데이터·접속정보가 섞였으면 push를 막는다.
# 활성화(최초 1회): git config core.hooksPath .githooks
# 정말 급하면: git push --no-verify  (권장하지 않음 — 왜 걸렸는지 먼저 확인할 것)
exec bash "$(git rev-parse --show-toplevel)/scripts/predeploy-check.sh"
